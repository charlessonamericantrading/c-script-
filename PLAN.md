# Plan de Desarrollo: **c-script** — Lenguaje Backend con End-to-End Type Safety para TypeScript

> Documento de ingeniería. Versión 1.0 · Objetivo: plan realista, detallado y honesto para diseñar y construir un lenguaje backend compilado cuyo diferenciador es la **interoperabilidad de tipos nativa y automática con frontends TypeScript**.

---

## 0. Resumen ejecutivo (léelo primero)

**La idea es buena y llega en buen momento.** El mercado ya demostró que valora la type-safety de extremo a extremo (tRPC, Server Actions, Convex, Encore). El "punto ciego" entre backend y frontend es un dolor real.

**Pero la recomendación honesta es: NO empieces construyendo un lenguaje.** El valor diferencial de tu propuesta no vive en la sintaxis ni en el runtime — vive en **el puente de tipos** (la generación automática de `.d.ts` + cliente RPC + validadores). Puedes entregar el 80% del valor con el 5% del esfuerzo construyendo primero ese puente como **framework + codegen sobre Rust o Go**. Un lenguaje nuevo es la forma más cara, lenta y arriesgada de conseguir ese valor.

| | Construir un lenguaje nuevo | Construir el "puente de tipos" primero |
|---|---|---|
| Tiempo a primer valor | 6–12 meses | 3–8 semanas |
| Esfuerzo hasta algo usable | 4–8 persona-año | 0.3–1 persona-año |
| Riesgo de adopción | Muy alto | Bajo (usas un lenguaje que ya tiene ecosistema) |
| Diferenciador que pruebas | Todo a la vez | Exactamente el que importa (E2E type safety) |

Este documento cubre **ambos caminos**: el plan del lenguaje completo (porque lo pediste) y, en la sección 8, la ruta pragmática que recomiendo de verdad.

**Nota (30/08/2026): la sección 8 ya NO contiene esa ruta alternativa** -- se repurposeó como roadmap de continuación del lenguaje (Fase 4, "Hacia c-script 2.0") cuando el usuario decidió seguir construyendo c-script como lenguaje completo en vez de un framework-puente sobre Rust/Go, camino que para esta fecha ya lleva 149 versiones y cientos de features en producción real. Esta sección 0 queda como registro histórico de la recomendación original, no como guía vigente -- la continuación real de la historia del proyecto vive en las secciones 8 y 9.

---

## 1. Análisis y Research

### 1.1 Comparativa con soluciones existentes

| Solución | Lenguaje backend | Type-safety E2E | Rendimiento | Codegen | Límite principal |
|---|---|---|---|---|---|
| **tRPC** | TS/JS (Node/Bun/Deno) | ✅ inferencia | Runtime JS | No | Backend atado a TS; sin perf de sistemas |
| **Encore.ts** | TS + runtime Rust | ✅ genera cliente | Alto (I/O en Rust) | Parcial | Sigue siendo TS; opinado |
| **Convex** | TS | ✅ | Medio (gestionado) | No | Plataforma cerrada; TS |
| **Elysia + Eden** | TS (Bun) | ✅ inferencia | Alto (Bun) | No | Solo TS |
| **Nestia / typia** | TS (Nest) | ✅ SDK gen | Runtime JS | Sí | Solo TS |
| **Rust + ts-rs / specta** | Rust | ⚠️ solo tipos | Muy alto | Sí | No genera cliente RPC ni valida el cable |
| **Go + tygo / oapi-codegen** | Go | ⚠️ vía OpenAPI | Muy alto | Sí | Contrato indirecto, no nativo |
| **gRPC + protobuf / Connect** | Cualquiera | ✅ vía IDL | Muy alto | Sí | IDL aparte; DX web pobre; no idiomático en TS |
| **Fern / Smithy** | Cualquiera | ✅ vía IDL | N/A | Sí | Escribes un IDL separado del código |
| **WASM Component Model (WIT)** | Cualquiera | ✅ vía WIT | Alto | Sí | Inmaduro; overhead de bindings |
| **c-script (propuesto)** | **Nuevo, compilado** | **✅ nativo** | **Alto (nativo/WASM)** | **Automático** | **Coste de crear un lenguaje** |

### 1.2 El gap que justificaría un lenguaje nuevo

Ninguna solución actual reúne **las tres cosas a la vez**:

1. **Rendimiento y seguridad de lenguaje de sistemas** (Rust/Go), y
2. **Type-safety E2E con TypeScript sin escribir un IDL aparte ni interfaces duplicadas**, y
3. **DX de "llamada a función" transparente** (no `fetch`, no `.proto`, no anotaciones manuales).

- Las soluciones **TS-first** (tRPC, Convex, Elysia) dan (2) y (3) pero no (1).
- Las soluciones **codegen desde Rust/Go** (ts-rs, tygo, gRPC) dan (1) pero pagan un IDL/anotación separado y no logran del todo (3).

El hueco real es: *"un lenguaje donde el sistema de tipos se diseñó desde el día 1 para ser isomórfico con TypeScript, de modo que el contrato ES el código."*

### 1.3 Riesgos principales (resumen; detalle en §7)

- **Adopción**: el mayor asesino de lenguajes nuevos. Sin ecosistema (DB, auth, libs) nadie migra.
- **Coste**: se subestima sistemáticamente en 3–5×.
- **Mantenimiento**: un lenguaje es un compromiso de años, no de meses.

---

## 2. Diseño del Lenguaje

### 2.1 Filosofía

1. **El contrato es el código.** Definir un tipo en el backend *es* definir su interfaz TS. Cero duplicación.
2. **Isomorfismo con TypeScript por diseño.** Cada construcción del sistema de tipos tiene un mapeo canónico y estable a TS.
3. **Romper en compilación, nunca en producción.** Un cambio incompatible en el backend debe romper el `tsc` del frontend.
4. **Seguro y rápido por defecto.** Memory-safe, sin data races evidentes, rendimiento de sistemas.
5. **Serialización predecible.** Todo tipo público tiene una forma JSON determinista y validable.

### 2.2 Sintaxis (borrador)

```rust
// users.link — un módulo = un contrato
type User = {
  id: Int,
  name: String,
  email: String,
  role: Role,
  createdAt: Timestamp,
}

// enum simple  ->  unión de literales string en TS
enum Role { Admin, Member, Guest }

// enum con datos (ADT)  ->  unión discriminada en TS
enum Result {
  Ok  { value: User },
  Err { code: Int, message: String },
}

// `service` expone RPCs. Cada rpc público entra al contrato .d.ts
service Users {
  // parámetro con default -> parámetro opcional en TS
  rpc list(limit: Int = 20) -> [User] {
    db.users.all().take(limit)
  }

  // T? -> T | null (decisión de diseño, ver §2.3)
  rpc getById(id: Int) -> User? {
    db.users.find(id)
  }

  rpc create(input: NewUser) -> Result {
    match validate(input) {
      Ok(u)  => Result.Ok { value: db.users.insert(u) },
      Err(e) => Result.Err { code: 422, message: e },
    }
  }

  // streaming -> AsyncIterable en el cliente TS
  stream watch(id: Int) -> User {
    db.users.subscribe(id)
  }
}
```

### 2.3 Sistema de tipos — el mapeo 1:1 a TypeScript (el corazón del proyecto)

> **Esta tabla es el borrador original de la propuesta, no el estado real.**
> La tabla de mapeo vigente y exhaustiva vive en [GRAMMAR.md §4](GRAMMAR.md);
> ahí está lo que el compilador de verdad emite. Se conservan acá las filas
> tal como se propusieron, marcando cuáles no se construyeron -- borrarlas
> escondería qué se prometió al principio y qué no llegó.

| Tipo en c-script | Tipo en TypeScript | Forma JSON | ¿Existe? |
|---|---|---|---|
| `Int`, `Float` | `number` | número | sí |
| `Int64`, `BigInt` | `bigint` \| `string`* | string (para no perder precisión) | sí, como `Int64` — pero TS `string`, no `bigint` (GRAMMAR.md §3.30: el cliente generado no tiene hoy ningún punto de (de)serialización dirigido por tipo, y `string` no necesita ninguno; `bigint` real queda como ronda futura separada) |
| `String` | `string` | string | sí |
| `Bool` | `boolean` | bool | sí |
| `[T]` | `T[]` | array | la sintaxis real es postfija: `T[]` |
| `{K: V}` (map) | `Record<K, V>` | objeto | la sintaxis real es `Map<K, V>` — `{K: V}` como literal de tipo se descartó por ambigüedad con un struct de un campo (GRAMMAR.md §2.2) |
| `(A, B)` (tupla) | `[A, B]` | array | sí |
| `type X = { ... }` | `type X = { ... }` (estructural) | objeto | sí (se emite como `interface`) |
| `enum E { A, B }` | `type E = "A" \| "B"` | string | sí |
| `enum` con datos | unión discriminada con `type` tag | objeto etiquetado | sí |
| `T?` | `T \| null` **(decisión, ver abajo)** | `null` presente | sí |
| `field?: T` | `field?: T` (clave ausente) | clave omitida | sí |
| `Timestamp` | `string` (ISO-8601) \| branded | string | sí -- string plano, no branded (GRAMMAR.md §3.31: mismo criterio minimalista que el resto del proyecto, revisar si aparece un caso real que pida la distinción nominal) |
| `Void` / `Unit` | `void` | — | `Void` sí; solo como retorno completo de un rpc (GRAMMAR.md §4.1) |

**\* La decisión de diseño más importante y con más matices** es cómo representar **ausencia**. TypeScript distingue tres cosas que JSON no distingue bien:

- `field: T | null`  → la clave **existe**, el valor es `null`.
- `field?: T`        → la clave puede **no existir** (`undefined`).
- `field?: T | null` → ambos.

Esto afecta a la serialización, a los validadores generados y a la DX. Es una bifurcación real con trade-offs, no un detalle. (En §8.3 esta es exactamente la decisión que te propongo implementar tú en el PoC.)

**ADT → unión discriminada.** El `enum Result` de arriba genera:

```typescript
export type Result =
  | { type: "Ok";  value: User }
  | { type: "Err"; code: number; message: string };
```

La convención del tag (`type` vs `kind` vs `_tag`) debe ser configurable pero con un default estable.

### 2.4 Modelo de ejecución

Tres candidatos, en orden de "rápido de construir" → "rápido de ejecutar":

| Estrategia | Perf runtime | Esfuerzo | Cuándo |
|---|---|---|---|
| **Intérprete** (tree-walking / bytecode) | Baja–media | Bajo | **MVP (Fase 0)** — prueba el valor ya |
| **Transpilar a Go/Rust** y usar su toolchain | Muy alta | Medio | Alternativa de Fase 1 con menos riesgo |
| **Compilar a WASM** (`wasm-encoder` directo, o recompilar el runtime a `wasm32-wasip1`) | Alta | Alto | Edge/serverless; Fase 1–2 |
| **Compilar a nativo** (LLVM) | Máxima | Muy alto | Fase 2–3 |

**Recomendación:** MVP interpretado o transpilado a Go. El backend de compilación "de verdad" hacia WASM llega en Fase 1, **después** de haber probado el killer feature. No inviertas en LLVM hasta que la propuesta de valor esté validada.

**Corrección (post-Fase-0):** esta sección originalmente decía "Cranelift → WASM" como si fueran la misma cosa — no lo son. Cranelift solo genera código de máquina NATIVO (x86/arm/etc.); `cranelift-wasm` CONSUME bytes `.wasm` como entrada para traducirlos a Cranelift IR (así es como wasmtime hace JIT), no los produce. Para emitir un `.wasm` real hay dos caminos genuinos: `wasm-encoder` (autoría directa de bajo nivel, activamente mantenido, usado por 800+ crates) para codegen nativo hacia wasm; o, como v0 pragmático — y el que efectivamente se usó en Fase 1 — recompilar el runtime interpretado existente al target `wasm32-wasip1` y alimentarle el programa ya parseado como dato, el mismo camino que siguieron RustPython y Boa para su primera versión real en wasm. Cranelift sigue siendo la opción correcta si lo que se quiere es codegen nativo (vía `cranelift-jit`/`cranelift-object`), no como atajo hacia wasm.

**v0 implementado y verificado (Fase 1):** `compiler/src/bin/wasm_demo.rs` -- el intérprete existente (`Rc<RefCell<Value>>` en runtime/mod.rs, `Mutex<Vec<Value>>` en db.rs) compila tal cual para `wasm32-wasip1` y ejecuta una llamada RPC real (`Users.getById`) de punta a punta dentro de `wasmtime`, devolviendo el JSON correcto. Requirió partir el paquete en `[lib]` + `[[bin]]` (`compiler/src/lib.rs`) porque un binario en `src/bin/*.rs` compila como un crate aparte del binario principal, aunque viva en el mismo paquete Cargo. El demo usa `include_str!` (embebido en tiempo de compilación) en vez de leer el archivo `.link` en runtime, a propósito: **no probado, y deliberadamente no perseguido más allá de esta ronda** -- correr la suite de tests completa (`cargo test --target wasm32-wasip1`) falla, porque varios tests existentes sí leen archivos de fixture del disco en runtime (`env!("CARGO_MANIFEST_DIR")` + `fs::read_to_string`), y un sandbox WASI no tiene acceso al filesystem del host salvo que se le otorgue explícitamente ("preopens" de directorio) -- eso es trabajo real y separado (configurar qué directorios exponer y a qué tests), no algo que debiera colarse a medio hacer acá. Tampoco prueba nada de concurrencia real (wasm32 es de un solo hilo) ni toca networking (`tiny_http` asume sockets de SO reales; WASI Preview 2 (`wasi:http`) es la vía idiomática si se retoma esto, adaptador aparte).

---

## 3. Arquitectura Técnica del Compilador

### 3.1 Pipeline completo

```
código .link
   │
   ▼
[1] Lexer ───────────► tokens
   │
   ▼
[2] Parser ──────────► AST (sintaxis)
   │
   ▼
[3] Análisis semántico
    · resolución de nombres/módulos
    · type checking / inferencia
    · chequeos de "contrato" (¿es serializable?) ─► AST TIPADO
   │
   ├──────────────► [4a] Codegen backend ──► WASM / nativo / bytecode  (SERVIDOR)
   │
   └──────────────► [4b] Emisor de contrato ──► .d.ts + cliente TS + validadores  (CLIENTE)
```

La clave arquitectónica: **[4b] es un pass de primera clase**, no un añadido. Recorre el AST tipado y emite:
1. `contract.d.ts` — los tipos.
2. `client.ts` — un cliente RPC tipado (thin wrapper sobre `fetch`/WebSocket).
3. `validators.ts` — guardas runtime generadas desde los tipos (esto es lo que hace la seguridad *real* en el borde, no solo compile-time).

### 3.2 Tecnologías recomendadas por etapa

| Etapa | Recomendación | Alternativas |
|---|---|---|
| Lenguaje del compilador | **Rust** (ecosistema de compiladores maduro) | Zig, OCaml, TS |
| Lexer | `logos` (Rust) o hand-written | — |
| Parser | recursivo descendente hand-written o `chumsky` | `lalrpop`, tree-sitter |
| Representación | AST tipado propio + arena (`id-arena`) | — |
| Type checker | propio (algoritmo bidireccional) | — |
| Backend de código | **Cranelift** (compila rápido, código NATIVO — no emite `.wasm` directamente, ver nota de §2.4) | LLVM (más perf, más complejo; sí tiene target `wasm32` real), transpilar |
| Emisión `.d.ts` | pass propio sobre AST tipado | — |
| Validadores runtime | generar TS estilo Zod/typia | — |
| Serialización wire | JSON (MVP) → luego formato binario opcional | MessagePack, Protobuf |

### 3.3 Cómo se implementa la generación de contratos (concreto)

Un mapeo puro `TipoLink → TipoTS` recorriendo el AST tipado. Núcleo (pseudocódigo Rust):

```rust
fn emit_ts(t: &Type) -> String {
    match t {
        Type::Int | Type::Float => "number".into(),
        Type::String            => "string".into(),
        Type::Bool              => "boolean".into(),
        Type::List(inner)       => format!("{}[]", emit_ts(inner)),
        Type::Optional(inner)   => format!("{} | null", emit_ts(inner)), // ← decisión §2.3
        Type::Named(name)       => name.clone(),
        Type::Enum(variants)    => emit_discriminated_union(variants),
        // ...
    }
}
```

El servidor RPC y el `client.ts` comparten el mismo emisor de tipos, garantizando que **no puedan divergir**.

---

## 4. Roadmap por Fases

| Fase | Duración | Objetivo | Entregable clave | Equipo mínimo |
|---|---|---|---|---|
| **Fase 0 · MVP** | 2–4 meses | Probar el killer feature E2E | Lexer + parser + checker mínimo · emisor `.d.ts` + cliente · runtime interpretado o transpilado · **1 demo full-stack donde cambiar el backend rompe `tsc`** | 1–2 ing. de compiladores |
| **Fase 1 · Alpha** | +4–6 meses | Compilar de verdad | Backend a WASM (v0: runtime recompilado a `wasm32-wasip1`; codegen directo vía `wasm-encoder` como evolución) · runtime RPC HTTP · std mínima (los builtins ya existentes en v0: `.length()`/`.contains()`/`.toFloat()`/`.toInt()`/`db.*`) · CLI (`linkc new/dev/build`) · LSP básico | 2–3 |
| **Fase 2 · Beta** | +6–9 meses | Usable en proyectos reales | DB tipada · auth · WebSocket/SSE · validadores runtime · hot reload · LSP completo · package manager · observabilidad | 3–5 |
| **Fase 3 · 1.0** | +6–12 meses | Producción | Estabilidad de sintaxis · ecosistema · docs · deploy edge/serverless · debugging con source maps | 4–6+ |

**v0 implementado y verificado (Fase 2, de los ítems de esta fila: validadores runtime, DB tipada, WebSocket/SSE y auth):** ver GRAMMAR.md §3.11 (`validators.ts`), §3.12 (`db { ... }`, tipado -- ver §3.17 para la persistencia real), §3.13 (`stream` sobre SSE real, no WebSocket -- HTTP chunked transfer alcanza para "una secuencia ya calculada, sin suscripción a eventos futuros", que era el alcance de esa ronda), §3.15 (constructo de loop `while`, prerrequisito del ítem siguiente), §3.16 (push real: eventos genuinamente futuros, resuelto TAMBIÉN sobre SSE -- la suposición original de que esto necesitaría WebSocket resultó innecesaria para el shape fijo `while true { db.<col>.subscribe() }`; WebSocket seguiría haciendo falta solo para push BIDIRECCIONAL, que sigue sin construirse), §3.17 (`db` sobre SQLite real -- `rusqlite`, persistencia genuina entre reinicios, único backend nativo+wasm32 verificado con un spike real) y §3.14 (`@authenticated`/`@requires(Role.X)` + sesiones opacas en memoria, sin JWT).

**`LSP completo` de esta misma fila: RESUELTO, Nivel 1+2+3 -- los 3 niveles, no solo los primeros dos.** Los 3 prerrequisitos (spans+diagnósticos, recuperación de errores del parser, spans en AST/checker) más el protocolo en sí (`linkc lsp` -- JSON-RPC sobre stdio, diagnósticos con imports resueltos de verdad vía `load_program_with_overlay`) ya estaban -- ver GRAMMAR.md §3.19 para el diseño Nivel 1+2 (hover/completion/goto-def a nivel de declaración). El Nivel 3 que esa sección dejaba explícitamente pendiente (goto-def de un nombre de tipo en una firma, hover de una expresión arbitraria en medio de un body, completion sensible al tipo real tras `x.`) se resolvió completo en 3 rondas encadenadas -- GRAMMAR.md §3.21, §3.24, §3.25 -- la última (hover/completion) reusando una única instrumentación mínima del checker bidireccional (`Checker::hover_type_at`, un "probe" en los dos puntos de entrada unificados `synth_expr`/`check_expr`) en vez de reimplementar las reglas de scoping en el LSP. Identidad de archivo en `Span` (GRAMMAR.md §3.22) y spans en `Field`/`Param` (§3.23) fueron los dos prerrequisitos que esa ronda expuso y cerró de paso.

**"Backend a WASM" de esta fila: decisión de roadmap tomada, ambos caminos con su rol final.** El "codegen directo vía `wasm-encoder`" que esta fila nombraba como evolución futura existe en v0 (`linkc wasm`, GRAMMAR.md §3.20) -- alcance mínimo a propósito (solo aritmética entera sobre `Int`/`Bool`, una función es una sola expresión). Auditoría post-push: se decide CONGELARLO ahí, no seguir invirtiendo -- cerrar la brecha hasta soportar un programa real (statements, `String`/structs/`db`, llamadas entre funciones) es en la práctica escribir un backend de codegen nativo completo, meses de trabajo, no una extensión incremental. El target `wasm32-wasip1` (recompilar el intérprete entero) es y sigue siendo el ÚNICO camino real de producción -- ya corre un programa real de punta a punta dentro de `wasmtime` (§2.4). Ver GRAMMAR.md §3.20 para el razonamiento completo, incluyendo por qué `cranelift-jit`/`cranelift-object` (no `wasm-encoder`) seguiría siendo la herramienta correcta si algún día hace falta codegen nativo de verdad.

**`package manager` de esta fila: RESUELTO en su forma "Git-as-registry", no un registro en red centralizado.** `link.json` ahora acepta `git+<url>#<rev>` como dependencia real, además de una ruta local -- resuelto invocando el binario `git` real (clonar/fetch/checkout, GRAMMAR.md §2.1) contra un caché local por proyecto, con `link.lock` grabando el commit exacto resuelto. Sin un registro centralizado tipo npm/crates.io (decisión consciente, mismo espíritu que evitar infraestructura nueva sin necesidad real) -- la URL git ES el "registro", igual que en Go. Queda como límite de v0, no como brecha a cerrar de forma incremental: `link.lock` es informativo (no un pin real que sobreviva a un `rev` de rama que avanzó) y no hay locking entre procesos concurrentes -- ver GRAMMAR.md §2.1 para el detalle completo.

**`observabilidad` de esta misma fila: RESUELTO, v0.** Tracing estructurado por RPC (`[req N] method=Users.create status=200 duration_ms=7`, GRAMMAR.md §3.26) sobre el `req_id` que ya existía como prerrequisito parcial -- formato `clave=valor` greppable, sin sumar `tracing`/OpenTelemetry todavía. Queda para una ronda futura: salida JSON, niveles de log configurables, y métricas agregadas -- ver GRAMMAR.md §3.26 para el detalle completo de qué falta.

**`hot reload` de esta misma fila: RESUELTO, v0.** `linkc dev <archivo> <outdir> [puerto]` (GRAMMAR.md §3.27) -- con el puerto opcional, cada rebuild exitoso reinicia un `linkc serve` hijo real con el programa actualizado (restart de proceso, no un hot-swap en memoria -- decisión deliberada para no tocar el modelo de threading de `runtime/server.rs`, ver §3.13). Un rebuild fallido nunca tira abajo el servidor. Con esto, los OCHO ítems de esta fila de Fase 2 (DB tipada, auth, WebSocket/SSE -- vía SSE real, no WebSocket, ver §3.13/§3.16 -- validadores runtime, hot reload, LSP completo, package manager, observabilidad) tienen al menos una v0 real y verificada -- cada uno con sus propios límites honestos documentados en su sección de GRAMMAR.md correspondiente, no pendientes de "empezar" en el sentido en que lo estaban al escribir este roadmap originalmente.

**`Testing` de §5 ("runner integrado + tests de contrato"): RESUELTO EN AMBAS MITADES, v1.0.0.**
1. **Tests de contrato**: `linkc test <archivo> <snapshot> [--update]` (GRAMMAR.md §3.29) compara el contrato emitido contra un snapshot commiteado a git con diff LCS línea a línea. Dogfooded en CI en cada commit (`examples/users.link.snap`).
2. **Runner integrado de comportamiento**: `test "nombre" { ... }` (GRAMMAR.md §3.33) con builtins `assert(cond, msg)` y `panic(msg)`, invocación directa de servicios `Service.rpc(...)`, y aislamiento automático de base de datos (`:memory:`) por test al ejecutar `linkc test <archivo.link>`.

**`Fase 3 · 1.0 (Producción)`: RESUELTO y publicado en v1.0.0.**
- Sistema de tipos bidireccional completo con inferencia, subtipado estructural para structs, nominal para enums, uniones `A | B`, genéricos monomorfizados y closures reales de primera clase.
- Tipos `Timestamp` (ISO-8601 UTC) e `Int64` (mismo rango 64-bit sin pérdida de precisión en TS), más builtin `now() -> Timestamp` (GRAMMAR.md §3.30–§3.32).
- Toolchain integral: `linkc build`, `serve`, `test`, `dev`, `lint`, `doc`, `docker`, `lsp`, `new`, `fmt`.

**Evolución Post-1.0 (v1.1.0 a v1.28.0) — Capacidades Enterprise y Cierre de Gaps Reales:**
- **PostgreSQL en Runtime** (v1.1.0/v1.4.0/v1.8.0, GRAMMAR.md §3.36/§3.40/§3.44): Soporte de base de datos PostgreSQL real (`--db postgres://...`), auto-migraciones de esquema no destructivas, TLS oportunista y obligatorio vía `rustls` puro (compatible con Supabase/Neon/RDS), auto-reconexión transparente tras corte y LISTEN/NOTIFY en hilo dedicado para sincronizar `stream` SSE entre múltiples instancias.
- **Criptografía y Seguridad** (v1.1.0/v1.3.0/v1.5.0, GRAMMAR.md §3.34/§3.38/§3.41): Argon2id (RFC 9106) con sal aleatoria en formato PHC y verificación en tiempo constante; tokens de sesión y UUIDs alimentados por el CSPRNG del sistema operativo (`getrandom`); cálculo de HMAC-SHA256 para verificación de webhooks; CORS con allowlist configurable (`--cors-origin`) y cabeceras de seguridad estrictas fijas (`nosniff`, `DENY`, `no-referrer`).
- **Extensibilidad Web y SEO** (v1.1.0/v1.2.0/v1.6.0/v1.9.0/v1.10.0, GRAMMAR.md §3.35/§3.37/§3.42/§3.45/§3.46): Decorador `@content_type("...")` para respuestas no-JSON (HTML, XML, CSV); URLs amigables `@route("/...")` con múltiples parámetros dinámicos y precedencia determinística; sanitización explícita `String.escapeHtml()`; y selección de status HTTP en éxito `response.setStatus(code)` (e.g. páginas 404 personalizadas o 201 Created).
- **Integraciones y Operaciones** (v1.3.0/v1.7.0/v1.11.0, GRAMMAR.md §3.38/§3.43/§3.47): Lectura de entorno `env.get()`, inspección de cuerpo crudo y cabeceras entrantes `request.rawBody()` / `request.header()`; límite de peticiones `@rate_limit("N/ventana")` con token bucket continuo; envío de correos vía relay `smtp.send(to, subject, body)` con TLS; y peticiones HTTP salientes con cabeceras `http.getWithHeaders` / `http.postWithHeaders`.
- **Motor de Consultas y Autorización Avanzada** (v1.12.0 a v1.18.0, GRAMMAR.md §3.48–§3.54): Paginación empujada a SQL nativo `db.<c>.page(limit, offset)`; agregación analítica nativa con `GROUP BY` en base de datos (`sumBy`, `countBy`, `avgBy`, `maxBy`, `minBy`) preservando tipos reales; autorización con OR de roles `@requires(Role.Admin | Role.Agent)`; expiración temporal de sesiones `--session-ttl` (o `LINK_SESSION_TTL`); introspección de sesión `auth.currentRole() -> String?`, emisión con identidad `auth.createSessionWithId(role, userId)` y lectura de identificador `auth.currentUserId() -> Int?`; y aleatoriedad numérica/comparación segura para código de usuario `crypto.randomInt(min, max)` / `crypto.timingSafeEqual(a, b)`.
- **Ergonomía del Lenguaje y Rutas Avanzadas** (v1.19.0, GRAMMAR.md §3.55–§3.57): Conversión explícita `.toString()` sobre `Int`/`Int64`/`Float`/`Bool` (primer método que existe sobre `Bool` en todo el lenguaje); `response.setStatus` ahora se rechaza en COMPILACIÓN dentro de un `stream` (antes era un no-op silencioso que solo se notaba en producción); segmento catch-all `:nombre*` en `@route` para rutas de profundidad variable (documentación, CMS), con precedencia determinística frente a rutas más específicas y detección de conflictos extendida.
- **Seguridad Configurable y Adopción de Bases Existentes** (v1.20.0, GRAMMAR.md §3.58–§3.59): Costo de `crypto.hashPassword` configurable vía `--argon2-memory-kib`/`--argon2-iterations` (o sus env vars), sin cambiar el comportamiento por default; `crypto.isLegacyHash(hash) -> Bool` para migrar contraseñas viejas de forma proactiva; y una tabla PostgreSQL preexistente con `id SERIAL`/`IDENTITY` de 32 o 16 bits (no solo `BIGSERIAL`) ya no falla en el primer `insert` -- corrige un desacuerdo real entre la validación al conectar (que sí los aceptaba) y la lectura de la columna (que exigía el OID exacto de 64 bits).
- **Inspección de Respuestas HTTP Salientes** (v1.21.0, GRAMMAR.md §3.60): `http.getWithStatus`/`http.postWithStatus` devuelven `{status: Int, headers: {...}[], body: String}` -- un 4xx/5xx de la API llamada deja de ser un error de runtime genérico y pasa a ser un dato que el programa puede inspeccionar (ej. reintentar solo en 429). `http.get`/`http.post`/`http.getWithHeaders`/`http.postWithHeaders` quedan sin cambios.
- **Paginación por Cursor** (v1.22.0, GRAMMAR.md §3.61): `db.<c>.pageAfter(cursor, limit)` -- el cursor es el `id` del último elemento visto (`null` para la primera página), estable bajo escritura concurrente a diferencia de `page(limit, offset)`, que cuenta filas desde el principio en cada llamada. `page` queda sin cambios, sigue siendo la opción correcta para saltar a una página arbitraria.
- **`@route` con Query String** (v1.23.0, GRAMMAR.md §3.62): cualquier parámetro del rpc que no esté en el path se lee de la query string por nombre (`String`/`Int` obligatorio, `String?`/`Int?` opcional). De paso, corrigió un bug real: la query string se coleaba entera dentro del último segmento de path capturado (ej. `/blog/slug?utm_source=x` corrompía `:slug`) -- ahora se separa antes de partir en segmentos, para toda ruta, tenga o no parámetros de query declarados.
- **`smtp` a Varios Destinatarios y HTML** (v1.24.0, GRAMMAR.md §3.63): `smtp.sendToMany(to, subject, body)` manda un mensaje con un `RCPT TO` por destinatario; `smtp.sendHtml(to, subject, html)` manda cuerpo HTML. `send` queda sin cambios. Envío asíncrono sigue pendiente (§8.3.3).
- **Auth Externo (JWT HS256)** (v1.25.0, GRAMMAR.md §3.64): `linkc serve --jwt-secret <secreto>` verifica un JWT ya emitido por un backend existente, junto con -- nunca en vez de -- las sesiones propias. `@requires`/`@authenticated`/`auth.currentRole()`/`currentUserId()` funcionan igual sin importar cuál de los dos autenticó. Solo HS256 (allowlist, no blocklist -- `"alg":"none"` y cualquier otro se rechazan); sin RS256/JWKS, eso queda para una ronda propia si hace falta un proveedor de identidad completo.
- **Agregación: soporte de `Int64`** (v1.26.0, GRAMMAR.md §3.65): `sumBy`/`countBy`/`avgBy`/`maxBy`/`minBy` aceptan `Int64` como campo de agrupación Y de valor -- de paso corrigió un bug real (`scalar_cell_to_value` nunca distinguía `Int64` de `Int` a nivel de storage, así que un resultado `Int64` habría llegado mal etiquetado y serializado como número en vez de string). Truncado de fechas sigue pendiente, ronda propia (§8.2.1) -- los dos backends divergen de verdad para truncar.
- **`linkc introspect`** (v1.27.0, GRAMMAR.md §3.66): genera un `.link` de partida (`type`/`db {...}`) leyendo `information_schema` de una base PostgreSQL ya existente -- solo PostgreSQL, sin FKs/índices/constraints, sin generar ningún `service`, y cualquier columna sin mapeo confiable (`jsonb`, `uuid`, timestamp nativo) sale como `String` con una advertencia explícita en vez de omitirse.
- **`--adopt-existing`: adoptar tablas sin auto-migrar** (v1.28.0, GRAMMAR.md §3.67): `linkc serve --adopt-existing` (o `LINK_ADOPT_EXISTING`) hace que cada colección declarada asuma que su tabla ya existe -- nunca ejecuta `CREATE TABLE` ni `ALTER TABLE`, ni siquiera el tipo no destructivo de siempre, solo SELECTs de solo lectura que confirman que cada columna declarada esté ahí. Resuelve dos bloqueos reales: un rol de base sin permiso de DDL (común en producción), y una tabla SQLite con columnas físicas que el `.link` no modela (antes hacía panic ante cualquier columna de más). Todo o nada por proceso, no valida `NOT NULL`/tipo columna por columna más allá de `"id"` -- límites honestos documentados en GRAMMAR.md §3.67.

**La cronología de versiones sigue de acá en adelante en las secciones 8 y 9** (Fase 4 y Fase 5), que citan cada `v1.X.0` desde v1.29.0 en su propio texto -- esta lista no se actualiza versión por versión más allá de v1.28.0 a propósito, para no duplicar esa cronología en dos lugares.

**Hitos "go / no-go":**
- Fin de Fase 0: ✅ Demo E2E probada y verificada.
- Fin de Fase 1: ✅ Herramientas CLI, LSP inicial y soporte WASI validados.
- Fin de Fase 2: ✅ DB SQLite embebida, SSE reactivo, auth y generador de contratos validados.
- Fin de Fase 3: ✅ Suite 1.0 lista con 573 pruebas automatizadas continuas.

---

## 5. Ecosistema y Herramientas

- **Package manager**: `link.json` con dependencias locales y dependencias Git remotas (`git+https://...#rev`), con lockfile criptográfico `link.lock` (SHA-256).
- **CLI**: `linkc new` (scaffolding Next.js/Vite/Minimal), `linkc dev` (hot reload interactivo con reinicio de servidor), `linkc build`, `linkc serve`, `linkc test` (unitario + snapshots), `linkc lint`, `linkc doc`, `linkc docker`, `linkc fmt`, `linkc lsp`.
- **LSP**: Protocolo JSON-RPC 2.0 completo en stdio con Nivel 1, 2 y 3 (diagnósticos en tiempo real, spans UTF-16, autocompletado sensible al tipo del receptor `x.`, hover de tipos de expresiones y salto a definición multi-archivo). Extensión oficial para VS Code y Cursor (`c-script-vscode-1.0.0.vsix`).
- **Testing**: Runner de tests de comportamiento `test "..." { assert(...) }` con DB aislada por test y verificación de snapshots de contrato con diff LCS (`linkc test <file.link> <file.snap>`).
- **Observabilidad**: Tracing estructurado por RPC con identificador de petición (`req_id`), método, código de estado y duración en milisegundos.
- **Integraciones de Almacenamiento y Auth**: SQLite nativo embebido con auto-migración y PostgreSQL con TLS, auto-reconexión y LISTEN/NOTIFY distribuido. Autenticación declarativa RBAC con sesiones opacas, Argon2id, roles (`auth.currentRole`), id de usuario (`auth.createSessionWithId`/`auth.currentUserId`) y expiración configurable.

---

## 6. Estrategia de Adopción y Comunidad

- **Cuña inicial**: Equipos full-stack TypeScript que buscan el rendimiento y robustez de un backend de sistemas sin sacrificar la inferencia de tipos inmediata en el frontend.
- **"Time to wow" < 5 minutos**: `linkc new my-app` → modificar un campo en `main.link` → `tsc` del frontend falla al instante en desarrollo.
- **Docs y plantillas**: Integración oficial con Next.js 14 App Router, Vite+React y Backend puro.
- **Transparencia y Calidad**: Documentación probada por el propio compilador (`compiler/tests/docs_examples.rs`), 574 tests en CI continuo y límites documentados sin promesas falsas.

---

## 7. Riesgos, Mitigaciones y Costes

| Riesgo | Prob. | Impacto | Mitigación Aplicada / Estado |
|---|---|---|---|
| Adopción nula | Alta | Crítico | Experiencia DX superior: cero IDLs separados, cliente TS + validadores Zod generados automáticamente |
| Type system no mapea 1:1 a TS | Media | Alto | Isomorfismo desde el diseño: suite exhaustiva de tests que compilan y validan el cliente emitido contra `tsc` |
| Ecosistema ausente (DB, auth, net) | Alta | Alto | Baterías incluidas: drivers SQLite y Postgres nativos, auth Argon2id, HTTP cliente/servidor, SMTP y SSE integrados |
| Divergencia checker vs runtime | Media | Alto | Verificación empírica continua: cada feature se prueba contra servidores y bases de datos reales en CI |
| Mantenimiento y complejidad | Media | Alto | Arquitectura modular en Rust (lexer/parser/checker/codegen/runtime) sin dependencias nativas pesadas (p.ej. pure `rustls`) |

---

## 8. Hoja de Ruta Futura (Fase 4 · Hacia c-script 2.0)

Con las Fases 0 a 3 completadas y el núcleo v1.28.0 plenamente operativo, las siguientes prioridades definen la evolución hacia la versión 2.0:

### 8.1 Ecosistema y Distribución
1. ~~**Publicación en el registro npm**~~ -- **PUBLICADO (30/08/2026): `link-lang` en npmjs.com, `npx linkc`/`npm install -g link-lang` reales.** El usuario creó su cuenta de npm y generó un access token (Granular, "All packages"/read-write, 7 días -- alcance amplio deliberadamente TEMPORAL, reemplazado por uno acotado solo a `link-lang` apenas el paquete existió) que Claude usó para publicar. Instalador (`npm/bin/linkc.js`, descarga el binario correcto por plataforma desde GitHub Releases, verificado contra `SHA256SUMS.txt`) ya existía desde v1.16.0 sin quedar nunca publicable -- versión desincronizada, sin `README.md`, sin verificación de integridad. Los tres cerrados antes de publicar.

**Bug real encontrado DESPUÉS de publicar v1.150.2, verificando `npx link-lang@latest` contra el registro real (no solo `node bin/linkc.js` local)**: bajo `npx`, npm agrega temporalmente `node_modules/.bin` (con un shim `.cmd` apuntando de vuelta a este MISMO script) al PATH -- la rama 3 del wrapper ("¿está en el PATH?") encontraba ESE shim y `child_process.spawn` en Windows no puede lanzar un `.cmd` sin pasar por un shell, así que fallaba con ENOENT. El primer intento de arreglo (`shell: true` + array de args) reveló un problema PEOR al probarlo en vivo: sin ninguna guarda, el hijo reentrante volvía a encontrar el MISMO shim y se re-invocaba a sí mismo sin límite -- confirmado que generó cientos de procesos `node.exe` reales en segundos antes de que el timeout cortara el árbol completo (verificado con `Get-CimInstance Win32_Process` que no quedó ninguno vivo). Arreglado de raíz, no con un parche apurado: `cross-spawn` (única dependencia nueva del paquete, resuelve el lanzamento de `.cmd`/`.bat` en Windows de forma correcta y ya probada por el ecosistema, en vez de reinventar a mano el quoting de línea de comandos de Windows -- el mismo `shell: true` que Node marca inseguro, DEP0190) más una guarda por variable de entorno que corta la rama 3 en el hijo reentrante. Verificado en vivo, tres escenarios: shim autorreferencial (antes recursión infinita, ahora cae limpio a la descarga), binario nativo real en PATH (ej. `cargo install`, sigue funcionando directo), y descarga en frío end-to-end contra el registro real. Publicado como v1.150.3 del paquete npm (sin bump del compilador -- el binario/release de v1.150.2 no cambió, solo el wrapper JS).

Ver §9.12 ítem 1 (ya resuelto también).
2. **Ampliación del Playground Web**: Soporte para resolución de múltiples archivos y ejecución simulada de tests en el navegador mediante `wasm32-unknown-unknown`.

### 8.2 Base de Datos y Consultas Avanzadas
1. ~~**Agregación con truncamiento temporal**: Soporte para agrupar por fechas truncadas...~~ -- **hecho, ver `.truncateToDay()`/`.truncateToMonth()`/`.truncateToYear()`, GRAMMAR.md §3.157 (cierra el límite que dejaba abierto §3.65); listado acá por error, nunca se sacó de esta lista al cerrarse.**
2. ~~**Filtrado con pushdown a SQL (`db.<c>.filter(predicate)`)**...~~ -- **hecho, vía `findWhere`/`countWhere`/`deleteWhere` (no un método `.filter` separado -- mismo predicado por forma que `sumBy`/etc.): `==`/`!=`/relacionales, `&&`/`||` en cualquier anidamiento, y campo-vs-campo, todos empujados a SQL real, ver GRAMMAR.md §3.95/§3.108/§3.109/§3.170/§3.171 (§9.3 arriba). Listado acá por error, nunca se sacó de esta lista al cerrarse.**
3. **Transacciones sobre múltiples escrituras `db.<c>`**: Hoy cada escritura es su propio commit implícito; falta una forma de agrupar varias escrituras relacionadas en una sola transacción con rollback ante error. **Hecho (26/08/2026): `transaction { ... }`, ver §9.3.**

### 8.3 Runtime y Comunicaciones
1. **Limpieza proactiva de sesiones**: Implementar recolección periódica en segundo plano para sesiones expiradas bajo `--session-ttl`.
2. **Rate limiting distribuido**: adaptadores para estado compartido (e.g. Redis) en despliegues con réplicas -- la mitad "proxy de confianza" (`X-Forwarded-For`) de este ítem se resolvió como `--trust-proxy`, ver §9.5 Hecho y GRAMMAR.md §3.89.
3. **Envío asíncrono no bloqueante para `smtp`**: `send`/`sendToMany`/`sendHtml` (GRAMMAR.md §3.63) siguen siendo sincrónicos -- un relay lento hace lento al servidor entero (de un solo hilo) mientras dura esa request. Múltiples destinatarios y cuerpo HTML ya están resueltos.

### 8.4 Autenticación y Seguridad
1. **Carga opcional del `User` completo en sesión**: `auth.currentRole()`/`currentUserId()` exponen rol e id, pero cargar el struct completo sigue requiriendo `db.users.find(uid)` explícito en cada rpc que lo necesite.

### 8.5 Almacenamiento
1. **Módulo `storage`/S3`**: URLs firmadas de DESCARGA (`crypto.awsS3PresignedUrl`, GET, GRAMMAR.md §3.110) y de SUBIDA (`crypto.awsS3PresignedUploadUrl`, PUT con `Content-Type` firmado, GRAMMAR.md §3.194, 30/08/2026) ya resueltas -- las dos verificadas contra el vector de prueba oficial `aws4_testsuite` de AWS. Sigue abierto, sin evidencia real de demanda todavía: ninguna forma de limitar el TAMAÑO del archivo subido (necesitaría una POST Policy de S3, mecanismo distinto de una URL presignada) y upload multipart directo.

**Origen de 8.4–8.5** (23/08/2026): 15 gaps nuevos, verificados contra el código real (no contra la documentación), a partir de dos fuentes externas -- un reporte de adopción real (app financiera "MyFinance" sobre una base Postgres ya existente) y una auditoría propia de los "límites honestos" que cada sección `§3.X` de GRAMMAR.md ya se admite a sí misma. Quedaron fuera de esta ronda por ser más especializados o de menor demanda general (identificados, no descartados): WebSocket bidireccional, jobs en background/cron, caché a nivel de app, búsqueda full-text, subida de archivos multipart, retry/backoff para `smtp`/`http` salientes, export OpenTelemetry, GraphQL, i18n, y migraciones más allá de agregar columna (rename/retype/drop). Conversión `.toString()`, `response.setStatus` en `stream` y el catch-all de `@route` (originalmente 8.6/8.7.1) se implementaron el 23/08/2026 -- ver v1.19.0 y GRAMMAR.md §3.55–§3.57. Argon2id configurable, señal de hash legado (originalmente 8.4.1/8.4.2) y aceptar PK autoincremental de 32/16 bits (originalmente 8.5.1) se implementaron el mismo día -- ver v1.20.0 y GRAMMAR.md §3.58–§3.59. `@route` con query string (originalmente 8.6.1) se implementó el mismo día -- ver v1.23.0 y GRAMMAR.md §3.62. **Cobertura de `escapeHtml()` (originalmente 8.6.2, hoy 8.7): auditado el 23/08/2026 y descartado como gap real** -- el método ya escapaba `'` además de `"` desde su introducción (v1.9.0), así que texto/atributo con CUALQUIER estilo de comillas ya estaba cubierto; lo único que quedaba (interpolar dentro de `<script>`/`<style>`, o un atributo sin comillas) son contextos que NINGÚN escapador de HTML resuelve por diseño -- necesitan escape de JS/CSS o directamente no escribir atributos sin comillas, no "más cobertura" de este método. Se corrigió únicamente la redacción de GRAMMAR.md §3.45, que decía "solo comillas dobles" por error. Auth externo/JWT (originalmente 8.4.2) se implementó el 24/08/2026 -- ver v1.25.0 y GRAMMAR.md §3.64. `linkc introspect` (originalmente 8.5.2) se implementó el mismo día -- ver v1.27.0 y GRAMMAR.md §3.66. Modo "adoptar tabla existente sin auto-migrar" (originalmente 8.5.1, renumerado de §8.6 a §8.5 tras esto) se implementó el mismo día -- ver v1.28.0 y GRAMMAR.md §3.67.

---

## 9. Hoja de Ruta Extendida (Fase 5 · Gaps de Adopción Real a Escala)

**Origen** (24/08/2026): dos reportes de adopción real, compartidos por el usuario -- `c-script-wishlist.md` (164 ítems, app financiera "MyFinance" sobre Postgres ya existente) y `peticiones-c-script-linkc.md` (108 ítems, migración real de 17 microservicios dentro de "IgnisLove"). 272 ítems en total, verificados uno por uno contra el código real (GRAMMAR.md §3.1–§3.67 y `README.md`) antes de sumarlos acá -- varios ya estaban resueltos y se descartaron (ver la lista al final de esta sección). El resto se depuró (mucha superposición entre los dos reportes) y se ordenó siguiendo el pedido explícito: **primero todo lo que no bloquea nada externo, al final lo ya trackeado en §8 y lo genuinamente bloqueado**. Dentro de cada subsección, el orden es aproximadamente el de urgencia que los propios reportes señalan.

### 9.1 Documentación (bajo costo, alto valor -- hacer esto primero, sin importar el tamaño del resto)

**§9.1 completo** (24/08/2026). **Matriz de comportamiento de auto-migrate** (originalmente 9.1.1) se implementó junto con un bug real que la auditoría encontró -- ver v1.29.0, GRAMMAR.md §3.17 (matriz completa SQLite+Postgres) y §3.68 (NULL en columna requerida tras migración de Postgres ya no se serializa en silencio, ahora es un error de runtime limpio). **Comportamiento ante colisión de colección** (originalmente 9.1.2) verificado contra un PostgreSQL real -- ver v1.30.0, GRAMMAR.md §3.36. **`CREATE EXTENSION "pgcrypto"`** (originalmente 9.1.3): auditado y sacado por completo del DDL generado en vez de solo documentado -- no se usaba para nada, verificado aplicando el schema con un rol Postgres real sin privilegios de superusuario -- ver v1.31.0, GRAMMAR.md §3.36. **`link.lock`** (originalmente 9.1.4) y **comportamiento de `SIGTERM`** (originalmente 9.1.9): ya estaba mayormente documentado (`link.lock`, GRAMMAR.md §2.1) o ya se comportaba bien sin necesitar cambios (`SIGTERM`: sin manejador de señales, terminación inmediata del SO, pero ninguna escritura ya confirmada puede perderse porque cada una es autocommit) -- se agregó el gotcha real que faltaba (`.link` compilado fuera de su carpeta real) y la confirmación explícita de `SIGTERM` -- ver v1.32.0, GRAMMAR.md §2.1 y §3.17. Las 4 guías restantes (SQLite vs PostgreSQL, despliegue multi-servicio, adopción incremental, integrar un servicio ya generado desde afuera) se escribieron como `docs/*.md` nuevos, cross-referenciados entre sí y con GRAMMAR.md, con sus propios hallazgos verificados contra un servidor real (forma exacta de `/health`, de un error 401/403, de `{"error": "..."}`) -- ver v1.33.0.

### 9.2 Núcleo del Lenguaje
1. ~~**Tipo `Decimal`/`Money` de precisión exacta**~~ -- **RESUELTO (29/08/2026, v1.140.0-v1.140.2).** `Float` era una fuente de error de redondeo conocida y confirmada por dos adoptantes financieros distintos; `Decimal` (punto fijo, `i128` interno ×10.000, 4 decimales) lo cierra -- construcción vía `.toDecimal()` (sin sintaxis de literal nueva, mismo patrón que `Int64`), aritmética checked con redondeo half-up, `NUMERIC(38,4)` nativo en Postgres (incluida lectura/escritura contra una columna `numeric` YA EXISTENTE, el caso real de MyFinance) y `INTEGER` escalado en SQLite. `sumBy`/`maxBy`/`minBy`/`maxRow`/`minRow` soportados; `avgBy` excluido a propósito (asimetría de almacenamiento entre backends, documentada). Ver GRAMMAR.md §3.184.
2. **Roadmap de concurrencia, Pilares 2 y 3** (propuesto por skynet-d3 a nombre de Carlos, 26/08/2026 -- Pilar 1, un hilo por request, ya HECHO, ver abajo): ~~**Pilar 2, FFI seguro y tipado hacia crates de Rust**~~ -- **DISCOVERY HECHO, RESUELTO PARCIAL (29-30/08/2026).** El discovery (2 forks, código real antes de proponer nada) encontró que el pedido ORIGINAL -- exponer `crates.io` entero -- no es viable con la arquitectura actual (`Value`/`Type` son enums cerrados matcheados exhaustivamente en checker/runtime/codegen; nada de `libloading`/WASM-component) sin construir antes un sistema de macros/codegen completo, y choca de frente con la política de "cero dependencias nuevas" ya sostenida. Presentado esto al usuario, eligió explícitamente NO atacar FFI arbitrario, sino un fast-path para seguir curando builtins a mano, mucho más rápido de agregar: el macro `builtin_args!` (`checker.rs`) colapsa la forma repetida de un arm de tipo curado (destructurar N args exactos + un `check_expr` por posición + devolver un `Type`) a una línea, para builtins NUEVOS de acá en adelante -- el lado runtime (la lógica real) sigue escrito a mano, a propósito. Ver GRAMMAR.md §3.186 (tooling interno del compilador, no una feature del lenguaje -- ningún `.link` cambia). ~~**Pilar 3, sistema de módulos/paquetes**~~ -- **DISCOVERY HECHO (27/08/2026), y corrigió esta misma entrada: estaba desactualizada.** El discovery (auditar el código real antes de diseñar nada) mostró que el sistema de módulos YA existía y era mucho más completo de lo que este plan reflejaba -- imports multi-archivo por ruta relativa, `link.json` con dependencias nombradas, dependencias `git+<url>#<rev>` reales con checkout cacheado, `link.lock` con detección de deriva, ciclos, caso diamante, colisiones entre archivos (todo GRAMMAR.md §2.1, construido en rondas anteriores de esta sesión). Las dos preguntas que esta entrada daba por abiertas ya tenían respuesta en el código: "import por archivo vs. paquete versionado" -- las dos formas existen; "cómo conviven dos `db {}`" -- no conviven, es un error duro. El ÚNICO hueco real encontrado (corriendo el binario, no leyéndolo) era que un módulo no podía aportar un `service` sin declarar un tipo-fantasma que se filtraba al contrato público generado -- cerrado con la forma de import "solo por efecto" (`import "./billing.link";`), ver GRAMMAR.md §3.161. **Lo que quedaba genuinamente abierto del Pilar 3**: (a) permitir varios `db {}` (uno por módulo) -- decisión de diseño con peso propio (¿se fusionan las colecciones? ¿qué pasa con dos del mismo nombre?), más el error en cascada confuso que hoy acompaña al error correcto; (b) visibilidad `pub`/privado, ya documentada como fuera de alcance en §2.1. Propuesta completa (arquitectura del Pilar 1 verificada contra el código real antes de escribir una línea, los dos pilares restantes marcados explícitamente como pendientes de su propio discovery) compartida con el usuario como artifact -- ver el enlace en el historial de la sesión del 26/08/2026 si hace falta releerla completa. **Hecho (27/08/2026): (a) resuelto -- cualquier cantidad de `db { ... }` se fusiona en un solo namespace de colecciones, cada módulo dueño de las suyas; el único error duro que queda es un nombre de colección repetido (mismo criterio que `type`/`enum`/`fn`/`const`), y de paso se cerró el error en cascada citado arriba y un gap preexistente (un nombre repetido DENTRO de un solo bloque se perdía en silencio antes). Ver GRAMMAR.md §3.172.** Sigue abierto (b), sin evidencia de demanda propia todavía. Con esto el Pilar 3 queda cerrado salvo (b); el Pilar 2 (FFI) sigue sin su propio discovery.

**Hecho** (24/08/2026): **narrowing/desreferencia de `T?`** (originalmente 9.2.1, el gap más repetido y con más fricción de los dos reportes) se resolvió de punta a punta, no solo el mínimo viable -- `match x { v: T => ..., null => ... }` (narrowing real, no solo `??`/`isSome`/`isNone`), reusando el mismo mecanismo de patrones que ya narrowaba uniones (§3.9). De paso: `a ?? b` (con encadenado real, `a ?? b ?? default`), `.isSome()`/`.isNone()` (con un caso adversarial de shadowing por un campo real resuelto), completion del LSP para `T?`, y el **mensaje de error** (originalmente 9.2.2) actualizado para señalar las alternativas reales en vez de solo "no se puede". Ver v1.34.0, GRAMMAR.md §3.69. **Tipo `Uuid` nativo** (originalmente 9.2.3): forma canónica validada en los tres bordes (runtime, `validators.ts`, `schemas.ts`/Zod, con la misma regex en los tres), tipo aparte de `String` sin mezcla implícita (mismo criterio que `Int64` vs `Int`), `crypto.uuid()` ahora devuelve `Uuid`. Ver v1.35.0, GRAMMAR.md §3.70. **`@deprecated("usa X en su lugar")`** (originalmente 9.2.2 en esta lista, renumerado): en un campo de struct o en un rpc/stream -- sobre un campo es la ÚNICA anotación que un campo admite hoy (`Field` no tiene el `Vec<Annotation>` genérico de `RpcDecl`), puramente informativo (no afecta subtipificación estructural ni runtime), propagado como JSDoc `@deprecated` en `contract.d.ts` y como `deprecated: true` + `description` nativo de OpenAPI/JSON Schema en `openapi.json`. Ver v1.36.0, GRAMMAR.md §3.71. **Docstrings `///` propagados a OpenAPI**: nueva infraestructura de lexer (`Token::leading_doc`, exactamente 3 slashes -- ni `//` ni `////`) que no rompe ningún programa existente (un `///` sigue siendo trivia válida en cualquier posición, la captura es puramente aditiva). Se propaga como `description` del Operation Object en `openapi.json` y como bloque JSDoc multilínea en `contract.d.ts`; si el mismo rpc también lleva `@deprecated`, las dos cosas se combinan en un solo campo/bloque en vez de pisarse. Alcance de esta ronda: solo `rpc`/`stream`, no `type`/campo de struct. Ver v1.37.0, GRAMMAR.md §3.72. **Validadores declarativos por campo** (originalmente el ítem 2 de esta lista, renumerado): `@validate(email)` / `@validate(regex, "...")` sobre un campo `String`/`String?` -- enforcement real en CUATRO lugares (servidor real vía `linkc serve`, `openapi.json` con `format`/`pattern` estándar de JSON Schema, `schemas.ts`/Zod con `.email()`/`.regex(new RegExp(...))`, y JSDoc informativo en `contract.d.ts`), no solo documentación. Única excepción de la sesión a "cero dependencias nuevas": trajo la crate `regex` (pura Rust, sin dependencias C) -- un patrón de usuario es texto arbitrario, a diferencia de las formas FIJAS (UUID/SHA-256/ISO-8601) que sí se hand-rollean en el resto del proyecto. Alcance de esta ronda: `validators.ts` (las funciones `isX()` hand-escritas) todavía no lo enforce, trabajan sobre el tipo estructural sin anotaciones. Ver v1.38.0, GRAMMAR.md §3.73. **Valores por defecto en campos de `struct`** (originalmente el ítem 2 de esta lista): `nombre: Tipo = expr`, mismo mecanismo que `Param::default` -- un campo con default se puede omitir de un literal sin volverse `Optional`; se completa en `Expr::StructLit`, evaluado DE NUEVO en cada construcción (`crypto.uuid()` como default da un valor distinto cada vez, verificado). Propagado a `contract.d.ts`/`schemas.ts` como campo opcional y a `openapi.json` (fuera de `required`, más `"default"` cuando es un literal simple). Sin soporte en un `type` genérico ni con acceso a otros campos del mismo literal -- alcance de esta ronda. Ver v1.39.0, GRAMMAR.md §3.74.

**Hecho** (24/08/2026, segunda ronda): **`dateFromParts(year, month, day, hour, minute, second) -> Timestamp`** -- gap NUEVO, encontrado por un segundo reporte de MyFinance (`myf errores.md`, backend de cálculo de Modelos tributarios 130/303/347) que no había quedado capturado en el wishlist original de 164 ítems: §3.31 documentaba a propósito que un `Timestamp` v0 solo podía llegar de un parámetro de rpc o de la base, nunca construirse arbitrariamente -- `now()` (§3.32) resolvía el instante ACTUAL, pero no una fecha arbitraria (el límite de un trimestre, por ejemplo), que es exactamente lo que Modelo 130/303 necesitaba calcular. Builtin sin receptor, mismo mecanismo que `now()`; una fecha inválida (mes 13, 30 de febrero) es `bad_request` (400), nombrando el campo mal formado. Ver v1.55.0, GRAMMAR.md §3.90.

**Hecho** (24/08/2026, tercera ronda): **`Float` decodifica `numeric`/`decimal` nativo de Postgres** -- gap NUEVO, tercer reporte de MyFinance verificando en su propio esquema real EL FIX de fechas (§3.91): "`Float` no decodifica columnas `numeric` de Postgres -- error deserializing column 1... todos los importes monetarios del schema real son `numeric`, nunca `float`". `numeric` es un formato binario de precisión arbitraria que `postgres-types` no sabe leer como `f64` -- decodificado a mano (`PgNumeric`, mismo espíritu que `PgTimestampMicros`/`PgDateDays` de §3.91, sin sumar `rust_decimal`). Alcance: solo LECTURA, igual que el fix de fechas. Ver v1.67.0, GRAMMAR.md §3.103. **Escritura de `Int` contra una columna Postgres no-`BIGINT` (`SERIAL`/`SMALLINT`)** -- encontrado auditando por qué CI llevaba varios pushes en rojo (`gh run list`, nunca chequeado hasta ahora): el test que §3.59 daba por "verificado" en realidad nunca había corrido contra un Postgres real -- corriéndolo apareció que `i64::to_sql` (crate `postgres-types`) ignora el ancho real de columna y siempre manda 8 bytes, corrompiendo el protocolo binario contra `WHERE "id" = $1` sobre una columna `int4`/`int2`. `Cell::to_sql` ahora despacha por el `ty` real que pide el servidor. De paso se corrigieron 4 tests de `pg_integration.rs` que también llevaban tiempo rotos por bugs propios de los tests (naming, `postgres::Error::Display` mal asumido, un guard correcto disparando por accidente, y `linkc introspect` recogiendo tablas de otro test corriendo en paralelo) -- ninguno de los cuatro era un bug del compilador. Ver v1.67.0, GRAMMAR.md §3.104 (incluye la nota de proceso completa sobre por qué esto pasó desapercibido tanto tiempo).

**Hecho** (26/08/2026, cuarta ronda -- bug fix, no feature nueva): **`while` real dentro de `test { }` fallaba siempre en su primera vuelta.** `run_tests_core` inicializaba el contador compartido de iteraciones (`MAX_WHILE_ITERATIONS`, §3.15) en el propio tope en vez de en cero -- cualquier `while` que corriera al menos una vez dentro de un test disparaba de inmediato "límite de 1000000 iteraciones excedido", el mismo ejemplo canónico de §3.15 funcionando bien vía `serve` y roto vía `linkc test`. Encontrado verificando (no asumiendo) un reporte externo de un `while` "colgado" solo bajo el test runner. Fix de una línea, con test de regresión que cubre tanto el caso corto (debe pasar) como un `while true` genuino (debe seguir cortando). Ver v1.110.0, GRAMMAR.md §3.15.

**Hecho** (26/08/2026, octava ronda): **Roadmap de concurrencia, Pilar 1 -- `linkc serve` pasa de un solo hilo a un hilo real por request.** Con autorización explícita del usuario ("arrancamos") sobre la propuesta compartida como artifact (ver ítem 2 arriba), se evaluaron dos caminos -- reescritura completa a `tokio`/`async` (descartado: "function coloring" propagando `async fn` por todo el árbol de evaluación del intérprete) contra hilo-por-request con candado reentrante sobre la conexión (elegido: riesgo mucho más acotado, sin tocar la forma de `eval_expr`/`eval_block`). `Db`/`Checker`/`SessionStore` convertidos campo por campo al primitivo correcto según su patrón de uso real: la conexión SQL a `parking_lot::ReentrantMutex` (reentrante a propósito -- `transaction{}` necesita tener el candado tomado durante BEGIN+cuerpo+COMMIT/ROLLBACK, y el cuerpo vuelve a pedir el mismo candado por cada operación individual; un `Mutex` no reentrante se autobloquearía), estado genuinamente por-request (`current_request`, overrides de status/location de `response`) a `thread_local!` en vez de forzarlo a un `Mutex` compartido, el resto (`subscribers`, contadores, sesiones) a `parking_lot::Mutex`/`RwLock` según si se escribe una vez o muchas. `Checker::in_stream_body`/`in_transaction`/`hover_result` (embebidos en `Db` para resolución de tipos en runtime) se convirtieron con primitivos de `std::sync`, no `parking_lot`, a propósito -- `checker.rs` no está detrás del feature `runtime` y debe seguir compilando a `wasm32-unknown-unknown`. Verificado con dos tests permanentes de concurrencia real (`std::thread::spawn`, no simulada) que corren estables 5 veces seguidas -- 40 inserts concurrentes nunca pierden ni duplican una fila, 40 `transaction{}` concurrentes sobre la misma fila nunca pierden un update -- más verificación manual contra `linkc serve` real (SQLite y Postgres): la ganancia de paralelismo real medida (5 llamadas HTTP lentas de 2s cada una -- 11.3s en secuencial, 2.3s concurrentes) confirma la queja original de skynet-d3 que motivó el roadmap. De paso, verificando que la superficie wasm32 seguía intacta, se encontró y arregló un bug PREEXISTENTE (`pub mod migrate;` sin `#[cfg(feature = "runtime")]` en `lib.rs`, nunca antes ejercitado) y se documentó uno adicional preexistente sin arreglar (`codegen/postgres_emit.rs` importa incondicionalmente de `runtime::db`, sin cobertura de CI en wasm32 -- confirmado, target aspiracional). Pilares 2 y 3 quedan explícitamente pendientes, cada uno con su propio discovery. Ver GRAMMAR.md §3.158.

**Hecho** (26/08/2026, novena ronda -- 3 bugs reales de concurrencia, encontrados auditando GRAMMAR.md el mismo día que la octava ronda, ninguno reportado externamente): releer las secciones que documentaban invariantes de "el servidor procesa una request a la vez" con la premisa nueva (un hilo real por request) hizo aparecer 3 lugares donde esa premisa vieja era la única razón por la que el código era correcto:
1. **`Db::subscribe` podía perder en silencio una fila insertada concurrentemente** -- sacaba la foto y RECIÉN DESPUÉS se registraba como suscriptor, sin candado compartido con `publish`/`deliver_local`. Fix: registrar y sacar la foto bajo el mismo candado. Ver GRAMMAR.md §3.16.
2. **Ese mismo fix casi introduce un deadlock** -- si `commit_transaction` entregara sus eventos diferidos con el candado de la conexión todavía tomado, un `transaction{}` confirmando y un `subscribe()` concurrente a la misma colección pedirían dos candados en órdenes opuestos. Fix: la entrega pasa a después de soltar el candado de la conexión. Ver GRAMMAR.md §3.154/§3.158.
3. **`upsert` podía duplicar una fila bajo el mismo `matchFn` concurrente** -- buscar la fila existente y decidir insert-o-patch eran dos pasos separados sin candado compartido. Fix: `upsert` entero corre bajo `Db::with_exclusive_connection`. Ver GRAMMAR.md §3.75.

Los 3 confirmados reproduciendo el fallo original a mano (revirtiendo cada fix antes de restaurarlo) -- el del punto 2 literalmente cuelga el test (deadlock real) con el orden viejo. Ver v1.115.0.

**Hecho** (26/08/2026, décima ronda -- segunda auditoría adversarial, agente `Explore` read-only, 3 bugs reales más, dos de ellos introducidos por los fixes de la novena ronda): un deadlock ABBA distinto (`upsert` bajo `with_exclusive_connection` desde v1.115.0 entregando eventos con la conexión todavía tomada, orden inverso al que `subscribe()` necesita), `@cron` roto en el TypeScript generado (uno de seis emisores sin el filtro, TS2420 confirmado con el `tsc` real del repo), y división/resto entero por cero como PANIC de Rust en vez de `RuntimeError` (con pérdida silenciosa de datos ya confirmados al cliente, reproducida de punta a punta). Los 3 dejaron explícitamente abierto un límite común: el fix de división por cero tapaba el disparador más alcanzable, pero ningún `catch_unwind` protegía todavía un panic GENÉRICO dentro de `transaction { }` ni dentro de `@cron`. Ver GRAMMAR.md §3.162.

**Hecho** (27/08/2026, undécima ronda -- cierra los dos límites que la décima ronda había dejado abiertos): **`catch_unwind` alrededor del cuerpo de `transaction { }`** y **alrededor de cada corrida de `@cron`.** Un panic real (no un `RuntimeError`) atrapado en cualquiera de los dos casos se traduce a una falla limpia -- `rollback_transaction()` para la transacción (mismo camino que cualquier otro error del cuerpo), un tick contado como falla en `/metrics` para `@cron` (el `loop` del scheduler sigue vivo y sigue reintentando en el próximo intervalo). El disparador de verificación en ambos tests es un desborde real de `+` sobre `i64` -- código de producción sin arreglar a propósito, distinto del caso de división por cero ya cerrado, para probar el `catch_unwind` contra un panic genérico y no repetir el mismo caso puntual. Gateados con `#[cfg(debug_assertions)]`: el desborde solo panica con `overflow-checks` activo (perfil `dev`, lo que corre `cargo test`/CI); en `release` simplemente wrappea, confirmado corriendo los mismos tests con `cargo test --release`. Verificado contra el binario real en el caso de `@cron` (`linkc_cron_failures_total` creciendo con el tiempo en un `linkc serve` real, no solo en el runtime aislado). Ver GRAMMAR.md §3.163/§3.164.

**Hecho** (27/08/2026, decimotercera ronda -- Ronda 2 de `AUDIT-FIX-PLAN-2026-08-27.md`, severidad alta): **`Patch<T>`/`applyPatch` nunca corría `@validate`/`@check`** -- `json_to_typed_value` tiene dos caminos que construyen un struct desde el wire, y solo uno (`Type::Struct`) llamaba a `apply_field_validators`; `Type::PatchOf` (la forma canónica de actualización parcial) se lo saltaba entero. Confirmado en vivo: `create` con un email inválido daba 400, `update`/`applyPatch` con el MISMO valor inválido daba 200 y lo persistía. Fix: `Type::PatchOf` ahora también llama a `apply_field_validators` -- la función ya toleraba un valor parcial (un patch nunca trae todos los campos), así que no hizo falta ningún cambio ahí. Ver GRAMMAR.md §3.166.

**Hecho** (27/08/2026, decimotercera ronda -- Ronda 2 de `AUDIT-FIX-PLAN-2026-08-27.md`, severidad alta): **`@idempotent` tenía una carrera TOCTOU real** -- `lookup`+`store` eran dos candados separados con el cuerpo del rpc corriendo sin ninguno sostenido entre medio, así que dos requests concurrentes con la misma `Idempotency-Key` corrían el cuerpo las DOS (confirmado en vivo: 30 requests concurrentes insertaron 2 filas para un solo cargo). Fix: `reserve` (revisar + marcar en vuelo, atómico bajo un único candado) reemplaza a `lookup` -- un segundo `reserve` sobre una clave en vuelo da `409` sin correr el cuerpo, mismo criterio que la API real de Stripe. Verificado con hilos de sistema operativo REALES (30 concurrentes contra un `linkc serve` real, `server_http.rs`) confirmando 1 sola fila. Ver GRAMMAR.md §3.167.

**Hecho** (27/08/2026, decimocuarta ronda -- Ronda 3 de `AUDIT-FIX-PLAN-2026-08-27.md`, severidad media, 6 hallazgos en un solo paquete): `insert()` panicaba (no `RuntimeError`) en la misma carrera de re-consulta-tras-escritura que `applyPatch` ya manejaba limpio -- asimetría cerrada. Agregaciones (`sumBy`/etc.) panicaban sobre una columna `NULL` heredada de agregar un campo requerido a una colección Postgres con filas viejas (la migración nunca agrega `NOT NULL`) -- mismo `RuntimeError` que `row_to_fields` ya usaba para el caso análogo de lectura normal. El checker aceptaba un rpc `@cron` como blanco de `@invalidates` (invalidación de caché muerta en silencio, `hooks.ts` generado con una llamada que ningún hook escribe jamás) -- excluido explícitamente. `linkc doc` no mostraba badges de auth/rate-limit/deprecated en un `stream` (documentación que desinformaba sobre qué está protegido) -- unificado en una función compartida con el brazo `rpc`. `GET /metrics` sostenía el candado de métricas mientras esperaba la conexión a la base (latencia/contención bajo tráfico real) -- reordenado para calcular antes de tomar el candado. `lint`: `mixed-service-auth` daba falso positivo con un `@cron` al lado de rpcs protegidos -- excluido del cálculo. Los 6 verificados con test unitario propio; 3 de ellos (`@invalidates`+`@cron`, badges de `doc`, falso positivo del lint) repetidos en vivo contra el binario real. Ver GRAMMAR.md §3.168.

**Hecho** (27/08/2026, decimoquinta ronda -- Ronda 4 de `AUDIT-FIX-PLAN-2026-08-27.md`, última del plan): 3 hallazgos cerrados con código, 3 evaluados y documentados a propósito sin cambio de código. `--jwt-secret ""`/`--service-api-key ""` (string vacío por flag) activaba la feature con secreto vacío -- filtro puntual, sin tocar `read_flag_or_env` (otros flags como `--host` tienen el contrato inverso deliberado). Tres panics de tipo-incompatible en `row_to_fields` decodificando filas de una tabla `--adopt-existing` con datos legado (JSON que ya no calza con el tipo actual, columna JSON con `Cell` inesperada, tipo nativo declarado que no coincide con lo físico) -- los tres convertidos a `RuntimeError` limpio. `+`/`-`/`*` sobre `Int`/`Int64` (y el `-` unario, y `List<Int>.sum()`) seguían con aritmética cruda sin `checked_*` -- generalizada la función que ya cubría `/`/`%` desde §3.162; ya no queda ningún operador aritmético entero sin guarda; efecto colateral honesto: dos tests de rondas anteriores que usaban desborde de `+` como disparador de panic se actualizaron, porque ese disparador específico ya no panica. Los otros 3 hallazgos (`@cache` con la misma carrera que `@idempotent`, `@unique`/`@softDelete` sin índices parciales, la composición check-then-act del lockout de login) se evaluaron y quedaron documentados como límites honestos en vez de atacados con apuro -- cada uno con su razonamiento explícito. Con esto, `AUDIT-FIX-PLAN-2026-08-27.md` queda completo: los 16 hallazgos de la auditoría, todos resueltos o documentados, ninguno silenciado. Ver GRAMMAR.md §3.169.

**Hecho** (27/08/2026, decimoctava ronda -- cierra el último hueco genuinamente abierto del Pilar 3 del roadmap de skynet-d3, ver ítem 2 arriba): **varios `db { ... }`, uno por módulo, se fusionan en un solo namespace de colecciones.** Antes de esta ronda, un SEGUNDO `db { ... }` en el cierre transitivo de imports era un error duro sin importar sus nombres -- el único patrón que funcionaba era un `schema.link` central con el `db {}` que los módulos de servicio importaban. Discovery primero (mismo criterio que §3.161): auditando quién consume un `Item::Db` apareció que, salvo el loop de `Checker::new` que construye `checker.db_collections` (el `HashMap<String, Type>` ya evaluado), TODO lo demás (`postgres_emit.rs`, `migrate.rs`, `runtime/db.rs`, `runtime/mod.rs`) ya consume exclusivamente ese mapa fusionado -- el blast radius real quedó contenido en un solo lugar. Regla nueva: cualquier cantidad de `db {}` se fusiona; el único error duro que queda es un nombre de colección repetido (mismo criterio que `type`/`enum`/`fn`/`const` duplicados entre archivos), sin importar si las dos apariciones caen en el mismo bloque o en dos distintos -- de paso cierra un gap preexistente (un nombre repetido DENTRO de un solo bloque se perdía en silencio antes) y el gotcha de UX de la cascada de errores que §3.161 había documentado. Verificado con 3 tests en `checker.rs` + 1 en `modules.rs` (dos módulos reales, cada uno con su `db {}` y su `service`, cargados por import de efecto), más repetición en vivo contra el binario real: `linkc build`/`linkc serve` con dos módulos reales generan el contrato y las dos tablas correctamente, cada `service` opera sobre la suya, y el caso de colisión da un solo error limpio sin cascada. Sigue abierto del Pilar 3: visibilidad `pub`/privado, sin evidencia de demanda propia. Ver GRAMMAR.md §3.172.

**§9.2 completo** salvo el Pilar 2 del roadmap de concurrencia (FFI hacia crates de Rust -- discovery hecho, resuelto parcial como `builtin_args!`, ver ítem 2 arriba) y la visibilidad `pub`/privado del Pilar 3 (sin evidencia de demanda propia). `Decimal`/`Money` (ítem 1) ya está resuelto -- nota desactualizada, corregida acá.

### 9.3 Base de Datos y Consultas
1. **`findWhere`/`deleteWhere`/`db.<c>.filter(predicate)` empujados a SQL para CUALQUIER predicado, no solo una conjunción `&&`**: `countWhere`/`findWhere` ya empujan a SQL de verdad tanto el caso de un solo operador (`==` v1.59.0, `!=`/`<`/`<=`/`>`/`>=` v1.71.0) como una conjunción `&&` de varias hojas simples (v1.72.0, ver Hecho -- los DOS casos reales de CRM que quedaban sin resolver, `notifications.link` e `inventory.link`, ya empujan). `deleteWhere` ya empuja la SELECCIÓN también (25/08/2026, ver Hecho -- el borrado en sí sigue fila por fila a propósito, para no perder la publicación a `stream` por fila). **Hecho (27/08/2026): `||` combinando condiciones, en cualquier profundidad de anidamiento con `&&`, respetando la precedencia real del lenguaje -- ver §9.3 Hecho.** **Hecho (27/08/2026): comparar dos campos del propio parámetro entre sí (`item.endDate > item.startDate`) para los cuatro operadores relacionales -- ver §9.3 Hecho, GRAMMAR.md §3.171.** Con esto se cierra por completo este ítem. Único límite honesto documentado: `==`/`!=` entre dos campos queda deliberadamente sin pushear (riesgo de NULL-seguridad sin caso real que lo justifique, ver §3.171).
2. **Índices/constraints ÚNICOS COMPUESTOS (de varios campos) declarativos**: `@index`/`@unique` de un solo campo ya está resuelto (ver Hecho, v1.45.0) -- la forma equivalente sobre VARIOS campos a la vez (`@unique(["email", "tenantId"])`, por ejemplo) sigue pendiente, necesitaría una anotación a nivel de `type`, no de campo (`TypeDecl` no tiene `annotations` hoy). **Ampliado (24/08/2026) por un caso real en Glowapp** (no usa c-script, pero su schema Drizzle es evidencia de demanda): `appointments` usa un índice único COMPUESTO Y CONDICIONAL a la vez -- `UNIQUE(userId, appointmentDate, startTime) WHERE status != 'cancelled'` -- permite reusar un horario una vez cancelado, sin acumular filas basura. Cuando se ataque este ítem, la forma condicional (`WHERE ...`) debería considerarse en el mismo diseño, no como una ronda aparte. **Hecho PARCIAL (26/08/2026): la mitad COMPUESTA, ver §9.3 Hecho.** **Hecho (28/08/2026): la mitad CONDICIONAL -- `@unique(...) where <expr>`, reusando la misma infraestructura de `@check(<expr>)` de tipo (validación de forma, traducción a SQL) que la ronda anterior ya había construido -- ver §9.3 Hecho, GRAMMAR.md §3.174.** Con esto se cierra por completo este ítem, incluido el caso exacto de Glowapp citado arriba.
3. **Constraints `@check` declarativos MÁS ALLÁ de un rango numérico simple**: `@check(min/max/range, ...)` sobre `Int`/`Int64`/`Float` ya está resuelto (ver Hecho, v1.60.0); `@check(minLength/maxLength, N)` sobre `String` también (25/08/2026, ver Hecho). **Hecho (28/08/2026): `@check(<expr>)` a nivel de `type`, una expresión booleana comparando dos campos entre sí (`endDate > startDate`) -- ver §9.3 Hecho, GRAMMAR.md §3.173.** Con esto se cierra por completo este ítem.
4. ~~**`--db-schema <nombre>` o `--db-prefix`**: namespacing para compartir una base Postgres entre varios `.link` sin pensar en colisiones de nombre.~~ -- **RESUELTO (30/08/2026). Con esto se cierra por completo §9.3.** **Auditado (24/08/2026), deliberadamente diferido, no atacado esa ronda**: la nota original temía "decenas de sitios en 3 archivos" que arman SQL con el nombre de colección tal cual. **Re-auditado (30/08/2026, research fork antes de diseñar nada) -- la nota vieja estaba desactualizada y sobreestimaba el alcance real.** El grueso de esos sitios no necesitó NINGÚN cambio de código -- Postgres resuelve un identificador sin calificar contra el `search_path` de la SESIÓN. **Hecho (30/08/2026): el bug de `information_schema` (§3.192) primero, después la feature en sí -- `--db-schema`/`LINK_DATABASE_SCHEMA`.** `SET search_path` UNA vez al conectar (`connect_postgres_client`, el único punto de conexión de todo el proyecto -- cubre `serve`/`db shell`/`inspect`/`export`/`import`/`migrate --dry-run`/`test --db`/el reconnect automático, todos gratis), `CREATE SCHEMA IF NOT EXISTS` solo en el constructor real de `serve` y nunca bajo `--adopt-existing` (mismo criterio que `CREATE TABLE`). `linkc introspect` compone con el `options=` nativo de la URL en vez de un flag propio (su interfaz ya es "URL cruda, sin flags"); `linkc serve-all` lo rechaza de entrada (nunca conecta a Postgres). Verificado con 6 tests de CLI + 3 contra Postgres real, incluido el caso motivador exacto: dos programas con la MISMA colección, cada uno con su propio `--db-schema`, contra la MISMA base, sin colisión. Ver GRAMMAR.md §3.193.
**Hecho** (26/08/2026, séptima ronda -- auditoría multi-agente adversarial, 5 bugs reales encontrados y arreglados, ninguno reportado externamente):** Con el pedido explícito del usuario de correr una auditoría en modo "ultracode", se lanzó un workflow con 6 agentes en paralelo (uno por cada feature shippeada esta sesión, v1.107.0-v1.112.0) más una fase de verificación independiente de cada hallazgo -- 5 de las 6 features auditadas tenían un bug real confirmado; solo el fix de `while`-en-`test{}` (v1.110.0) salió limpio. Todos verificados de nuevo a mano contra el binario real antes de arreglar, y con test de regresión nuevo después:
1. **`upsert`/`findWhere`/`countWhere`/`deleteWhere` pusheados: `"campo" = ?` ligado a NULL rompía la semántica de igualdad con NULL**, insertando filas duplicadas en `upsert` en vez de actualizar -- divergía del camino interpretado, que sí trata `Null == Null` como `true`. Fix: `IS [NOT] NULL` para una hoja cuyo operando resultó NULL. Ver GRAMMAR.md §3.75.
2. **`transaction` anidada alcanzada por una llamada a función auxiliar** compilaba limpio (el checker solo atrapa el anidamiento SINTÁCTICO) y fallaba en runtime con el error crudo del backend. Fix: `Db::begin_transaction` chequea antes de intentar el `BEGIN` real y da el mismo mensaje claro que el caso sintáctico. Ver GRAMMAR.md §3.154.
3. **Nombre de índice de `@unique` compuesto ambiguo** (`fields.join("_")`) -- `@unique(a_b, c)` y `@unique(a, b_c)` sobre el mismo `type` colisionaban de nombre, y `CREATE UNIQUE INDEX IF NOT EXISTS` volvía el segundo un no-op silencioso, sin enforcar de verdad. Fix: codificación con prefijo de longitud, inyectiva por construcción. Ver GRAMMAR.md §3.155.
4. **Revivido de `Union` con un miembro `Int64` nunca revivía ese miembro** -- la disambiguación corría el `isX` (que ya asume post-revivido) contra el valor CRUDO, así que un `Int64 | String` caía siempre al miembro `String` y quedaba como string para siempre, en silencio. Fix: revivir cada candidato PRIMERO (envuelto en `try/catch`, `BigInt(...)` sobre algo no numérico tira de verdad), validar después. Ver GRAMMAR.md §3.156.
5. **Truncado de fecha en SQLite usaba división entera** para un epoch pre-1970 con resto de milisegundos, redondeando hacia el día equivocado -- Postgres ya usaba división real y daba el resultado correcto, así que los dos backends discrepaban por un día entero. Fix: `/1000.0` en vez de `/1000`. Ver GRAMMAR.md §3.157.

**Hecho** (26/08/2026, sexta ronda): **`.truncateToDay()`/`.truncateToMonth()`/`.truncateToYear()`: agregación agrupada por fecha** -- cuarto ítem de la auditoría de Glowapp, cierra el límite que §3.65 dejaba documentado a propósito ("agrupar por Timestamp produciría un grupo por fila, sin truncar"). Método de truncado reconocido SOLO sintácticamente en la posición de selector de CLAVE de `sumBy`/`countBy`/`avgBy`/`maxBy`/`minBy` (`ast::recognize_group_key_selector`, mismo espíritu que `recognize_live_subscribe`) -- la ÚNICA posición de todo el lenguaje donde un método existe sobre `Timestamp`, nunca evaluado de verdad; usarlo en cualquier otra posición sigue siendo el error de siempre. SQL específico por backend, los dos devolviendo milisegundos-desde-epoch planos: SQLite con los modificadores nativos de `strftime` (`'start of day'`/etc.), Postgres con `date_trunc(unit, ts, 'UTC')` -- el overload de TRES argumentos que trunca en una zona horaria explícita, no la variante de dos argumentos (que dependería en silencio del `TimeZone` de la sesión del servidor). Bug real encontrado en la verificación manual: `scalar_cell_to_value` no tenía brazo para `Type::Timestamp` -- nunca hizo falta antes, porque `Timestamp` jamás había sido clave de agrupación válida; sin el brazo, la clave truncada viajaba como número plano en el JSON en vez de string ISO-8601, rompiendo §3.31 en silencio (mismo bug de etiquetado que ya se había encontrado y cerrado para `Int64` en la ronda de §3.65). Verificado con 3 tests de checker, 1 contra SQLite real, 1 contra Postgres real (confirmando la `key` como string ISO-8601), y una verificación manual adicional con fecha PRE-1970 contra los dos motores, resultados idénticos. Ver GRAMMAR.md §3.157.

**Hecho** (26/08/2026, quinta ronda): **`Int64` como `bigint` real en `client.ts`** -- tercer ítem de la auditoría propia de Glowapp. §3.30 (v1.35.0) había dejado el tipo TS de `Int64` en `string` a propósito, documentando que subir a `bigint` real necesitaría "un walker recursivo dirigido por tipo nuevo, arquitectura nueva" -- exactamente lo que se construyó acá al aparecer un caso concreto que lo pedía. El WIRE no cambia (sigue siendo string en las dos direcciones); `validators_emit.rs` suma un segundo juego de funciones (`reviveX`, junto a los `isX` de siempre) que convierte cada `Int64` alcanzable de string a `bigint` DESPUÉS de `res.json()` y ANTES de validar -- cubre struct (con nombre y anónimo), `Optional`/`List`/`Tuple`/`MapOf`/`Union`/`Result`/`Patch`, y expande `Generic`/`enum` de verdad (no los trata como caja opaca, para no dejar un Int64 escondido que el tipo declarado prometiera sin cumplir). Del lado de ida (argumentos salientes), un replacer estructural de `JSON.stringify` alcanza sin ningún walker (`__int64SafeStringify`, cualquier `bigint` se vuelve texto sin ambigüedad posible) -- esa mitad sí era simple. Ambos helpers se emiten condicionalmente (solo si algún rpc realmente usa `Int64`), cero costo generado para el caso común. Verificado con 5 tests unitarios nuevos + los 2 existentes de §3.30 actualizados a la nueva forma, y con el binario real: un programa `.link` con `Int64` compiló y tipó limpio contra un `tsconfig.json` real (`tsc --strict --noUnusedLocals`), y contra un `linkc serve` real -- `i64::MAX` como `bigint` real, ida y vuelta por HTTP (struct, scalar, lista, `findWhere` filtrando por ese campo), exacto en cada punto. `schemas.ts` (Zod, para formularios) y `openapi.json` quedan sin cambios a propósito, alcance distinto. Ver GRAMMAR.md §3.156.

**Hecho** (25/08/2026): **Aviso de colisión de tabla (§3.94): `createdAt`/`updatedAt`/`deletedAt` ya no cuentan como evidencia de relación por sí solos** -- landmine encontrado en un barrido propio de "límites honestos" pendientes en GRAMMAR.md, motivado por el patrón real del incidente de puertos de IgnisLove (§3.153, reportado por otra sesión de Claude trabajando en el VPS de ese adoptador): un límite ya documentado con meses de anticipación terminó rompiéndose recién cuando alguien lo tocó en producción. Auditando §3.94 con el mismo criterio apareció que la propia convención de auditoría que el lenguaje promueve (`createdAt: Timestamp = now()`, `@autoUpdate`, `@softDelete`) hace casi seguro que DOS programas sin ninguna relación entre sí compartan ese nombre de campo -- alcanzaba para suprimir la advertencia de colisión de tabla, exactamente el escenario que existe para atrapar. Ahora esa terna se ignora como evidencia de overlap (con un fallback conservador si el struct declarado no tiene NINGÚN campo fuera de ella). Verificado contra Postgres real: dos `.link` sin relación que solo comparten `createdAt` siguen disparando la advertencia; el caso de borde (struct compuesto solo por campos de auditoría) no regresiona. Ver GRAMMAR.md §3.94 (actualizada, no una sección nueva).

**Hecho** (26/08/2026): **`@unique(campo1, campo2, ...)`: constraint UNIQUE compuesto a nivel de `type`** (originalmente parte del ítem 2 de esta lista, la mitad compuesta) -- segundo ítem del mismo pedido de discovery de migración (Glowapp, vía auditoría propia): "un slug único POR PERFIL, no globalmente" necesita un constraint sobre VARIOS campos a la vez, que `FieldAnnotation` (atado a un campo) no puede expresar. `TypeDecl` gana `annotations: Vec<TypeAnnotation>` (enum propio, mismo criterio que `FieldAnnotation`/`Annotation` de rpc) -- por ahora una sola variante, `Unique(Vec<String>)`, identificadores sueltos como `@invalidates`. El checker exige al menos 2 campos, que cada uno exista de verdad, sin repetidos dentro del mismo `@unique`, sin `@unique` sobre un `type` que no sea struct, y sin dos `@unique` declarando el MISMO conjunto (sin importar el orden). DDL idéntico en los dos backends (`CREATE UNIQUE INDEX ... ON tabla(col1, col2, ...)`), emitido tanto por el runtime real como por `linkc build`/`linkc migrate --dry-run`.

**Bug real encontrado en la verificación manual, preexistente (no introducido por este ítem):** una violación de `@unique`/`@check` contra Postgres real daba 500, no el 400 documentado desde v1.45.0/v1.60.0 -- `postgres::Error::to_string()` para un error del servidor es el literal fijo "db error", así que el chequeo por substring de mensaje nunca matcheaba nada real contra ese backend. Arreglado clasificando por SQLSTATE (`23505`/`23514`, nunca se traduce) en vez de por el mensaje humano (que SÍ está localizado -- confirmado con un Postgres de prueba corriendo en español). Verificado con un test dedicado contra Postgres real confirmando el 400 real por HTTP. Ver GRAMMAR.md §3.155.

**Hecho** (26/08/2026): **`transaction { ... }`: transacciones SQL multi-escritura** (originalmente §8.2.3) -- pedido real de un adoptador en fase de discovery de migración (IgnisLove, vía otra sesión de Claude coordinando en su VPS), confirmado como el ÚNICO bloqueo REAL (no de conveniencia) para migrar un flujo de checkout/pedidos completo: "crear pedido + descontar stock + cerrar carrito, con rollback si falla algo" no tenía forma segura de expresarse, cada escritura siendo autocommit individual. `transaction { ... }` es una expresión de bloque, misma familia que `if`/`match` (modo chequeo nada más, no sintetizable) -- `BEGIN` real antes del cuerpo, `COMMIT` si termina normal, `ROLLBACK` automático si cualquier `RuntimeError` se propaga desde adentro (`panic`, una violación de `@check`/`@unique`, lo que sea). `panic(...)` (ya existente, §3.34) es el mecanismo para abortar por una regla de negocio -- sin ningún `db.rollback()` nuevo, sin superficie de lenguaje extra. Publicación a `stream` DIFERIDA hasta el `COMMIT` (encolada mientras la transacción sigue abierta, descartada entera en un rollback) -- el punto no negociable del diseño: sin esto, un suscriptor vería una fila que la base después descarta. Sin anidamiento ni `return` en el cuerpo (v0, mismo criterio que `while`) -- los dos se rechazan en compilación. Los dos backends comparten la misma implementación (`Backend::execute_ddl("BEGIN"/"COMMIT"/"ROLLBACK")`, ya existente para DDL, sin código nuevo por motor). Verificado exhaustivamente: 6 tests de checker, 3 de runtime contra SQLite real (commit completo, rollback completo, la base sigue usable después), 2 de integración con un `stream` conectado por socket real confirmando que un rollback NUNCA genera un evento SSE mientras un commit sí genera exactamente uno, y 1 test contra Postgres real. Ver GRAMMAR.md §3.154.

**Hecho** (26/08/2026): **`upsert` empuja `matchFn` a SQL cuando es pusheable** (parte del ítem 1 de esta lista) -- sexto y último hallazgo del barrido de "límites honestos": una colección que crecía de cientos a decenas de miles de filas hacía que un `upsert` antes instantáneo empezara a tardar segundos, sin ningún error ni aviso -- se notaba por quejas de latencia, nunca por el compilador. Mismo criterio que `findWhere`/`countWhere`/`deleteWhere`: si `matchFn` tiene la forma `|x| x.campo == valor` (o una conjunción `&&` de varias así), usa `find_where_conjunction` en vez de traer la tabla entera; `||` o comparar dos campos entre sí siguen sin pushear (mismo límite ya documentado, ítem 1 de arriba). Verificado con un `matchFn` no pusheable confirmando que el camino interpretado sigue funcionando idéntico, y contra un Postgres real confirmando que el camino pusheado genera SQL válido en ese backend. Ver GRAMMAR.md §3.75.

**Hecho** (25/08/2026): **`@check(minLength/maxLength, N)`: constraints de longitud sobre `String`** (parte del ítem 3 de esta lista) -- misma forma "kind + argumento" y mismos dos puntos de enforcement que `@check(min/max/range, ...)` sobre numéricos (v1.60.0): aplicación Y un `CHECK (length(...) >= N)` real en la base, en los dos backends (`check_clause_sql` es la MISMA función compartida). Cuenta caracteres Unicode, no bytes, en los dos lados. `@check(minLength, 1)` expresa "no vacío". Verificado con un `INSERT` SQL crudo contra un Postgres real, rechazado por el `CHECK` sin pasar por c-script. Ver GRAMMAR.md §3.146.

**Hecho** (25/08/2026): **`deleteWhere` empuja la SELECCIÓN a SQL** (parte del ítem 1 de esta lista, la última que quedaba sin cubrir) -- con un predicado pusheable, usa `find_where_conjunction` (la MISMA función que `findWhere`/`countWhere`, respeta `@softDelete` automáticamente) para encontrar las filas a borrar, en vez de traer la colección entera y filtrar en el intérprete. El borrado en sí sigue fila por fila, a propósito -- un `DELETE ... WHERE` de una sola sentencia perdería la publicación a `stream` por fila que `delete()` ya hace. Verificado: predicado pusheable borra solo lo que matchea, un predicado no pusheable (dos campos del mismo parámetro comparados entre sí) cae al camino interpretado con el mismo resultado, y el test existente de soft-delete confirma que sigue respetándose. Ver GRAMMAR.md §3.145.

**Hecho** (25/08/2026): **`@cache("60s")` declarativo: cache de resultado del lado del servidor** (originalmente el ítem 5 de esta lista) -- keyeado por `(service, rpc, argumentos)`, un reintento dentro del TTL repite la respuesta grabada sin correr el cuerpo de nuevo. Dimensión ortogonal a `@cache_control` (le dice al cliente cuánto puede cachear) y `@idempotent` (opt-in por header para escrituras) -- automático, sin ningún header, pensado para lecturas costosas y poco cambiantes. Solo se graba un éxito. Alcance v0 deliberado: sin integración con `@invalidates` (esa es la cache del cliente) -- una entrada expira sola por tiempo. Verificado contra un `linkc serve` real: un rpc que inserta una fila real confirma, por conteo de filas, que un segundo POST dentro del TTL nunca corrió el cuerpo de nuevo. Ver GRAMMAR.md §3.144.

**Hecho** (25/08/2026): **`@idempotent`: idempotency keys nativas en rpcs de escritura** (originalmente el ítem 6 de esta lista, reforzado por Glowapp -- varios de sus webhook handlers de pago, `v2RevolutHandler.ts`/`orderConfirmation.ts`/`connections.ts`, implementaban a mano el chequeo "¿ya procesé este evento?" antes de escribir). `@idempotent` sin argumentos sobre un rpc (rechazado sobre un `stream`) -- opt-in por REQUEST vía el header `Idempotency-Key` (mismo nombre que Stripe usa para lo mismo), nunca forzado: un caller que no lo manda ve el rpc funcionar exactamente igual que siempre. Con el header, un `idempotency::IdempotencyStore` en memoria (mismo modelo que `rate_limit::RateLimiter`, un solo proceso servidor single-threaded) recuerda el resultado de la primera ejecución EXITOSA por `(service, rpc, clave)` -- un reintento con la misma clave y el mismo body (hasheado con SHA-256) repite la respuesta grabada sin correr el cuerpo de nuevo; la misma clave con un body distinto da 409, en vez de devolver en silencio un resultado que no corresponde. TTL de 24hs por entrada, no persiste entre reinicios (límite aceptado a propósito). Verificado de punta a punta contra un `linkc serve` real: `Orders.create` inserta una fila real en SQLite, un reintento con la misma clave confirma -- por CONTEO de filas, no solo por el resultado -- que la segunda request nunca insertó nada. Ver GRAMMAR.md §3.140.

**Hecho** (24/08/2026, cuarta ronda): **`countWhere`/`findWhere` empujan una conjunción `&&` de varias hojas** (parte del ítem 1 de esta lista, pedido explícito del usuario tras revisar el estado de IgnisLove/MyFinance -- "eso tenemos que solucionar") -- resuelve LOS DOS casos reales de CRM que la ronda de un solo operador (§3.108) había dejado sin cubrir: `notifications.link` (`n.userId == uid && !n.read`) e `inventory.link` (`p.stock <= 5 && p.stock > 0`, el MISMO campo dos veces en la misma conjunción). `ast::recognize_conjunction_predicate` reemplaza y generaliza `recognize_comparison_predicate` (una conjunción de una sola hoja es exactamente el caso de siempre, sin código duplicado) recorriendo el árbol de `&&` recursivamente; de paso reconoce dos formas de hoja booleana SIN operador explícito, necesarias para el caso real de `notifications.link`: `!x.campo` (`== false`) y `x.campo` solo (`== true`). `runtime/db.rs::conjunction_condition` arma `"f1" op1 $1 AND "f2" op2 $2 AND ...` con un placeholder posicional por hoja, en vez de uno solo. Alcance deliberado, igual criterio que §3.108: solo `&&` -- `||` necesitaría una cláusula `OR` separada en el SQL generado y no tiene evidencia real de demanda todavía, queda explícitamente abierto (ver ítem 1 de arriba). `deleteWhere` sigue sin ganar el atajo (mismo motivo de siempre: publicar cada fila borrada a `stream` complica un `DELETE` de una sola sentencia). Verificado con 1 test en `runtime/mod.rs` (los dos casos reales de CRM + las dos hojas booleanas sueltas) y 1 en `pg_integration.rs` contra un Postgres real confirmando que el `AND` con dos placeholders posicionales (`$1`/`$2`) bindea en el orden correcto. Ver v1.72.0, GRAMMAR.md §3.109.

**Hecho** (24/08/2026): **`List<Int>.sum() -> Int`** -- gap NUEVO, encontrado analizando "CRM" (Nexus, 11 `.link`, primer análisis de este adoptador) **con un bug de producción real y confirmado como evidencia**: `accounting.link`, `getAccountingSummary()`, necesitaba sumar montos reales de una lista ya filtrada en memoria (`incomeTx`/`expenseTx`) y, al no existir forma de sumar sin un `while` manual, el código quedó con un placeholder que multiplica la CANTIDAD de transacciones por una tarifa plana inventada (`incomeTx.length() * 1000`) en vez de sumar `t.amount` de verdad -- un reporte financiero mostrando números fabricados, no aproximados, en código que pasó sus propios tests porque estos nunca verificaban esos campos puntuales. Alcance deliberadamente acotado a `List<Int>` en esta ronda -- NO `List<Int64>`/`List<Float>`: en runtime `Value::List` no lleva ningún tag de tipo de elemento (mismo límite ya documentado para `Value::Uuid`), así que una lista VACÍA de `Int64`/`Float` no tiene de dónde sacar qué variante de `Value` devolver sin construir infraestructura de recuperación de tipo estático que esta ronda no amerita -- `List<Int>` no tiene esa ambigüedad (un solo tipo posible, incluso vacía). Ver v1.65.0, GRAMMAR.md §3.101. **`db.<c>.maxRow(selector)`/`db.<c>.minRow(selector) -> T?`** (originalmente el ítem 8 de esta lista, nombrado `top()` al descubrirse) -- gap NUEVO, encontrado analizando IgnisLove en profundidad, **con un bug de producción real y confirmado como evidencia**: `bandit_rewards.link`, `getBestArm()`, hacía `db.arms.all()` y devolvía `allArms[0]` -- el orden de `all()` es por `id` (§3.48), NUNCA por el campo de recompensa, así que ese rpc JAMÁS devolvía el brazo con mejor `avgRewardTenths` pese a su nombre. `maxBy`/`minBy` (§3.52) ya existían pero solo agregan un VALOR agrupado (siempre `{key, value}[]`), nunca la fila completa que lo alcanza. Dos métodos nuevos, no uno con un parámetro de dirección (`dir: "asc"|"desc")`) -- mismo criterio de "nombre por forma" que ya usa §3.52 para no inventar un query builder ni una API con modo-por-string. Mismo shape reconocido (`field_selector`, reusado tal cual de `maxBy`/`minBy`) y mismas restricciones de tipo que el campo de valor de esos dos (`Int`/`Int64`/`Float`, nunca opcional) -- `SELECT ... ORDER BY "<campo>" {DESC|ASC} LIMIT 1`, respetando `@softDelete` igual que el resto de las lecturas. `Value::Null` sobre una colección vacía, nunca un error. Ver v1.66.0, GRAMMAR.md §3.102. **`db.<c>.increment(id, selector, delta) -> T`** (originalmente el ítem 7 de esta lista) -- gap NUEVO, mismo análisis de IgnisLove, **con un riesgo de producción real y confirmado como evidencia** (lost-update estructuralmente posible, no ya materializado): tres `.link` (`bandit_rewards`, `bot_defense`, `banners`) hacían read-then-write manual con `upsert`/`updateFn` para contadores -- en la topología real de este adoptador (varios procesos `linkc serve-all`/pm2 compartiendo un único Postgres), dos procesos pueden leer el mismo valor antes de que el otro escriba y perder un incremento. `UPDATE "campo" = "campo" + ?` real, sin ninguna lectura previa -- la atomicidad la da el motor (row-level locking), no c-script. Alcance acotado a `Int` (Int64/Float deliberadamente afuera, sin evidencia real de demanda). Verificado con el test más fuerte posible: 20 hilos incrementando la MISMA fila 500 veces en total contra un Postgres real, conteo final exacto. Ver v1.68.0, GRAMMAR.md §3.105. **`countWhere`/`findWhere` empujan a SQL `!=`/`<`/`<=`/`>`/`>=`** (parte del ítem 1 de esta lista, el otro operador simple resuelto) -- reforzado por CRM: de los tres casos reales citados, solo `chat.link` (`c.unreadCount > 0`, un único operador) se beneficia -- `notifications.link`/`inventory.link` combinan `&&`, que sigue abierto. `ast::recognize_comparison_predicate` generaliza el reconocimiento (antes `recognize_equality_predicate`, solo `==`) a los cinco operadores relacionales restantes, con el operador "enderezado" cuando el campo aparece del lado derecho (`valor < campo` se trata como `campo > valor`). Alcance deliberado: un solo operador por predicado, `&&`/`||` sigue sin pushear -- ítem grande, queda para una ronda dedicada. Ver v1.71.0, GRAMMAR.md §3.108. **Lint `delete-then-insert-same-id`** (originalmente el ítem 7 de esta lista) -- gap NUEVO, mismo análisis de IgnisLove: varios `.link` del repo (`bandit_rewards`, `bot_defense`, `stock_cache`, `catalog_facets`, `seo_engine`, `rfm_scorer`) documentan en comentarios propios por qué migraron de "borrar e reinsertar" a `upsert`/`applyPatch` ("delete+insert con autoincrement no reproduce el id"); `banners.link` todavía no había migrado. El motivo real: `insert()` SIEMPRE asigna un id nuevo por autoincrement, nunca respeta un `id:` literal -- el patrón NO preserva la fila aunque el código lo intente. Shape detectado: `db.<c>.delete(X)` seguido más adelante en el mismo bloque de `db.<c>.insert(Tipo { id: X, ... })` sobre la misma colección con la misma expresión `X`. Mismo estilo que `hardcoded-secret-literal` (v1.62.0) -- puramente informativo. Ver v1.69.0, GRAMMAR.md §3.106. **`db.<c>.upsert(matchFn, insertValue, updateFn)`**: ver v1.40.0, GRAMMAR.md §3.75. **`db.<c>.insertMany(items)`**: cada elemento pasa por el mismo `insert` real de siempre (una sentencia SQL autocommit por fila) -- lo que ahorra es la ida y vuelta HTTP N veces desde el cliente, no el costo de N inserts contra la base. Sin transacción envolvente (mismo criterio "autocommit por sentencia" del resto del lenguaje): si un ítem falla a mitad de la lista, los anteriores quedan insertados. Ver v1.41.0, GRAMMAR.md §3.76. **`createdAt`/`updatedAt` automáticos**: resuelto SIN ninguna anotación mágica por nombre de campo -- es la composición de dos primitivas ya existentes (`now()` builtin + default de campo `= now()`, §3.74) para "asignado una sola vez al crear", más una anotación chica y nueva, `@autoUpdate` (solo sobre `Timestamp`), para la única parte que de verdad faltaba: "pisar a `now()` en CADA `applyPatch`/`upsert`-actualización, incluso si el patch no lo menciona". Ver v1.42.0, GRAMMAR.md §3.77. **Soft-delete nativo**: un campo `@softDelete` sobre `Timestamp?` -- `delete(id)` deja de ser un `DELETE` SQL, pasa a un `UPDATE` idempotente que fija el campo a `now()`; toda lectura que devuelve lista o conteo (`all`/`page`/`pageAfter`/`count`/`sumBy`-etc., más `findWhere`/`deleteWhere` que las reusan por dentro) filtra automáticamente. Límite deliberado: `find(id)` NO filtra -- una fila borrada sigue siendo encontrable por id directo (mismo criterio que Django/Rails para el mismo problema), necesario además para que la re-consulta interna de `insert`/`applyPatch` no explote si un patch toca justo ese campo. Ver v1.43.0, GRAMMAR.md §3.78. **`linkc build --diff <archivo-anterior>`** (originalmente el ítem 8 de esta lista): reusa el mismo diff LCS que `linkc test` ya tenía para mostrar por qué un snapshot cambió -- ahora también se puede pedir desde `linkc build`, comparando el `contract.d.ts` recién generado contra un archivo guardado aparte (típicamente `git show <rev>:ruta > archivo`). Puramente informativo, nunca hace fallar el build. Ver v1.44.0, GRAMMAR.md §3.79. **Índices declarativos de un solo campo: `@index`/`@unique`** (originalmente parte del ítem 3 de esta lista, la parte de VARIOS campos a la vez sigue abierta -- ver arriba): dos anotaciones de campo sin paréntesis, a lo sumo una por campo (rechazado en el parser, mismo criterio que `@autoUpdate`/`@softDelete`) -- ninguna exige un tipo de campo particular, a diferencia de esas dos. El índice se crea de verdad al arrancar en LOS DOS backends (`CREATE [UNIQUE] INDEX IF NOT EXISTS`, idempotente, nombre determinístico `idx_<tabla>_<campo>`), y `linkc build` emite la misma sentencia en el DDL estático de Postgres. Una violación de `@unique` en `insert`/`applyPatch` se traduce a 400 (detectando el mensaje específico que cada motor devuelve para esta violación puntual), no a un 500 genérico. `--adopt-existing` nunca ejecuta este DDL, mismo criterio que el resto del schema. Ver v1.45.0, GRAMMAR.md §3.80. **`Timestamp` decodifica `date`/`timestamp`/`timestamptz` nativos de Postgres** -- gap NUEVO, la otra mitad del mismo segundo reporte de MyFinance que motivó `dateFromParts` (§9.2): una tabla YA EXISTENTE adoptada (el caso normal, no una tabla que `linkc build` creó) casi siempre tiene sus columnas de fecha en el tipo NATIVO de Postgres, no en el `BIGINT` propio de c-script -- auditando `runtime/store.rs` apareció que esto estaba roto en los DOS sentidos (declarado `String`: falla por wire binario no-UTF8; declarado `Timestamp`: fallaba TAMBIÉN, el OID nativo no matchea ninguno de los anchos de entero que se probaban). `ColumnKind::Timestamp` nuevo, decodificado a mano contra el wire binario de Postgres (sin sumar `chrono`) con el mismo espíritu que el algoritmo de calendario de Hinnant ya existente. `linkc introspect` (§3.66) ahora recomienda `Timestamp` sin advertencia para estas columnas -- antes recomendaba `String`, una recomendación que en los hechos también estaba rota. Alcance: solo LECTURA -- escribir contra una columna nativa adoptada sigue sin funcionar, no era parte del caso reportado. Ver v1.55.0, GRAMMAR.md §3.91. **Aviso de colisión de nombre de tabla en PostgreSQL** (originalmente el ítem 5 de esta lista, priorizado de nuevo en un quinto reporte del usuario que cita el mismo incidente real, `telemetry.link` casi choca contra un `events` ajeno): `CREATE TABLE IF NOT EXISTS` es un no-op sobre una tabla que ya existe -- no mira si sus columnas tienen algo que ver con lo declarado, así que la migración no destructiva de Postgres (`ADD COLUMN IF NOT EXISTS`) le agregaría, en silencio, TODAS las columnas del programa nuevo a una tabla ajena. Antes de migrar una tabla preexistente, si NINGUNA columna declarada (aparte de `id`) coincide por nombre con las que la tabla ya tiene, se imprime una advertencia por stderr -- pero la migración sigue igual que siempre, nunca bloquea el arranque: dos `.link` DISTINTOS compartiendo una tabla con columnas disjuntas es un caso YA soportado y probado a propósito (matriz de §3.17, "Columna eliminada del `.link`"), indistinguible de una colisión accidental solo mirando "cero columnas en común" -- convertirlo en error habría roto ese caso legítimo. Solo Postgres: SQLite (`check_schema_matches`) ya falla fuerte ante cualquier diferencia de schema. Ver v1.58.0, GRAMMAR.md §3.94. **`db.<c>.countWhere(predicate) -> Int` + `findWhere` empujado a SQL para el caso `|x| x.campo == valor`** (originalmente parte de los ítems 1/2 de esta lista, priorizados de nuevo en el mismo tercer reporte del usuario sobre IgnisLove -- "agregué `@index` a `reviews.productId`/`telemetry.sessionId` y no aceleró nada, cada `.filter()` sigue trayendo la tabla entera a memoria"): `countWhere` es un método NUEVO (mismo contrato de tipos que `findWhere`/`deleteWhere`, `fn(T) -> Bool`) que reconoce estáticamente el shape `|x| x.campo == valor` (`ast::recognize_equality_predicate`, un literal o una variable capturada del entorno externo del lado derecho) y lo traduce a un `SELECT COUNT(*) ... WHERE` real -- CERO filas viajan del motor al proceso. `findWhere` gana el mismo atajo (antes traía toda la colección siempre). Cualquier otra forma de predicado (otro operador, `&&`/`||`, comparar dos campos entre sí) cae al camino interpretado de siempre -- correcto en cualquier caso, solo más lento en ese caso puntual, nunca silenciosamente incorrecto. `deleteWhere` NO gana este atajo todavía (sigue trayendo todo + borrando fila por fila) -- queda en el ítem 1 de arriba, junto con el resto de formas de predicado. Ver v1.59.0, GRAMMAR.md §3.95. **`@check(min/max/range, ...)` -- constraints numéricos de nivel de BASE** (originalmente el ítem 3 de esta lista, priorizado de nuevo en un séptimo reporte del usuario, con el ejemplo exacto: "`reviews.link` solo evita un rating fuera de 1-5 porque `clampRating()` lo fuerza en el código; no hay ninguna barrera a nivel de base si algún día otro rpc inserta sin pasar por esa función"): tres formas (`@check(min, N)`/`@check(max, N)`/`@check(range, N, M)`, mismo criterio "kind + argumento(s)" que `@validate`), solo sobre `Int`/`Int64`/`Float` (requerido u opcional). Enforcement DOBLE: aplicación (`apply_field_validators`, mismos dos puntos de entrada que `@validate` -- wire y `StructLit` construido en el cuerpo de un rpc, 400 nombrando el campo) Y base de datos (`CHECK (...)` inline de verdad en el `CREATE TABLE`, en los DOS backends -- confirmado con un `INSERT` SQL crudo, sin pasar por c-script en absoluto, rechazado por SQLite y por Postgres reales). `--adopt-existing` nunca ejecuta este DDL (mismo criterio que `@index`/`@unique`), pero la validación de aplicación sigue aplicando. Alcance deliberado: solo rangos numéricos simples, ninguna expresión booleana arbitraria (comparar dos campos entre sí) ni constraint sobre `String` -- ver ítem 3 de arriba para lo que queda. Ver v1.60.0, GRAMMAR.md §3.96. **`linkc migrate --dry-run`** (originalmente el ítem 5 de esta lista, priorizado de nuevo en un octavo reporte del usuario: "antes de apuntar cualquier servicio a una tabla real con `--adopt-existing`, ver el DDL exacto que se ejecutaría sin aplicarlo todavía sería la verificación que le falta a ese paso"): `linkc migrate <archivo.link> --db <url-postgres> --dry-run` (módulo nuevo, `src/migrate.rs`) conecta de solo lectura y reporta el `CREATE TABLE`/`ALTER TABLE ADD COLUMN` exacto que `linkc serve` ejecutaría, sin ejecutar nada -- reusa las MISMAS funciones puras de generación de DDL que ya usa el runtime real (nunca una copia propia que pudiera desincronizarse). También corre el chequeo de colisión de tabla (§3.94) y de tipo de `id` (§3.36) antes de que alguien intente conectar en serio. Solo Postgres (SQLite ya falla fuerte con el diff exacto al conectar de verdad); sin `--allow-destructive` porque auditando la migración real apareció que hoy no existe ningún camino destructivo que advertir (solo crea/agrega, nunca borra ni cambia tipos). Ver v1.61.0, GRAMMAR.md §3.97. **`linkc test --db <url-postgres>`** -- gap NUEVO, encontrado por un segundo reporte de MyFinance verificando el fix de decodificación de Postgres (§3.91) contra su propio esquema real: "`linkc test` corre contra SQLite embebido, que NO reproduce el bug original... sin esto, el fix está 'compilado y probado con datos falsos', no 'verificado'". Cierto: el motor de `test "..." { ... }` siempre creaba SQLite `:memory:` nueva por test, sin forma de apuntar a Postgres -- los dos backends emiten SQL y decodifican el wire distinto, así que pasar contra SQLite no prueba nada sobre Postgres real. `--db <url-postgres>`/`LINK_TEST_DB` (env var DISTINTA de `LINK_DATABASE_URL`, a propósito -- para que `linkc test` nunca use por accidente la URL de producción/desarrollo de `serve`) corre TODOS los tests contra esa base real. Límite honesto documentado, no escondido: sin el aislamiento por test que `:memory:` da gratis -- Postgres no tiene equivalente de "`:memory:`", así que los tests COMPARTEN estado (lo que uno inserta, el siguiente lo ve), en vez de fingir un reset automático que sería destructivo (mismo criterio de "sin --allow-destructive" de `linkc migrate --dry-run`, arriba). Correr esto contra una base de TEST dedicada, nunca producción, es responsabilidad de quien pasa la URL. Ver v1.63.0, GRAMMAR.md §3.99.

**Hecho** (27/08/2026, decimosexta ronda -- primer ítem "sin bloqueos" atacado tras cerrar `AUDIT-FIX-PLAN-2026-08-27.md`): **`countWhere`/`findWhere`/`deleteWhere`/`upsert` empujan `||` combinando condiciones**, en cualquier profundidad de anidamiento con `&&` (parte del ítem 1 de esta lista, el hueco que §3.109 había dejado explícitamente abierto). `ast::PredicateExpr` (árbol `Leaf`/`And`/`Or`) reemplaza la lista plana de hojas de `recognize_conjunction_predicate` (eliminada, sin callers) -- respeta la precedencia REAL del lenguaje (`&&` liga más fuerte que `||`, `a && b || c` reconoce `(a && b) || c`, nunca `a && (b || c)`), aplanando una cadena del MISMO tipo (`a && b && c` sigue siendo un solo `And` de 3 hojas) para que el SQL del caso puro de siempre no gane paréntesis de más. `db.rs::condition_expr_sql` recorre el árbol parentizando solo un hijo compuesto del tipo CONTRARIO al de su padre, y el `WHERE` entero se parentiza antes de AND-ear `@softDelete` si el árbol de nivel superior es un `Or` (sin esto, el filtro de soft-delete se perdería sobre la mitad izquierda de la disyunción). Mismo comportamiento NULL-seguro (`IS NULL` en vez de `= ?` con un parámetro NULL) ahora también dentro de una rama `||`. Verificado con una disyunción pura de 3 hojas, `&&` mezclado con `||` confirmando la precedencia exacta, una hoja NULL adentro de un `Or`, más repetición en vivo contra un `linkc serve` real (`countWhere`/`findWhere`/`deleteWhere` con predicados mixtos, conteos exactos). Sigue sin cubrir: comparar dos campos del propio parámetro entre sí (`endDate > startDate`). Ver GRAMMAR.md §3.170.

**Hecho** (27/08/2026, decimoséptima ronda -- cierra por completo el ítem 1 de esta lista): **`countWhere`/`findWhere`/`deleteWhere` empujan comparaciones campo-vs-campo** (`item.endDate > item.startDate`) para los cuatro operadores relacionales (`<`/`<=`/`>`/`>=`). `ast::PredicateOperand` gana una tercera forma, `Field(&str)` (junto a `Expr`/`Bool`) -- reconocida SOLO para los relacionales, nunca `==`/`!=`: el checker (`synth_binary`, brazo `Lt | LtEq | Gt | GtEq`) exige ambos lados `Int`/`Int64`/`Float`/`Timestamp` sin `Optional`, y un campo no opcional siempre es columna `NOT NULL` -- así que esta forma nunca puede toparse con NULL para una tabla que c-script creó, a diferencia de `==`/`!=` (donde el checker sí deja comparar dos `T?`, y `NULL = NULL` en SQL no es `true` como en el camino interpretado) -- replicar esa NULL-seguridad columna-vs-columna se dejó fuera, sin caso real que la pida. `runtime::ConditionExpr` gana `FieldPair(String, BinaryOp, String)`; `db.rs::field_pair_condition_sql` genera `"campoA" OP "campoB"` directo, sin placeholder, integrado al mismo recorrido recursivo de `&&`/`||` que cualquier otra hoja. Verificado con los cuatro operadores, el caso mezclado con una hoja normal adentro de un `&&`, `deleteWhere` empujando la selección, y repetición en vivo contra un `linkc serve` real. Único límite honesto que queda: `==`/`!=` entre dos campos (documentado, sin evidencia de demanda). Suite completa sin regresiones. Ver GRAMMAR.md §3.171.

**Hecho** (28/08/2026, decimonovena ronda -- cierra por completo el ítem 3 de esta lista): **`@check(<expr>)` a nivel de `type`**, una expresión booleana comparando campos del propio struct entre sí (`endDay > startDay`), complementando (sin reemplazar) el `@check(min/max/range/minLength/maxLength, ...)` de un solo campo ya existente. Acotado a lo que un `CHECK` de SQL puede expresar -- `ast::validate_check_expr_shape` rechaza en el checker, antes de tipar, cualquier forma que no sea identificador/literal/`!`/`-` unario/paréntesis/los operadores `==`/`!=`/`</<=`/`>`/`>=`/`&&`/`||`/`+`/`-`/`*`/`/`/`%` -- ninguna llamada, acceso a `db`, closure ni índice. Enforcement DOBLE, mismo criterio que el resto de `@check`: un `CHECK` real de TABLA en el `CREATE TABLE` (`runtime::db::type_check_expr_sql`, compartida por los dos backends) Y aplicación (`runtime/mod.rs::eval_check_expr`, un evaluador chico y autocontenido que de todos modos reusa `checked_int_numeric_op`/`compare`/`as_bool` del intérprete general, para no duplicar esas reglas). Un `applyPatch` parcial que no toca ninguno de los campos referenciados saltea la expresión entera, sin evaluarla ni rechazar en falso. Verificado con 9 tests de checker, 4 de runtime (incluida la distinción patch-parcial-saltea vs. patch-que-toca-los-dos-campos-se-evalúa), 1 de DDL estático, 1 contra un Postgres real (acepta/rechaza/rechaza-por-SQL-crudo), y repetición en vivo contra SQLite (`.schema` + `INSERT` crudo por `sqlite3`). Suite completa sin regresiones (1285 tests, +15). Ver GRAMMAR.md §3.173.

**Hecho** (28/08/2026, vigésima ronda -- cierra por completo el ítem 2 de esta lista, incluido el caso exacto de Glowapp): **`@unique(...) where <expr>`**, la mitad CONDICIONAL de `@unique` compuesto que §3.155 había dejado explícitamente afuera de su ronda. Reusa DIRECTO la infraestructura que `@check(<expr>)` de tipo (ronda anterior, misma sesión) acababa de construir -- `Checker::check_type_level_check_expr` valida la condición exactamente igual (mismo subconjunto de gramática, mismo tipado contra `Bool`), `runtime::db::type_check_expr_sql` la traduce a SQL, sin ninguna infraestructura nueva del lado de la aplicación (a diferencia de `@check`, `@unique` nunca tuvo enforcement de aplicación -- siempre fue puramente un constraint de base). El índice único compuesto se vuelve PARCIAL (`CREATE UNIQUE INDEX ... WHERE <condición>`), reproduciendo el caso exacto de Glowapp: `@unique(userId, appointmentDate, startTime) where status != "cancelled"` -- dos turnos con el mismo horario chocan SOLO si ninguno está cancelado. La condición puede referenciar CUALQUIER campo del struct, no solo los del conjunto único (`status` es ajeno a `(userId, appointmentDate, startTime)`). Bug encontrado y arreglado en el camino: el nombre determinístico del índice no podía concatenar la condición SQL tal cual (comillas/paréntesis rompían el identificador entre comillas que lo envuelve) -- se hashea con el mismo SHA-256 que `lockfile::hash_source` ya usaba para otra cosa, en vez de escapar a mano o sumar una segunda implementación de hashing. El dedup de redundancia ahora es por `(conjunto de campos, condición)`, no solo por conjunto -- dos `@unique` con los mismos campos pero condiciones DISTINTAS son dos constraints parciales legítimos, no un duplicado. Verificado con 7 tests de checker, 1 de runtime contra SQLite real (reproduce el caso de Glowapp: mismo horario confirmado rechaza, mismo horario cancelado se acepta), 1 de DDL estático, 1 contra un Postgres real, y repetición en vivo contra SQLite (`.schema` confirma el índice parcial exacto). Suite completa sin regresiones. Ver GRAMMAR.md §3.174.

### 9.4 HTTP y Diseño de API
1. ~~**Hooks de middleware de usuario**: lógica "antes"/"después" de cualquier rpc, para logging o métricas transversales -- cambio real en el modelo de ejecución.~~ -- **DISCOVERY HECHO (30/08/2026), NO SE CONSTRUYE por ahora -- sin evidencia de demanda real.** A diferencia de casi todo lo demás en este backlog (cada ítem resuelto en esta sesión cita un adoptador concreto -- MyFinance, Glowapp, IgnisLove, skynet-d3/skynet-43), esta entrada no tiene ninguna cita: viene del volcado masivo de 272 ítems de la Fase 5 (commit `ab35d0d`, 24/08/2026) y nunca se rastreó a un dolor concreto -- el ítem peor evidenciado de todo ese lote, por el propio estándar de citas de este proyecto. Auditando el código real antes de proponer nada: lo que el título pide ("logging o métricas transversales") ya está resuelto en un ~80% por lo que existe hoy sin anotar nada -- el logging estructurado (`log_done_with_audit`, GRAMMAR.md §3.122) da antes/después por rpc gratis en cada request (`req_id`/`method`/`status`/`duration_ms`, más `auth_role`/`auth_user_id`/`auth_allowed` en cualquier rpc `@requires`/`@authenticated`), y `/metrics` (GRAMMAR.md §3.149) suma conteo+duración por rpc, gauges de suscriptores de `stream`, tamaño de la base, rechazos de rate limit, corridas de `@cron`, latencia de NOTIFY -- todo ya cableado al mismo camino de despacho. El hueco real que SÍ queda, más angosto que "middleware genérico": correr código `.link` propio del usuario (no un contador fijo del lado Rust) antes/después de CUALQUIER rpc sin anotar cada uno a mano -- por ejemplo, escribir una fila de auditoría propia con los valores reales de los argumentos, o llamar a un sistema externo. Nadie lo pidió todavía. Se deja explícitamente abierto, no descartado -- si aparece un caso real (un adoptador con esa necesidad puntual, citable como los demás), se retoma con ese caso como ancla de diseño en vez de la generalización especulativa original.
3. ~~**Compresión gzip/brotli** de la respuesta, opcional vía flag -- necesitaría sumar `flate2` (no hand-rolleable de forma razonable, a diferencia de HMAC/SigV4), la 3ra excepción a "cero dependencias nuevas".~~ -- **RESUELTO. Entrada desactualizada -- ya se había atacado y cerrado, esta entrada nunca se marcó.** Solo GZIP (no brotli, sin evidencia de demanda de un ratio mejor), transparente vía negociación de contenido HTTP estándar (`Accept-Encoding`/`Content-Encoding`, sin flag ni anotación nueva) -- `flate2` terminó siendo la SEGUNDA excepción real a "cero dependencias" (después de `regex`), no la tercera (`aes-gcm`, para `@encrypted`, fue la tercera). Umbral mínimo de 1024 bytes, un `stream` SSE queda excluido estructuralmente. Ver GRAMMAR.md §3.180.

**Hecho** (27/08/2026): **`http.postWithRetry(url, body, headers, maxAttempts)`: reintentos con backoff para webhooks salientes** (originalmente el ítem 2 de esta lista, "webhooks salientes declarativos: registrar una URL de terceros y que el runtime reintente/firme automáticamente"). Auditado antes de escribir código: la mitad de FIRMAR ya funciona hoy sin ningún primitivo nuevo (`crypto.hmacSha256` + `http.postWithHeaders`, mismo patrón que la auditoría de proveedores de §3.112 ya documentó para Stripe/SendGrid/etc.) -- el gap real, el único que GRAMMAR.md §3.86 ya dejaba anotado como pendiente, era que ninguna llamada `http.*` reintenta sola ante una falla transitoria. Nuevo método en el namespace `http` YA existente (no un namespace nuevo -- confirmado que eso hubiera significado tocar el enum `Type` central, mucho más superficie de la que este ítem amerita): `maxAttempts: Int` es el único parámetro nuevo, backoff exponencial FIJO (200ms doblando, techo de 5s, mismo criterio "backstop generoso, no configurable" que `MAX_WHILE_ITERATIONS`) -- deliberadamente mucho más corto que el techo de 30s de `--restart-backoff`, porque esto bloquea el hilo de UNA request (§3.158), no un proceso servidor entero. Reintenta ante CUALQUIER falla (red o no-2xx, mismo criterio que `post`/`postWithHeaders`), sin distinguir 4xx de 5xx -- alcance v0 deliberado, sin evidencia de que la distinción fina haga falta todavía. Verificado con 3 tests de integración contra un servidor de mentira real (2 fallas transitorias + éxito dentro del presupuesto; un 500 persistente agotando el presupuesto falla limpio; `maxAttempts=0` falla ANTES de mandar ninguna request real) + 1 test unitario confirmando la progresión exacta del backoff. Ver GRAMMAR.md §3.160.

**Hecho** (25/08/2026): **`@cors("...")`: override de CORS por ruta** (originalmente el ítem 4 de esta lista) -- `@cors("https://a.com, https://b.com")` o `@cors("*")` sobre un rpc/stream reemplaza ENTERO al CORS global (`--cors-origin`) para ese endpoint puntual, para el caso real de una API detrás de un allowlist salvo un endpoint público. Aplica tanto al preflight `OPTIONS` como a la respuesta real -- exigió resolver (service, rpc) del path ANTES del chequeo de `OPTIONS`, reusando `resolve_route` con un body vacío (seguro: determinar CUÁL rpc es nunca depende del body). Verificado contra un `linkc serve` real: un rpc con `@cors("*")` ignora un allowlist global restrictivo; uno con `@cors("https://partner.example.com")` ignora un CORS global abierto; y el override también aparece en el preflight, no solo en la respuesta real. Ver GRAMMAR.md §3.147.

**Hecho** (25/08/2026): **`--hsts`: `Strict-Transport-Security` opt-in** (originalmente el ítem 5 de esta lista). `linkc serve` nunca termina TLS por sí solo (§3.41 lo documentaba como el motivo para NO mandar este header nunca), pero en producción real es común que un proxy de confianza sí lo termine delante -- mismo patrón que `--trust-proxy` ya resuelve para `X-Forwarded-For`. `--hsts <valor>`/`LINK_HSTS` -- texto LITERAL (mismo criterio que `@cache_control`, sin parsear la gramática interna de HTTP), sin el flag ningún header (comportamiento idéntico a siempre). Se manda en TODA respuesta, incluido un `stream` SSE (`sse_preamble`, el otro lugar que arma headers a mano). Verificado contra un `linkc serve` real: sin el flag, sin header; con `--hsts "max-age=63072000; includeSubDomains"`, el valor literal en `/health`, en un rpc normal Y en un `stream`; `LINK_HSTS` como env var. Ver GRAMMAR.md §3.143.

**Hecho** (25/08/2026): **`@rate_limit(..., key: <param>)`: una clave adicional a la IP** (originalmente el ítem 6 de esta lista, gap encontrado analizando Glowapp -- su propio middleware `v2RateLimit.ts` limita por `prefix:ip:email` en vez de solo IP, con un comentario propio explicando el motivo: un endpoint de pago dejaba rotar de IP reusando el mismo email para seguir abusando). Segundo argumento OPCIONAL de `@rate_limit`, `key: <nombre>`, nombrando un parámetro real del rpc (`String`/`Int`, los únicos dos que se pueden combinar con la IP sin ambigüedad). Sin `key:`, comportamiento idéntico a siempre. La clave del bucket pasa de "solo IP" a `"{ip}|{param}={valor}"` -- `rate_limit::RateLimiter` no cambió nada de su lado, sigue recibiendo un string de identidad único, armado ahora en `server.rs` a partir del body ya parseado de la request. Verificado contra un `linkc serve` real: dos requests con el mismo valor de clave desde la misma IP comparten balde y el tercero da 429; con un valor DISTINTO desde la MISMA IP, balde propio sin verse afectado -- la prueba directa de que la clave combina IP+valor, no reemplaza la IP. Ver GRAMMAR.md §3.142.

**Hecho** (24/08/2026): **Timeout configurable en `http.*`** (originalmente el ítem 1 de esta lista): auditando `runtime/mod.rs` apareció que `http.get`/`post`/`getWithHeaders`/`getWithStatus`/`postWithStatus`/`postWithHeaders` no fijaban NINGÚN timeout de lectura/escritura propio -- `ureq` (la crate) trae 30s de timeout de CONEXIÓN por default, pero el de lectura/escritura es "nunca" por default, documentado así por la propia crate. Para un intérprete de un solo hilo, eso significaba que una request saliente a un servidor lento o colgado bloqueaba el proceso ENTERO para siempre, ni siquiera `/health` respondía mientras tanto -- un bug de disponibilidad real, no solo un gap de documentación. `--http-timeout`/`LINK_HTTP_TIMEOUT` (mismo formato `Ns`/`Nm`/`Nh`/`Nd` que `--session-ttl`, default 30s -- el mismo número que `ureq` ya usaba para conexión) fija un timeout total por llamada, guardado en `Db` con el mismo mecanismo que `argon2_params`. **Reintentos** (la otra mitad de este ítem, resuelto por separado -- ver `http.postWithRetry` arriba) sigue sin resolver acá mismo, a propósito: un timeout u otro error de red fallaba la llamada y reintentar quedaba en manos del código de usuario hasta que `postWithRetry` lo cerró. Ver v1.51.0, GRAMMAR.md §3.86.

**§9.4 completo** salvo el ítem 1 (hooks de middleware de usuario) -- discovery hecho, dejado explícitamente abierto sin evidencia de demanda real, ver arriba.

### 9.5 Autenticación y Seguridad
1. ~~**Lint sobre "autorización de fachada"**~~ -- **RESUELTO (30/08/2026), como `manual-role-check-without-requires`.** Reformulado tras auditarlo (24/08/2026): la forma original ("`@requires(Role.X)` que nunca llama a `auth.currentRole()`/`currentUserId()`") era de mala señal -- el caso MÁS COMÚN y CORRECTO de `@requires(Role.Admin)` nunca llama a ninguno de los dos. La versión implementada es la INVERSA: un rpc que llama a `auth.currentRole()`/`currentUserId()` para su PROPIA verificación manual de rol, SIN `@requires`/`@authenticated` en su propia anotación -- el chequeo real vive en lógica ad-hoc del cuerpo, un bug ahí bypasea todo en silencio. Ver GRAMMAR.md §3.188.
2. ~~**Cifrado de campo a nivel de columna** (`@encrypted` en un `String` sensible).~~ -- **RESUELTO (30/08/2026). Última pieza de la ronda de seguridad completa, cierra §9.5 entero.** AES-256-GCM (`aes-gcm`, tercera excepción real a "cero dependencias nuevas" tras `regex`/`flate2` -- un cifrador simétrico nunca debería hand-rollearse). Puramente a nivel de almacenamiento: el `Value` que ve el resto del programa sigue siendo el `String` plano. `--encryption-key`/`LINK_ENCRYPTION_KEY` (32 bytes base64) obligatoria solo si el programa declara algún campo así marcado -- `linkc serve` rechaza arrancar sin ella. La guarda de correctness real: `findWhere`/`countWhere`/`deleteWhere` sobre un campo `@encrypted` caen al camino interpretado de siempre (mismo guard que ya existía para columnas JSON, extendido una línea) en vez de pushear a SQL una comparación contra ciphertext que nunca podría matchear; `sumBy`/`countBy`/etc. (sin camino interpretado de respaldo) y `@index`/`@unique` en el mismo campo se rechazan en compile-time, porque el nonce aleatorio hace que agrupar/unicidad sobre ciphertext sea una garantía falsa, no solo redundante. Verificado con 9 tests unitarios de cifrado + 8 de checker + 7 de CLI contra el binario real (SQLite, confirmando la fila física con `linkc db shell`) + 2 contra Postgres real. Límite honesto: `db export`/`import` todavía no soportan una colección con campos `@encrypted` (rechazan de entrada); `db shell` no necesita cambios (ya muestra el ciphertext crudo, consistente con cualquier otro valor físico). Ver GRAMMAR.md §3.191.
3. ~~**RBAC por recurso**: permisos más allá de todo-o-nada por rol.~~ -- **RESUELTO (30/08/2026), junto con el ítem 4 -- ver abajo, son la MISMA feature.**
4. ~~**ABAC**: reglas basadas en atributos del propio recurso (ej. "solo el dueño de la factura").~~ -- **RESUELTO (30/08/2026), como `@requires(..., ownerOf: <colección>, id: <parámetro>, field: <campo>)`.** El ejemplo motivador de este ítem ("solo el dueño de la factura") y el pedido vago del ítem 3 resultaron ser la MISMA necesidad al diseñarlos: una condición adicional, más allá del rol, evaluada contra un recurso real guardado en `db`. Un solo mecanismo nuevo cierra los dos -- deliberadamente angosto (forma FIJA `campo == currentUserId()`, sin expresión arbitraria ni reuso de la máquina de `@check`), `id:` nombra explícitamente el parámetro del rpc que trae el id del recurso (mismo criterio que `@rate_limit(..., key: ...)`). El chequeo de rol (`check_auth_gate`) no cambia en nada -- una etapa nueva y separada corre DESPUÉS, decodificando solo el parámetro nombrado y comparando contra `sessions.user_id_for(token)`. Un rol que necesite bypasear el chequeo de dueño se modela como un rpc separado sin cláusula -- nunca una excepción implícita por rol. Verificado con 8 tests de checker + 7 de parser + 5 contra el binario real (`server_http.rs`: dueño real 200, no-dueño 403, recurso inexistente 404, rol incorrecto 403 sin tocar la base incluso contra un id inexistente, Admin no exento en el rpc compartido pero sí vía su propio rpc separado). Ver GRAMMAR.md §3.190.

**Hecho** (25/08/2026): **Bloqueo de cuenta configurable** (originalmente el ítem 1 de esta lista) -- sin `login` nativo en el lenguaje, no podía ser un mecanismo automático por anotación. Tres primitivas chicas sobre `auth`: `recordFailedLogin(identifier)`, `failedLoginCount(identifier, windowSeconds)`, `resetFailedLogins(identifier)` -- umbral y ventana los elige el propio `.link`, `identifier` es responsabilidad de quien llama (email, user id, IP). Estado en memoria sobre `SessionStore` (el mismo store de sesiones, sin módulo nuevo), podado por ventana en el momento de la consulta. Verificado contra un `linkc serve` real con un `login` de verdad (`Result<String, LoginError>`): un login válido no se ve afectado por fallos de OTRO identifier, el intento que supera el umbral da una variante de error DISTINTA (confirmando que corrió el camino de bloqueo, no el de credenciales inválidas de siempre), y un login exitoso resetea el conteo del MISMO identifier. Ver GRAMMAR.md §3.152.

**Hecho** (25/08/2026): **Log de auditoría de autorización estructurado** (originalmente el ítem 2 de esta lista) -- `check_auth_gate` (la única decisión de autorización del servidor) ahora produce, además de si la request pasa, un rastro de auditoría opcional: rol resuelto, `user_id` (si la sesión se creó con `createSessionWithId`) y si se permitió o no. `Some` SOLO cuando el rpc declaró `@authenticated`/`@requires` -- un rpc público no genera ruido. Tres campos nuevos en la línea de log de siempre (`auth_role`/`auth_user_id`/`auth_allowed`), como claves de PRIMER NIVEL en modo JSON (no enterradas en `extra`) para poder indexar/filtrar de verdad. Verificado contra un `linkc serve` real, leyendo su stdout: denegado (403) loguea el rol real y `allowed=false`; permitido (200) loguea rol+user_id+`allowed=true`; sin token (401) loguea rol `null`; un rpc público no lleva ninguno de los tres campos. Ver GRAMMAR.md §3.148.

**Hecho** (25/08/2026): **`--service-api-key-exempt <nombre1,nombre2,...>` bajo `serve-all`** -- landmine encontrado en el mismo barrido de "límites honestos" que el fix de colisión de tabla (§9.3), motivado por el mismo patrón del incidente de puertos de IgnisLove: de todos los flags globales de `serve-all` (`--jwt-secret`/`--cors-origin`/`--session-ttl`/etc.), `--service-api-key` es el único que es una capa de SEGURIDAD real, no solo conveniencia -- el más caro de tener atascado global sin excepción. Antes de este fix, un workspace con UN servicio que necesita quedar público (un healthcheck de terceros, un webhook entrante) obligaba a sacarlo de `serve-all` por completo. Ahora un nombre en la lista (validado contra los `.link` reales descubiertos, falla limpio antes de arrancar nada si no matchea ninguno) recibe `None` en su propio hilo, sin tocar el resto. Requiere `--service-api-key` configurado (error de CLI limpio si no). Verificado contra el binario real: un servicio exento responde sin el header mientras el otro sigue exigiéndolo. Ver GRAMMAR.md §3.93.

**Hecho** (24/08/2026): **API keys de servicio para llamadas servidor-a-servidor** (originalmente el ítem 3 de esta lista, priorizado de nuevo explícitamente junto con los ítems 1/2 de §9.7 en el mismo tercer reporte sobre IgnisLove): `--service-api-key <clave>`/`LINK_SERVICE_API_KEY` exige el header `X-Service-Api-Key` en toda request que no sea `/health`/`/`/`/status`, verificado en tiempo constante (reusando `constant_time_eq`, la misma función de `crypto.timingSafeEqual`) ANTES de leer el body -- cierra la mitad interna del hueco que `--host 127.0.0.1` (v1.46.0) dejaba abierta: cualquier OTRO proceso corriendo en la misma máquina, no solo uno externo, podía llamar a cada `linkc serve` igual que el gateway legítimo. Capa DISTINTA y ANTERIOR a `@requires`/JWT/sesiones (que autentican al USUARIO final, no a la conexión) -- las dos conviven, no se reemplazan. Funciona igual bajo `linkc serve-all` (v1.56.0), como un valor GLOBAL compartido por todos los servicios de la corrida. Ver v1.57.0, GRAMMAR.md §3.93.

**Hecho** (27/08/2026, decimosegunda ronda -- tercera auditoría adversarial, 5 agentes `Explore` read-only en paralelo, uno por capa): 16 hallazgos documentados en `AUDIT-2026-08-27.md` (reporte completo) y priorizados en `AUDIT-FIX-PLAN-2026-08-27.md` (plan de arreglos, 4 rondas por severidad). Los 2 de severidad crítica se cierran en esta ronda: **`crypto.randomToken(length)`** con `length` negativo o gigante mataba el proceso `linkc serve` ENTERO (`std::process::abort()` real, no un panic catcheable, con un valor lo bastante grande) -- ahora `length` se valida contra `1..=1024` antes de tocar memoria. **`@cache` + `@authenticated`/`@requires`** filtraba datos de un usuario autenticado hacia otro (la clave de caché nunca incluyó la sesión del caller) -- reproducido en vivo (Bob recibiendo el perfil completo de Alice con su propio token válido), ahora rechazado en compilación hasta que exista un diseño real de scoping por sesión. Los 14 hallazgos restantes (alta/media/baja severidad) quedan en el plan para rondas siguientes. Ver GRAMMAR.md §3.165.

**Hecho** (24/08/2026): **Revocar todas las sesiones de un usuario** (originalmente el ítem 2 de esta lista): hasta esta ronda solo existía `destroySession()`, que opera sobre la sesión que ya autenticó la request ACTUAL (deliberadamente sin tomar un token como argumento, para que nadie pueda revocar la sesión de otro adivinando su token) -- no había forma de cerrar TODAS las sesiones de un usuario dado a la vez (útil tras un cambio de contraseña, o para un admin que banea a alguien). `auth.destroyAllSessions(userId: Int) -> Int` es el nuevo builtin -- a diferencia de `destroySession`, SÍ toma un identificador (mismo criterio que `createSessionWithId`: un `user_id` es una clave de aplicación, no un secreto adivinable como un token). Devuelve cuántas sesiones se borraron. Quién puede LLAMARLO es responsabilidad de quien escribe el `.link` (típicamente `@requires(Role.Admin)`) -- el método en sí no impone ninguna política. Ver v1.49.0, GRAMMAR.md §3.84. **Lint: comparación `==` sobre un campo `secret`/`token`/`password`** (originalmente el ítem 6 de esta lista): `==`/`!=` donde cualquiera de los dos lados es un `Ident`/campo cuyo nombre sugiere un secreto (substring laxo, sin distinguir mayúsculas) recomienda `crypto.timingSafeEqual` (§3.54) -- comparar contra `null` (chequeo de presencia, no de valor) queda afuera a propósito. Recorre todo el cuerpo, cualquier nivel de anidamiento. Ver v1.53.0, GRAMMAR.md §3.88. **`@rate_limit` con `X-Forwarded-For` de confianza** (originalmente el ítem 1 de esta lista, también la mitad "proxy de confianza" de §8.3.2): `remote_addr()` (la conexión TCP real) sigue siendo el default -- detrás de un proxy/balanceador de verdad (confirmado como bloqueo real, todo corre detrás de nginx en la adopción de IgnisLove) eso es siempre la IP del proxy, compartiendo el límite entre TODOS los usuarios reales a la vez. `--trust-proxy`/`LINK_TRUST_PROXY` (apagado por default, mismo criterio que `--adopt-existing`) usa el PRIMER valor de `X-Forwarded-For` en su lugar -- opt-in explícito: prenderlo sin un proxy de confianza real delante deja evadir el límite por completo. v0 sin validar cuántos proxies hay en el medio ni de qué IP vienen (sin CIDR/N-hops configurable). Ver v1.54.0, GRAMMAR.md §3.89. **Lint `hardcoded-secret-literal`** (originalmente el ítem 3 de esta lista): `const NOMBRE: String = "literal"` de nivel superior se marca si el valor tiene forma de URL de conexión con credenciales embebidas, o si el NOMBRE sugiere un secreto (mismo heurístico laxo que el lint de comparación insegura, arriba) con un valor no vacío. El mensaje recomienda `env.get("...")` -- pero como reemplazo del `const` mismo (no lo declares como const, leelo con env.get en el momento que lo necesites), nunca como el VALOR de un const: un `const` en c-script solo admite literales, `env.get(...)` ahí es un error de compilación aparte. Puramente informativo, como el resto del linter. Ver v1.62.0, GRAMMAR.md §3.98.

### 9.6 Almacenamiento y Comunicaciones

**Hecho** (25/08/2026): **`smtp.sendMessage`: cc/bcc y adjuntos reales** (originalmente el único ítem de esta lista -- `sendToMany`/`sendHtml` ya resueltos, GRAMMAR.md §3.63 -- reforzado por "CRM"/Nexus, primer adoptador que ABANDONÓ el módulo `smtp` por completo: `mailer.link` ni siquiera llamaba a `smtp.*`, el envío real ocurría en 314 líneas de TypeScript con `nodemailer`, adjuntos incluidos). `smtp.sendMessage({ to, cc?, bcc?, subject, body, html?, attachments? })` -- variante "kitchen sink" aparte de las tres simples (`send`/`sendToMany`/`sendHtml`, sin cambios). `attachments` viaja como `contentBase64` (c-script no tiene tipo de bytes), decodificado del lado del runtime directo a `Vec<u8>` -- sin pasar por `base64.decode`, que exige UTF-8 válido en el resultado. cc/bcc llegan al sobre SMTP real (`RCPT TO:`); `cc` además aparece en el header `Cc:`, `bcc` nunca aparece en ningún header. Adjuntos como partes MIME reales (`multipart/mixed`), no texto concatenado. Verificado contra un servidor SMTP de mentira REAL hablando el protocolo completo (EHLO/MAIL FROM/RCPT TO/DATA): cc/bcc como destinatarios del sobre + `Cc:` visible/`Bcc:` invisible en el header, un adjunto real llega como parte MIME con su contenido decodificado byte a byte, base64 inválido falla limpio sin panic. Ver GRAMMAR.md §3.141.

**Hecho** (24/08/2026): **`crypto.awsS3PresignedUrl(accessKeyId, secretAccessKey, region, bucket, objectKey, expiresSeconds) -> String`** -- gap NUEVO, reportado por MyFinance: `DocumentStorageService` tenía una firma S3 FALSA (`?signature=hmac_verified`, un literal) porque `crypto.hmacSha256` (String -> String, §3.38) no alcanza para el encadenado de HMACs con bytes CRUDOS que AWS Signature V4 exige -- confirmado como limitación real del primitivo existente, no negligencia del adoptador ni falla de discoverabilidad de la documentación (una recomendación inicial de "armalo a mano con hmacSha256" resultó ser incorrecta al verificarla, corregida antes de comunicarla como solución). Función completa (arma la URL entera, no solo la firma) para evitar que cada adoptador tenga que reimplementar el canonical-request/URI-encoding exacto de AWS -- el mismo trabajo fino que llevó al placeholder en primer lugar. Alcance acotado a esta ronda: solo `GET` (compartir/descargar, el caso real reportado), sin `PUT`/subida; solo credenciales de larga duración, sin `X-Amz-Security-Token`; estilo virtual-hosted-style siempre. Verificado SIN necesitar una cuenta de AWS real -- contra el vector de prueba oficial que Amazon publica (`aws4_testsuite`, mismo estándar que ya se usó para `hmacSha256` en §3.38), reproduciendo la firma byte a byte. El resto del módulo `storage`/S3 (I/O real: subir/bajar/listar) sigue bloqueado esperando credenciales de prueba reales (ver arriba). Ver v1.73.0, GRAMMAR.md §3.110.

### 9.7 CLI y Experiencia de Desarrollador
1. **Unix domain sockets** (`--socket /run/app.sock`) como alternativa a TCP. **Nota (28/08/2026): requiere `std::os::unix::net`, sin equivalente en el target Windows de esta máquina de desarrollo -- implementable detrás de `#[cfg(unix)]`, pero sin forma de verificarlo en vivo acá (la matriz de CI sí tiene runners Linux/macOS reales); deliberadamente pospuesto hasta poder verificarlo de verdad, no solo compilarlo a ciegas.**
2. **Suite de administración de datos**: `linkc db inspect`/`db shell`/`export`/`import`/`seed` -- listar tablas y conteo de filas, REPL de solo lectura, exportar/importar entre entornos o motores, poblar una base nueva desde un fichero. **Hecho (28-30/08/2026): `linkc db inspect` (GRAMMAR.md §3.175), `linkc db export`/`db import` (GRAMMAR.md §3.185) y `linkc db shell` (GRAMMAR.md §3.189) -- `seed` no necesitó su propia pieza, importar contra un target vacío YA ES ese caso, mismo mecanismo. Suite completa, ítem cerrado.**

**Hecho PARCIAL** (28/08/2026, vigésimo primera ronda): **`linkc db inspect <archivo.link> [--db <url|archivo>]`** -- primera pieza de la suite de administración de datos (ítem 2 de esta lista); `shell`/`export`/`import`/`seed` quedan explícitamente para rondas futuras, esta ronda ataca solo la más chica y de mayor valor inmediato: un diagnóstico de SOLO LECTURA de qué colecciones existen físicamente y cuántas filas tienen, sin ejecutar ningún DDL. Mismo espíritu que `linkc doctor`/`linkc migrate --dry-run` -- de hecho reusa el mismo `resolve_db_source` que esos dos y `linkc serve`. Módulo nuevo (`src/inspect.rs`, detrás del feature `runtime`, mismo motivo que `migrate`/`introspect`) que reusa DOS funciones ya existentes en vez de duplicar introspección (`sqlite_table_exists`/`existing_columns`, ambas subidas de `fn` privada a `pub(crate)`, sin cambio de comportamiento). `exists: false` implica `row_count: None`, nunca `Some(0)` -- "no existe todavía" nunca se confunde con "existe pero está vacía". El conteo es FÍSICO, sin filtrar `@softDelete` -- mismo criterio que `db.tableStats()` (§9.7 más abajo, GRAMMAR.md §3.151), a propósito distinto de `count()`. SQLite abre con `SQLITE_OPEN_READ_ONLY` explícito; un archivo `.db` inexistente nunca es un error, es exactamente el caso "ninguna colección creada todavía". Verificado con 5 tests de CLI contra el binario real (`cli_db_inspect.rs`, incluida una base REAL poblada por un `linkc serve` real, no un archivo armado a mano) + 1 contra un Postgres real (`pg_integration.rs`). Suite completa sin regresiones (1301 tests). Ver GRAMMAR.md §3.175.

**Hecho** (30/08/2026): **`linkc db shell <archivo.link> [--db <url|archivo>]`** -- última pieza de la suite de administración de datos (ítem 2 de esta lista), cierra el ítem entero. REPL de SOLO LECTURA sobre stdin/stdout: una línea de entrada es una consulta SQL completa, ejecutada contra la base real. Mismo criterio de "loop bloqueante, sin async" que `Lsp::run_stdio`, framing mucho más simple (sin `Content-Length`, no hay protocolo que respetar). Pieza genuinamente nueva: un camino de "SQL arbitrario, filas de tipo dinámico" que no existía en ningún lado del proyecto -- SQLite vía `Row::get_ref`/`ValueRef`, Postgres vía dispatch manual por `Type` en `format_pg_cell`, reusando los decodificadores de rondas anteriores (`PgUuidText`/`PgDecimal`/`PgTimestampMicros`/`PgDateDays`/`PgJsonText`, subidos a `pub(crate)`). Solo lectura real, no una ilusión: SQLite abre `SQLITE_OPEN_READ_ONLY` (mismo criterio que `inspect`), Postgres corre `SET default_transaction_read_only = on` una vez tras conectar -- el SERVIDOR rechaza cualquier escritura de esa sesión, robusto contra cualquier truco de SQL que engañaría a un parser de palabras clave del lado del cliente. Verificado con 6 tests de CLI contra el binario real (`cli_db_shell.rs`: SELECT real, INSERT rechazado con el mensaje real de SQLite, SQL inválido no tumba el loop, `.exit`/línea vacía/EOF cierran limpio) + 2 contra Postgres real (`pg_integration.rs`: el rechazo de escritura es real del lado del servidor, columnas nativas `numeric`/`uuid`/`jsonb` salen legibles). Suite completa sin regresiones. Ver GRAMMAR.md §3.189.

**Nota** (28/08/2026): el ítem 1 de esta lista (**Unix domain sockets**) quedó deliberadamente sin atacar esta ronda -- necesita `std::os::unix::net`, sin equivalente en el target Windows de la máquina de desarrollo de esta sesión; implementable detrás de `#[cfg(unix)]`, pero sin forma de verificarlo EN VIVO acá (la disciplina de este proyecto exige correr el binario real, no solo que compile) más allá de lo que la matriz de CI (que sí tiene runners Linux/macOS reales) confirmaría después del hecho. Pospuesto hasta poder verificarlo de verdad.

**Hecho** (27/08/2026): **`@cron("Ns"/"Nm"/"Nh"/"Nd")`: tareas recurrentes nativas dentro de `linkc serve`** (originalmente el ítem 4 de esta lista, identificado y deliberadamente diferido en la ronda §8.4-8.5 por "menor demanda general", re-priorizado el 24/08/2026 por evidencia fuerte de Glowapp -- 10+ schedulers hand-rolled con `setInterval` más un `schedulerSupervisor.ts` completo). Atacado recién ahora porque necesitaba, sin saberlo hasta escribirlo, la infraestructura de hilos reales de la Etapa 1 de concurrencia (GRAMMAR.md §3.158, shippeada el día anterior) -- antes de esa ronda, correr una tarea de fondo real hubiera significado inventar concurrencia de un solo uso solo para esto. Diseño: una anotación (`@cron("5m")`) sobre un `rpc` normal, no una palabra reservada ni un bloque de nivel superior nuevo -- reusa la gramática de anotaciones existente. Tiene que ser la ÚNICA anotación del rpc (ninguna otra tiene efecto sobre algo que nunca recibe una request HTTP real), sin parámetros, retorno `Void` obligatorio, y NUNCA alcanzable vía HTTP -- ni en su path por defecto (`is_cron_member` bloquea con 404 antes de cualquier otro procesamiento), ni en ningún artefacto generado (`client.ts`/`openapi.json`/`llms.txt`/etc.). Un hilo de sistema operativo dedicado por tarea, spawneado una vez al arrancar `serve()`, reusando la misma infraestructura `Arc<Db>`/`Arc<Program>`/`Arc<SessionStore>` de §3.158 -- sin scheduling nuevo. Observabilidad: una línea de log por corrida (`log_cron_tick`) y dos contadores nuevos en `/metrics` (`linkc_cron_runs_total`/`linkc_cron_failures_total`, el segundo solo si de verdad hubo una falla). Límites honestos documentados, no escondidos: sin coordinación entre instancias (N réplicas corren la tarea N veces, mismo tipo de límite que `@rate_limit`), sin catch-up tras downtime, sin disparo manual, sin guard contra solapamiento si una corrida tarda más que su propio intervalo. Verificado con 9 tests de checker + 2 de parseo + 2 de integración contra un `linkc serve` real (confirmando que corre sola Y que da 404 en su path por defecto) + 1 de `/metrics` real. Ver GRAMMAR.md §3.159.

**Hecho** (25/08/2026): **`linkc serve-all --port-registry <archivo.json>`: puerto estable por nombre de servicio** -- gap nuevo, encontrado por otra sesión de Claude ("skynet-d3") diagnosticando el incidente de colisión de puerto de IgnisLove directamente en el VPS de producción: confirmó el mecanismo EXACTO (con 17 servicios, `bot_defense` cayó en el puerto `8792`, el mismo que `myfinance` tenía hardcodeado) y de paso descartó que el consumo de recursos de `linkc` (12.8 MB RAM / 0% CPU en un solo proceso de 103 hilos) fuera el origen de ninguna suba de RAM observada -- el proceso Node de la app (912.9 MB) es el consumidor real, sin relación con la migración a Link. `--port-map-out` (§3.107) ya hacía legible la asignación pero era de solo escritura -- cada arranque la recalculaba entera por orden alfabético. Con `--port-registry`, el archivo se LEE primero si ya existe: cada nombre de servicio ya presente conserva su puerto de siempre sin importar qué otro `.link` se agregue/borre/renombre alrededor; un nombre nuevo recibe el siguiente puerto libre. Un servicio borrado deja su entrada intacta en el registro A PROPÓSITO -- su puerto nunca se reasigna solo a un servicio distinto, para no reproducir el mismo tipo de incidente al revés (un gateway externo con ese puerto viejo hardcodeado). Ver GRAMMAR.md §3.153.

**Hecho** (25/08/2026): **`db.vacuum()`/`db.tableStats()`: RPCs de administración** (originalmente el ítem 3 de esta lista) -- ajustado al espíritu del lenguaje: dos builtins nuevos SOBRE `db` directo, no un service `_admin` auto-inyectado, para que quien escribe el `.link` los exponga en su PROPIO service con `@requires(Role.Admin)` (gramática de autorización YA existente, sin ninguna anotación nueva). `db.vacuum() -> Void` (VACUUM real, verificado contra Postgres real que no choca con "no puede correr en una transacción"); `db.tableStats() -> Map<String, Int>` (filas físicas por colección, sin filtrar `@softDelete` a propósito -- distinto de `count()`). Bug real encontrado en la verificación manual antes de shippear: la intercepción original rompía una colección de VERDAD llamada "vacuum" (`db.vacuum.insert(...)`) -- arreglado acotando el atajo a solo cuando `db.vacuum()` es la llamada DIRECTA, mismo criterio que el checker ya usaba. Ver GRAMMAR.md §3.151.

**Hecho** (25/08/2026): **`linkc pm2-config <archivo> <puerto> -o ecosystem.json`** (originalmente el ítem 4 de esta lista) -- generador de configuración PM2, mismo criterio que `linkc docker`/`linkc systemd` (`pm2::generate_pm2_config`, `pm2.rs` con la misma forma que `docker.rs`/`systemd.rs`). A diferencia de esos dos (directorio de salida con nombre fijo), acá el CALLER elige el archivo completo con `-o` (default `./ecosystem.json`). Genera un `ecosystem.json` NATIVO de PM2 (`script`/`interpreter: "none"` para ejecutar el binario `linkc` directo, `args` como array) con `--restart-backoff 30s` DENTRO de `args` (el backoff exponencial de conexión sigue siendo responsabilidad de `linkc serve`, no de PM2) y `autorestart: true` del lado de PM2 (reinicio de proceso, complementario). Sin `LINK_DATABASE_URL` en el `env` generado -- a diferencia de la variable comentada de `linkc docker`/`linkc systemd`, JSON no tiene comentarios, así que un placeholder ahí sería un valor REAL, no una referencia inerte. Ver GRAMMAR.md §3.121.

**Hecho** (25/08/2026): **`linkc systemd <archivo> <puerto>`** (originalmente el ítem 4 de esta lista, antes de esta ronda) -- generador de unidad systemd, a la par de `linkc docker` que ya existe (mismo mecanismo: `systemd::generate_systemd_unit`, `docker.rs`/`systemd.rs` con la misma forma). A diferencia de `linkc docker` (puerto siempre `3000` en la plantilla), acá el puerto es un argumento REQUERIDO -- `linkc serve` no tiene default. Genera `<nombre>.service` con `ExecStart`, `WorkingDirectory`, `Restart=on-failure`+`RestartSec` (reinicio de proceso, complementario a `--restart-backoff` de `serve`/`serve-all`), la variable `LINK_DATABASE_URL` comentada como referencia, y hardening mínimo (`NoNewPrivileges`/`ProtectSystem=strict`/`ReadWritePaths`/`PrivateTmp`). Ver GRAMMAR.md §3.120.

**Hecho** (25/08/2026): **Lint `unused-var`: 14 falsos positivos dentro de closures y struct-literals arreglados** -- issue #11, reportado por IgnisLove con evidencia excepcional (3 repros mínimos aislando la causa exacta + tabla de 14 falsos positivos reales verificados a mano en 7 de sus 17 `.link`). Causa raíz: `expr_count_ident` (`lint.rs`, el contador de usos que `unused-var` consulta) tenía seis variantes de `Expr` sin manejar (`Closure`, `StructLit`, `Match`, `Index`, `TupleLit`, `TupleIndex`), cayendo todas al `_ => 0` genérico -- una variable usada SOLO adentro del `body` de una closure pasada a `.filter()`/`upsert`/`findWhere`, o SOLO como valor de un campo de un struct-literal de cola, se marcaba como no usada pese a estar bien. Confirmado desde que el check existe (v1.62.0), no una regresión puntual. Arreglado agregando los seis arms que faltaban -- sin cambiar nada del comportamiento ya correcto para el resto de los casos. Verificado con los 3 repros EXACTOS del issue reproducidos literalmente, más `Match` (mismo bug de fondo) y un test de no-regresión. Ver v1.78.0, GRAMMAR.md §3.115.

**Hecho** (24/08/2026): **`--host <dirección>`/`LINK_HOST`** (originalmente el ítem 4 de esta lista): `linkc serve` escuchaba SIEMPRE en `0.0.0.0` (todas las interfaces), sin ninguna alternativa -- gap de seguridad real, no solo de conveniencia, para un proceso que solo necesita aceptar conexiones locales. Mismo orden de precedencia que el resto de los flags de `serve` (`--host` primero, después `LINK_HOST`, después el default `"0.0.0.0"` de siempre). El valor se pasa tal cual a `tiny_http::Server::http`, sin resolución propia -- una dirección que no le pertenece a ninguna interfaz local hace fallar el bind al arrancar, nombrando la dirección exacta, nunca cae en silencio a `0.0.0.0`. Ver v1.46.0, GRAMMAR.md §3.81. **`linkc test <archivo> --filter <nombre>`** (originalmente el ítem 1 de esta lista): substring sobre el nombre del test, sensible a mayúsculas -- mismo criterio que `cargo test <substring>`. Solo aplica al test runner integrado (`test "..." { ... }`), nunca al testing de contrato (`linkc test archivo.link archivo.snap`) -- combinar los dos es un error de uso claro, no un `--filter` ignorado en silencio. Un filtro que no matchea ningún nombre corre cero tests sin fallar. Ver v1.47.0, GRAMMAR.md §3.82. **`linkc --version` por archivo generado** (originalmente el ítem 1 de esta lista): `linkc` no tenía NINGUNA forma de reportar su propia versión -- ni `--version`/`-v`/`version` estaban despachados, ni ningún archivo generado decía con qué versión se había generado. `linkc::VERSION` (`env!("CARGO_PKG_VERSION")`, tomada de `Cargo.toml` en tiempo de compilación) alimenta LAS DOS cosas a la vez, así que nunca pueden desincronizarse entre sí: `linkc --version` la imprime, y el header de `contract.d.ts`/`client.ts`/`hooks.ts`/`validators.ts`/`schemas.ts` queda estampado con ella. `openapi.json` (que no admite comentarios) lleva la misma info en `x-generated-by`, una extensión de vendor estándar -- deliberadamente NO en `info.version`, que es la versión del API documentada, un concepto distinto. Puramente informativo: nada compara la versión estampada en un `gen/` viejo contra el binario que lo sirve o reconstruye. Ver v1.48.0, GRAMMAR.md §3.83. **Límite configurable de tamaño máximo de body** (originalmente el ítem 3 de esta lista): `linkc serve` leía el body de CUALQUIER request entero a memoria sin ningún límite -- un vector real de agotamiento de memoria. `--max-body-bytes`/`LINK_MAX_BODY_BYTES` (default 10 MiB) corta la lectura con `Read::take(max_body_bytes + 1)` y responde `413 Payload Too Large` ANTES de que auth/rate-limit/parseo del JSON compitan por memoria con un body ya sabido demasiado grande -- no lo lee completo primero. Límite de proceso, no por rpc; no se drena el resto de un body rechazado (si el cliente reusa la conexión, el próximo intento da 400 y cierra, nunca un colgado ni una fuga). Ver v1.50.0, GRAMMAR.md §3.85. **`linkc doctor`** (originalmente el ítem 1 de esta lista, elegido al cierre de la ronda de reportes de IgnisLove/MyFinance por ser el ítem del backlog general de menor riesgo de regresión disponible -- lectura pura, ningún camino de código ya tocado esta sesión): diagnóstico de entorno antes de un despliegue -- versión de `linkc`, que el `.link` de entrada resuelva sus imports/parsee/tipe, permiso de escritura en su directorio, y conectividad de solo lectura (`SELECT 1`, nunca DDL) a la base configurada vía `--db`/`LINK_DATABASE_URL`. "PATH" del ítem original reinterpretado a propósito: un binario estático sin dependencias de sistema no tiene nada real que ganar inspeccionando la variable de entorno `PATH`, así que ese chequeo pasó a ser "el programa de entrada compila", la señal que de verdad importa antes de desplegar. Ver v1.64.0, GRAMMAR.md §3.100. **`linkc serve-all --port-map-out <archivo.json>`** (originalmente el ítem 6 de esta lista) -- gap NUEVO, mismo análisis de IgnisLove: `serve-all` (v1.56.0) asigna puerto por orden ALFABÉTICO de los `.link` descubiertos, pero nada externo puede leer ese mapeo salvo adivinándolo -- `server/cscript-gateway.ts` (gateway de producción real, 13 servicios) hardcodea a mano un mapa nombre→puerto con un comentario propio admitiendo el riesgo de desactualizarse. Escribe `{"nombre_archivo": puerto, ...}` a un JSON ANTES de arrancar cualquier servicio -- si falla, no arranca nada (mejor que un gateway leyendo un mapeo viejo). No cambia la regla de asignación, solo la hace legible desde afuera. Ver v1.70.0, GRAMMAR.md §3.107.

**Hecho** (24/08/2026): **Modo "workspace" (`linkc serve-all`) + `--restart-backoff`** (originalmente los ítems 5 y 6 de esta lista, priorizados de nuevo por un tercer reporte del usuario citando el incidente puntual -- 68 reinicios de `telemetry` en un arranque en frío -- y pidiendo resolverlo antes de seguir con el resto del backlog): `linkc serve-all <directorio> --port-base N` descubre cada `.link` de un directorio, los compila TODOS antes de arrancar cualquiera, y levanta uno por hilo del sistema operativo dentro de un ÚNICO proceso -- puerto `N`+posición alfabética, SQLite propio por servicio preservado (`--db`/`LINK_DATABASE_URL` compartido rechazado de entrada, mismo motivo que §9.3.4 más abajo). Auditando `runtime::server::serve` para esto apareció que un fallo de conexión a Postgres usaba `std::process::exit(1)` -- dentro de UN proceso por servicio (como hoy) eso solo mataba a ese servicio, pero bajo `serve-all` se habría llevado puesto TODO el workspace por un solo servicio caído; `serve` ahora devuelve `Result<(), String>` en vez de terminar el proceso (`linkc serve` preserva el comportamiento externo de siempre). `--restart-backoff <duración>`/`LINK_RESTART_BACKOFF` (funciona en `linkc serve` y en `linkc serve-all`) agrega backoff exponencial NATIVO ante ese mismo fallo -- reemplaza la mitigación externa (`pm2 --restart-delay`, una espera fija) con una que dobla en cada fallo consecutivo (techo 30s, reseteada tras 60s estable). Reproducido el incidente real contra el binario (un puerto ocupado transitoriamente, backoff 1s/2s/4s, el otro servicio sano todo el tiempo, recuperación automática al liberarse el puerto) -- ver v1.56.0, GRAMMAR.md §3.92.

### 9.8 Observabilidad

**Hecho** (26/08/2026): **`linkc_rate_limit_rejections_total` en `/metrics`** -- quinto hallazgo del barrido de "límites honestos": `RateLimiter` es en memoria POR PROCESO (ya documentado en §3.39), así que correr N réplicas detrás de un balanceador diluye el límite real sin que nadie se entere hasta que un endpoint caro (email, cobro) queda de hecho sin protección real. No hay forma de arreglar la dilución sin estado compartido entre procesos (Redis o una tabla Postgres con incremento atómico -- una pieza mucho más grande, fuera de alcance sin evidencia real de demanda todavía), pero contar cada `429` real por rpc en `/metrics` (agregable entre réplicas con una consulta Prometheus normal) le da a quien opera la señal real para notar el problema antes de que se vuelva un incidente, en vez de después. Verificado contra el binario real: un rpc con `@rate_limit("1/1h")` golpeado tres veces confirma exactamente 2 rechazos en el contador, no un valor inventado. Ver GRAMMAR.md §3.39/§3.149.

**Hecho** (26/08/2026): **`linkc_notify_oversized_dropped_total` en `/metrics`** -- tercer hallazgo del barrido de "límites honestos" (después de la colisión de tabla y `--service-api-key-exempt`): un payload NOTIFY de más de 8000 bytes se descarta PARA SIEMPRE, y hasta esta ronda la única señal era un `eprintln!` invisible bajo `pm2`/`systemd` sin revisar logs -- una colección con filas grandes podía quedar desincronizada entre instancias durante meses antes de notarse, por datos divergentes, nunca por un error. Contador nuevo por colección en `Db`, expuesto en `/metrics` (solo si hubo al menos un drop, mismo criterio que la latencia de NOTIFY) -- en la instancia que ESCRIBE, no la que recibiría el cambio. Verificado contra Postgres real: un insert con un campo de 8200 caracteres confirma el contador en 1 sin afectar el insert local; un insert normal no lo incrementa. Ver GRAMMAR.md §3.150.

**Hecho** (25/08/2026): **Latencia de propagación NOTIFY + cola de reintento acotada** (originalmente el único ítem de esta lista -- con esto, PLAN.md §9.8 queda completamente resuelta). `sent_at_ms` viaja en el propio payload del `NOTIFY`; la instancia receptora lo resta de "ahora" al drenar el canal de cambios remotos y lo expone en `/metrics` como `linkc_notify_latency_seconds_sum`/`_count` (solo si se registró al menos un evento). Cola acotada (50 entradas, FIFO) SOLO para fallas TRANSITORIAS de conexión -- nunca para un payload de más de 8000 bytes, que jamás se arregla reintentando -- reintentada en el mismo loop de 200ms que ya drena cambios remotos, sin hilo/timer nuevo. Verificado contra DOS instancias `linkc serve` reales sobre el MISMO Postgres: una escritura real en B propagada a A vía LISTEN/NOTIFY real, `/metrics` de A confirma el conteo y la suma de latencia reales. Ver GRAMMAR.md §3.150.

**Hecho** (25/08/2026): **`GET /metrics` en formato Prometheus** (originalmente los ítems 1 y 2 de esta lista, cerrados JUNTOS -- resultaron ser la misma pieza) -- `linkc_http_requests_total`/`linkc_http_request_duration_seconds_sum` por rpc (conteo+duración, sin declarar buckets de histograma), `linkc_stream_subscribers` por colección (reusa `Db::subscriber_counts()`, ya existente para el push real de stream -- sin ningún contador cruzando hilos), `linkc_db_size_bytes` (PRAGMA en SQLite, `pg_database_size` en Postgres). `/metrics` NO exento de `--service-api-key` a diferencia de `/health` -- más sensible. Alcance v0: solo el dispatch normal de un rpc suma al conteo/duración -- un hit de `@idempotent`/`@cache` y un `stream` no. Verificado contra un `linkc serve` real: conteo/duración reales tras llamadas reales, tamaño de una base SQLite real > 0, DOS conexiones de stream reales abiertas confirman el conteo exacto, `/metrics` rechazado sin la api-key configurada cuando `/health` sigue exento; más un test contra Postgres real confirmando `pg_database_size`. Ver GRAMMAR.md §3.149.

**Hecho** (25/08/2026): **Logging estructurado en JSON (`--log-format json`) + nivel de log configurable (`--log-level warn|info|debug|error`)** (originalmente el ítem 1 de esta lista) -- `linkc serve` ya dejaba una línea `clave=valor` por request completada (`log_done`, `runtime/server.rs`); faltaba una forma de indexarla como JSON sin parsear texto libre, y una forma de bajar el volumen en producción con tráfico real. `--log-format text|json`/`LINK_LOG_FORMAT` (default `text`, sin cambios); `--log-level debug|info|warn|error`/`LINK_LOG_LEVEL` (default `info`, EXACTAMENTE el comportamiento de siempre -- las dos líneas por request se siguen imprimiendo SIEMPRE). Clasificación automática por `status` (5xx=`Error`, 4xx=`Warn`, resto=`Info`), no una anotación manual por call-site. `LogConfig` (`Copy`) cruza a los hilos de `stream` igual que `max_body_bytes` ya cruzaba. Alcance: solo las líneas POR REQUEST -- la línea de arranque y un error de `accept()` siguen como `println!`/`eprintln!` planos, no son la fuente de volumen que este ítem ataca. Ver GRAMMAR.md §3.122.

**Hecho** (24/08/2026): **Health check real** (originalmente el ítem 3 de esta lista): `/health` (`/`/`/status`, mismo handler) devolvía `200` FIJO sin tocar la base para nada -- inútil para cualquier orquestador que lo usa para decidir si reiniciar el proceso. `Db::health_check()` ejecuta un `SELECT 1` real en CADA request a `/health`, sin caché -- `200`/`"status":"ok"` si respondió, `503`/`"status":"error"` si no, con el mensaje real en `"database"`. Del lado Postgres pasa por el MISMO `with_reconnect` (§3.40) que cualquier otra query -- una caída transitoria se autorepara ahí mismo. Alcance de esta ronda: solo la base, no "servicios externos declarados" -- c-script no tiene hoy ningún concepto declarativo de dependencias externas, así que esa mitad del ítem original queda pendiente de esa pieza previa. Ver v1.52.0, GRAMMAR.md §3.87.

### 9.9 SEO y descubribilidad para IA

**Origen** (24/08/2026): pedido explícito del usuario -- "necesito que este lenguaje sea mucho mejor que cualquier otro lenguaje en el área de SEO y buscadores de IA". c-script ya tenía base para esto ANTES de esta sección: `@route` con URLs amigables (§3.37/§3.42/§3.57/§3.62), `@content_type` para servir HTML/XML sin las comillas de JSON (§3.35, con un ejemplo de `sitemap.xml` en la propia documentación), `escapeHtml()` (§3.45), `response.setStatus` para una 404 propia (§3.46), y docstrings `///` propagados a `openapi.json` (§3.72). Lo que sigue es TODO lo que falta, ordenado por las tres dimensiones que el usuario confirmó (SEO clásico de páginas, descubribilidad para agentes de IA, reglas para crawlers de entrenamiento/búsqueda de IA) -- alcance deliberadamente atado a lo que c-script YA es (compilador backend/RPC con `@route`+`@content_type` como mecanismo de servir páginas), no un framework de renderizado nuevo.

**Hecho** (25/08/2026): **`sitemapXml`/`robotsTxt` declarativos** (originalmente el ítem 1 de esta lista) -- `sitemapXml(urls: {loc: String, lastmod: Timestamp?}[]) -> String` y `robotsTxt(rules: {userAgent: String, disallow: String[]?, allow: String[]?}[], sitemapUrl: String?) -> String` como builtins sin receptor, siguiendo el mismo patrón de 5 puntos de enganche que `dateFromParts` (tipo estructural anónimo en `checker.rs`, dispatch en los 3 puntos de `runtime/mod.rs`). Ambos aceptan cualquier `type` nominal con la forma correcta por subtipado estructural (mismo mecanismo que `http.getWithHeaders`). Decisión de alcance explícita: el ítem 3 original de esta lista (preset de user-agents de IA conocidos -- `GPTBot`, `ClaudeBot`, `PerplexityBot`, `Google-Extended`) quedó deliberadamente NO implementado como preset hardcodeado -- una lista de crawlers se desactualiza en cuanto aparece uno nuevo, obligando a un release del compilador para corregirla, mientras que `robotsTxt` genérico ya permite declarar cualquier `userAgent` como una regla más, sin conocimiento especial del lenguaje. Ver GRAMMAR.md §3.116.

**Hecho** (25/08/2026): **Metadata SEO clásica como helpers de `String`** (originalmente el ítem 1 de esta lista) -- `metaTags(tags: {name: String, content: String}[]) -> String`, `openGraphTags(tags: {property: String, content: String}[]) -> String`, `canonicalLink(url: String) -> String` y `jsonLd(data: Dynamic) -> String`, cuatro builtins sin receptor más, mismo patrón de 5 puntos de enganche que `sitemapXml`/`robotsTxt`. `jsonLd` incluye una mitigación de XSS (escape Unicode de cada `<` del JSON serializado, técnica recomendada por OWASP) contra un valor de usuario que contenga literalmente `</script>` y cierre el bloque antes de tiempo. Ver GRAMMAR.md §3.117.

**Hecho** (25/08/2026): **`llms.txt` auto-generado por proyecto** (originalmente el ítem 1 de esta lista) -- convención [llmstxt.org](https://llmstxt.org/), no confundir con el `llms.txt` de ESTE repo que documenta el compilador. `linkc build` ahora emite un `llms.txt` junto a `contract.d.ts`/`client.ts`/`validators.ts`/`hooks.ts`/`schemas.ts`/`openapi.json`, listando servicios/rpcs (uno por bullet, con su firma completa) con sus docstrings `///` (§3.72) ya capturados como nota -- reusa infraestructura existente (mismo dato que `openapi_emit` ya usa como `description`), sin gramática nueva. Un rpc sin docstring sigue apareciendo (solo sin nota); un docstring de más de una línea aporta solo la primera. Ver GRAMMAR.md §3.118.

**Hecho** (25/08/2026): **`@example(request: ..., response: ...)`: ejemplos tipados en `openapi.json`** (originalmente el último ítem de esta lista) -- a diferencia de los ocho ítems anteriores de esta sección, este SÍ necesitó gramática nueva: una anotación cuyos valores son EXPRESIONES de c-script (reusa `parse_expr`, acepta un `StructLit`/`ArrayLit` completo), no `String` crudo. Las dos mitades se TIPAN contra la forma real del rpc (`request` contra sus parámetros, `response` contra `return_type`, mismo mecanismo que `= default` de un campo/param) -- un ejemplo desincronizado del contrato es un error de compilación, no un dato que puede mentir en silencio. Restringido a expresiones literales (rechaza `crypto.uuid()`/`now()`/cualquier llamada) para que `openapi.json` no cambie en cada build sin que el `.link` cambie, lo que rompería `linkc build --diff` (§3.79) en silencio. Propagado como `"example"` en el Media Type Object de `requestBody`/`responses` -- sin cambios en `contract.d.ts`/`client.ts`/`schemas.ts`, alcance atado exactamente a lo que este ítem pedía. Ver GRAMMAR.md §3.119.

**Con esto, PLAN.md §9.9 (SEO y descubribilidad para IA) queda completamente resuelta** -- los nueve ítems originales (contando el preset de crawlers de IA, subsumido por `robotsTxt` genérico en vez de implementarse aparte) están todos en producción: `@route`/`@content_type` (base previa a la sección), `sitemapXml`/`robotsTxt`, metadata SEO clásica (`metaTags`/`openGraphTags`/`canonicalLink`/`jsonLd`), `llms.txt` auto-generado, y ejemplos tipados en `openapi.json`.

**Hecho** (25/08/2026): **`llms-full.txt`: la mitad expandida de la convención llmstxt.org** -- auditoría pedida explícitamente por el usuario con terminología más amplia que la de esta sección ("SEO, meta datos, AEO, GEO, AIO, LLMO y todo lo relacionado"), antes de volver al backlog general. Confirmado: los nueve ítems de arriba siguen resueltos, y AEO/GEO/AIO/LLMO son en sustancia la misma dimensión "descubribilidad para agentes de IA" con nombres más nuevos -- no pidieron ninguna pieza técnica que no estuviera ya cubierta. La única brecha real: `llms.txt` (arriba) implementa solo la mitad "índice" del spec de [llmstxt.org](https://llmstxt.org/); el spec define un `llms-full.txt` hermano con el contenido completo (docstring entero, no solo su primera línea, más el `@example` si el rpc lo declaró) para que un agente no tenga que invocar el rpc solo para ver el detalle. `linkc build` ahora escribe los dos archivos siempre, sin flag nuevo. Ver GRAMMAR.md §3.139.

**Hecho** (25/08/2026): **`@cache_control("...")` por rpc** (originalmente el ítem 6 de esta lista) -- header `Cache-Control` declarativo, dimensión ORTOGONAL (se combina con `@route`/`@content_type`/auth/`@rate_limit`, mismo criterio que `@rate_limit`). Solo en el camino de ÉXITO -- una respuesta de error nunca hereda la política de caché del éxito, mismo criterio que `@content_type`/`response.redirect`. Rechazado sobre un `stream` (error de compilación, mismo motivo que `setStatus`/`redirect`: una conexión SSE nunca es cacheable de forma sensata). Mecanismo estático (viene del AST vía `Annotation::CacheControl`, no de un override en runtime como `redirect`) -- mismo patrón que `ContentType`/`RateLimit`/`Deprecated`. Verificado de punta a punta contra un servidor `linkc serve` real: el header aparece exacto en éxito, falta por completo en un error forzado del MISMO rpc anotado, y el caso combinado real (`@route`+`@content_type`+`@cache_control` sobre un sitemap) da los tres a la vez. Ver v1.76.0, GRAMMAR.md §3.113.

**Hecho** (24/08/2026): **`response.redirect(url: String, permanent: Bool) -> Void`** (originalmente el ítem 6 de esta lista) -- azúcar sobre `response.setStatus` (§3.46) + un header `Location` nuevo, que antes no existía ninguna forma de fijar. Fija 301 (`permanent`) o 302 (si no) Y el header `Location` a la vez -- mismo mecanismo interno que `setStatus` (`Db::response_status_override`), con un campo hermano nuevo (`response_location_override`) para la URL destino; los dos viajan siempre juntos. `url` vacío o con un salto de línea (`\r`/`\n`, inyección de headers HTTP -- `url` es un `String` arbitrario que el propio rpc arma, a diferencia de un header YA parseado de una request entrante) es un error de runtime limpio. Mismo límite que `setStatus` dentro de un `stream` (§3.56): error de COMPILACIÓN, no un no-op silencioso -- una conexión SSE ya fijó su status antes de que el cuerpo corra. Verificado de punta a punta contra un servidor `linkc serve` real (status Y header `Location` leídos del socket crudo, no solo un body que los mencione), no solo por tipos. Ver v1.74.0, GRAMMAR.md §3.111.

### 9.10 Integración de terceros -- reducir fricción con proveedores

**Origen** (25/08/2026): pedido explícito del usuario tras el incidente de la firma S3 falsa de MyFinance -- "aparte de AWS, debemos dar soporte a todos los demás [proveedores] igual, debemos dar soporte a la mayor cantidad de proveedores posibles para que no haya fricción". Auditoría completa en GRAMMAR.md §3.112: el problema real casi nunca es que el LENGUAJE no pueda hacer algo -- es que una capacidad ya existente (como `base64.encode`/`decode`, encontrado en esta misma auditoría, ya en el compilador desde antes pero sin documentar ni probar) es invisible para quien la necesita, humano o agente de IA, así que termina fabricando un placeholder en vez de encontrarla.

**Hecho** (25/08/2026): **`base64.encode(data: String) -> String` / `base64.decode(base64Str: String) -> String` documentados y probados** -- ya existían en el checker y el runtime (RFC 4648 estándar, crate `base64`) sin un solo test ni una sola línea de documentación en GRAMMAR.md/README/`llms.txt`. Esto solo ya destraba cualquier proveedor con HTTP Basic Auth (Twilio, el caso real que motivó la auditoría) combinado con `http.postWithHeaders` -- sin escribir código nuevo del compilador. Verificado contra vectores conocidos (`"hello"` <-> `"aGVsbG8="`, confirmados con el `base64` del sistema) más los casos de error (base64 mal formado, bytes decodificados que no son UTF-8 -- `base64.decode` devuelve `String`, nunca bytes crudos corruptos). Ver v1.75.0, GRAMMAR.md §3.112.

**Clasificación de proveedores auditados** (GRAMMAR.md §3.112, detalle completo ahí):
1. **Stripe, SendGrid, y cualquier API con `Authorization: Bearer <token>`**: YA funcionan hoy, completas -- `http.postWithHeaders`/`http.getWithHeaders` (§3.47/§3.60) + `crypto.hmacSha256`/`env.get`/`request.rawBody()` para verificar el webhook de vuelta (§3.38, que ya citaba a Stripe). Nada que construir -- el gap era pura discoverabilidad, ya cerrado documentándolo.
2. **Twilio, y cualquier API con HTTP Basic Auth**: YA funciona hoy completa gracias al ítem "Hecho" de arriba.
3. **Azure Blob SAS (URLs firmadas)**: gap REAL, mismo tipo estructural que AWS S3 (§3.110) pero más chico en teoría -- un solo HMAC-SHA256 (no encadenado), pero la clave de cuenta viaja en BASE64 (hay que decodificarla a bytes crudos) y la firma resultante también es BASE64 (no hex como `crypto.hmacSha256`). Ninguna de las dos cosas es posible con los primitivos de `String` actuales. **Auditado en profundidad (25/08/2026), NO implementado esta ronda**: a diferencia de AWS (que publica `aws4_testsuite`, un vector de prueba OFICIAL completo -- clave, string-to-sign, firma esperada, todo reproducible), la documentación de Microsoft (`service-sas-examples`) da string-to-sign y firma esperada pero NUNCA la clave de cuenta que los relaciona -- así que el resultado publicado no se puede reproducir de forma independiente, solo "confiar" en que el doc no tiene un error de transcripción. Implementar sin esa verificación rompería el estándar que este proyecto sostuvo con AWS -- queda pendiente de encontrar un vector completo (ej. en los tests de algún SDK oficial de Azure) antes de escribir código.
4. **SQS**: mismo AWS Signature V4 que S3 (§3.110), pero como llamadas de API firmadas con headers, no URLs presignadas -- `awsS3PresignedUrl` no cubre este caso tal cual; haría falta generalizar la derivación de firma a un primitivo separado de "armar una URL de S3". Sin evidencia real de demanda todavía.
5. **Google Cloud Storage (URLs firmadas V4)**: gap más grande -- RSA-SHA256 sobre la clave privada de una cuenta de servicio (PEM/PKCS8), no HMAC. Necesitaría sumar una crate de firma RSA (excepción nueva a "cero dependencias", como `regex`) -- decisión de alcance propia, sigue en §9.12 (Bloqueado) hasta que haya evidencia real o el usuario decida priorizarlo.
6. **RabbitMQ**: protocolo AMQP binario y con estado -- categoría totalmente distinta a firmar requests HTTP, necesitaría el protocolo de transporte entero. Sigue en §9.12 (Bloqueado).

**Hecho** (25/08/2026): **Flujo OAuth2 "client credentials" (servidor a servidor)** -- gap NUEVO auditado, CERO código nuevo del compilador necesario: Google APIs/Microsoft Graph/Salesforce/HubSpot y muchas otras APIs empresariales usan este flujo (distinto de OAuth2 "authorization code" con login de usuario, que sigue bloqueado en §9.12 -- necesita un proveedor de identidad real). Las tres piezas ya existían: `http.postWithHeaders` para pedir el token, `json.parse(text) -> Dynamic` + acceso de campo sobre `Dynamic` (tipa devolviendo `Dynamic`, asignable donde se espera `String` sin cast explícito) para extraer `access_token` de la respuesta sin declarar su forma completa, y `http.getWithHeaders` con `Authorization: Bearer <token>` para la llamada real. Verificado de punta a punta contra DOS servidores HTTP de mentira reales (uno de token, uno de API protegida) -- confirma que el token que el primero devuelve llega EXACTO como header al segundo, no solo que las dos llamadas tipan. Ver v1.77.0, GRAMMAR.md §3.114.

### 9.11 Diferido -- ya trackeado en §8, prioridad menor que 9.1–9.10
Por pedido explícito, lo que ya estaba en la hoja de ruta antes de estos dos reportes pasa a continuación de todo lo de arriba, no porque sea menos importante en términos absolutos, sino porque los dos reportes de adopción real no lo señalan como más urgente que lo nuevo: **§8.1.2** (playground multi-archivo), ~~**§8.2.1** (agregación con truncamiento de fecha)~~ -- **hecho, GRAMMAR.md §3.157; listado acá por error**, ~~**§8.2.2** (`db.<c>.filter` pushdown)~~ -- **hecho, GRAMMAR.md §3.95/§3.108/§3.109/§3.170/§3.171; listado acá por error**, ~~**§8.2.3** (transacciones)~~ -- **hecho, ver §8.2 arriba y GRAMMAR.md §3.154; listado acá por error, nunca se sacó de esta lista al cerrarse**, **§8.3.1** (limpieza proactiva de sesiones), **§8.3.2** (rate limiting distribuido -- la mitad de estado compartido/Redis; la mitad de `X-Forwarded-For` se adelantó a §9.5.1 por ser más simple y más citada), **§8.3.3** (smtp asíncrono), **§8.4.1** (carga completa de `User` en sesión), **§8.5.1** (módulo `storage`/S3 -- descarga Y subida ya resueltas, GRAMMAR.md §3.110/§3.194; sigue abierto solo límite de tamaño y multipart, ver §8.5 arriba).

**Corrección de tres referencias cruzadas obsoletas (30/08/2026, barrido de higiene del backlog):** las tres arriba (§8.2.1, §8.2.2, §8.5.1) seguían citadas como abiertas en este párrafo pese a estar resueltas en otra parte del documento desde hace días -- mismo patrón que §8.2.3 (ya corregido antes). Ninguna implica trabajo nuevo, es puramente que nadie actualizó esta lista de reenvío al cerrar cada ítem en su sección de origen.

### 9.12 Bloqueado -- requiere algo externo o una decisión previa
No autónomo por diseño, no por pereza -- cada ítem nombra qué falta:
1. ~~**Publicación en npm** (`link-lang`)~~ -- **RESUELTO (30/08/2026), ver §8.1.1.** Cuenta creada, token generado, paquete publicado.
2. **Publicación en VS Code Marketplace / JetBrains / Zed**: cuentas de publisher del usuario en cada marketplace.
3. **Imagen Docker oficial publicada**: credenciales de un registro (Docker Hub o similar) del usuario.
4. **Paquetes `apt`/`brew`**: cuentas y claves de firma de cada gestor de paquetes.
5. ~~**GitHub Action oficial publicada** (`setup-linkc@v1`)~~ -- **CASI RESUELTO (30/08/2026).** Repo público nuevo `charlessonamericantrading/setup-linkc` (composite action, misma disciplina de verificación SHA-256 que el paquete npm), probado de punta a punta en CI real (Linux/macOS/Windows + la referencia PUBLICADA `@v1` con una versión fija, no solo `uses: ./`) -- ver el repo. Tag `v1`/`v1.0.0` y release ya existen y son usables YA vía `uses: charlessonamericantrading/setup-linkc@v1`, sin esperar el Marketplace. GitHub Marketplace Developer Agreement v2.4 aceptado (confirmado explícitamente por el usuario antes, es un compromiso legal a nombre de la empresa). Único bloqueo real que queda para el LISTADO en el Marketplace (búsqueda pública, no la usabilidad): **GitHub exige 2FA activado en la cuenta antes de publicar una Action** -- 100% del usuario, necesita su teléfono/app de autenticación.
6. **Integraciones que de verdad necesitan credenciales reales de un proveedor para verificar de punta a punta** -- auditado el 25/08/2026 (ver §9.9 y GRAMMAR.md §3.112), varias de las que estaban acá se sacaron por no ser gaps reales: **Google Cloud Storage (URLs firmadas V4, RSA-SHA256 sobre una clave de cuenta de servicio)** sigue bloqueado -- necesitaría sumar una crate de firma RSA (excepción nueva a "cero dependencias") y no hay forma de verificar la implementación sin al menos los vectores de prueba oficiales de Google o una cuenta real. Cualquier otro proveedor con auth Bearer/Basic estándar (Stripe, SendGrid, Twilio, etc.) YA funciona hoy con `http.postWithHeaders`/`crypto.hmacSha256`/`base64.encode` -- ver §9.9 para el detalle de la auditoría completa.
7. **OAuth2/OIDC nativo (login social)**: el protocolo se puede implementar, pero verificarlo de verdad necesita un proveedor de identidad real (Google/GitHub/etc.) con una app de prueba registrada por el usuario.
8. **Backend MySQL/MariaDB**: un backend nuevo a la par de SQLite/Postgres, toca `storage.rs`/`db.rs`/codegen de punta a punta -- necesita decisión explícita de alcance antes de empezar, no es una ronda autocontenida.
9. ~~**Intérprete multi-hilo**~~ -- **HECHO (26/08/2026)**: la decisión explícita que este ítem pedía se tomó y se ejecutó -- ver §9.2 ítem 2 y GRAMMAR.md §3.158. `linkc serve` corre cada request en su propio hilo real; el árbol de evaluación de UN programa sigue siendo secuencial dentro de ese hilo (eso no cambió ni hacía falta que cambiara -- el problema real era procesos bloqueándose entre sí, no paralelismo dentro de una sola request).
10. **Sharding, réplicas de solo lectura, base separada por tenant**: subsistemas distribuidos genuinamente grandes -- necesitan diseño aprobado antes de implementar.
11. **Compilación AOT a binario nativo** independiente del intérprete: proyecto grande de backend de codegen, más allá del WASM actual limitado a escalares -- necesita decisión de alcance.
12. **GraphQL como transporte alternativo**: compite arquitectónicamente con el diseño RPC-first del proyecto -- necesita decisión de si encaja con la tesis del proyecto en absoluto, no solo una implementación.
13. **Auto-scaling hints, rolling updates, rollback automático tras health check fallido**: presuponen una plataforma de orquestación (k8s/ECS) que el proyecto no apunta hoy -- o es una decisión de alcance para que `linkc serve` la conozca, o es puramente un tema de guía de despliegue (ver §9.1).
14. **Canal de soporte (Discord/Slack), programa de "adoptantes de referencia", benchmark comparativo publicado contra terceros**: acciones organizacionales/de comunidad, no trabajo de código -- las tiene que decidir y ejecutar el usuario, no algo que se implemente en el compilador.

**Ítems de los dos reportes ya resueltos antes de esta ronda** (no reimplementar, verificado contra el código real): adoptar tabla existente sin auto-migrar (§3.67, v1.28.0), tipo de PK flexible en Postgres -- SERIAL/IDENTITY 16/32 bits (§3.59, v1.20.0), `linkc introspect` (§3.66, v1.27.0), puente de sesión JWT externo (§3.64, v1.25.0), status/headers en `http.get`/`post` salientes (§3.60, v1.21.0), `smtp` a varios destinatarios y HTML (§3.63, v1.24.0), `Int64` como campo agregable/agrupable (§3.65, v1.26.0), paginación empujada a SQL + cursor (§3.48/§3.61), `@route` con múltiples parámetros/catch-all/query string (§3.37/§3.42/§3.57/§3.62), `response.setStatus`/`@content_type`/`escapeHtml` (§3.45/§3.46/§3.35), Argon2id configurable + detección de hash legado (§3.58), `crypto.randomInt`/`timingSafeEqual` (§3.54), CORS allowlist + cabeceras de seguridad fijas (§3.41), PostgreSQL runtime + TLS + auto-reconexión + LISTEN/NOTIFY cross-instancia (§3.36/§3.40/§3.44), `--session-ttl` (§3.50), `auth.currentRole`/`createSessionWithId`/`currentUserId` (§3.51/§3.53).

### 9.13 Calidad de los hooks de React generados

**Origen** (25/08/2026): pedido explícito del usuario -- "mejora las conexiones del backend con componentes [de React] y SEO", aclarado a "hooks de React generados (`hooks.ts`) para que se integren mejor con componentes reales -- loading/error states, invalidación de cache, tipos más ergonómicos". Auditando `codegen::ts_emit::emit_hooks` para esto apareció algo más urgente que ergonomía: los hooks de Query/Mutation NO tenían ninguna protección contra una respuesta VIEJA resolviendo después de una más nueva (`use{Servicio}{Rpc}Query` re-dispara la request en cada cambio de parámetros vía su `useEffect`, el caso real es un buscador llamando al hook por cada letra tipeada) -- sin guarda, la respuesta más LENTA de las dos podía pisar `data` con un resultado desactualizado, en SILENCIO, sin ningún error visible. El hook de `stream` ya se protegía de esto (`cancelled`, un booleano de cancelación en su `useEffect`); Query/Mutation no.

**Hecho** (25/08/2026): **Guarda de "solo la respuesta más reciente gana"** en los hooks de Query y Mutation -- un `requestIdRef` (`useRef`, contador monotónico) por instancia del hook: cada llamada a `refetch`/`mutate` se queda con su propio número al arrancar, y solo llama a `setData`/`setError`/`setLoading` si sigue siendo la request más reciente cuando la promesa resuelve. El `useEffect` del hook de Query además invalida en su cleanup cualquier request de ESE efecto que siga en vuelo al desmontar o antes de que las dependencias cambien (mismo criterio que el `cancelled` del hook de `stream`, adaptado a un contador porque acá conviven requests disparadas por el efecto Y por una llamada manual a `refetch`); `reset()` del hook de Mutation también invalida cualquier `mutate()` en vuelo, para que una respuesta tardía de ANTES del reset no pise el estado recién limpiado. **Gap adyacente encontrado y cerrado de paso**: `hooks.ts` no tenía NINGUNA cobertura de type-check automatizada -- el único frontend que corre en CI (`frontend/`, el "demo insignia") solo importa `client.ts`, nunca `hooks.ts`, y `examples/taskboard/frontend` (el único ejemplo real que sí usa los hooks, con React de verdad) no está conectado a ningún workflow. Verificado a mano regenerando `examples/taskboard/frontend/src/gen/` con el binario real y corriendo `npx tsc --noEmit` contra React 18 + TypeScript estricto -- pasó limpio (antes de esta ronda ni siquiera podía correr: al `package.json` de ese ejemplo le faltaba la dependencia `zod` que `schemas.ts` importa, agregada de paso). `.gitignore` tenía además dos entradas puntuales de `node_modules` (`/frontend/`, `/editors/vscode/`) que no cubrían `examples/taskboard/frontend/` -- generalizado a `**/node_modules/` para que cualquier ejemplo nuevo con su propio `package.json` quede cubierto sin acordarse de sumar una entrada. Ver GRAMMAR.md §3.123.

**Hecho** (25/08/2026): **Cache compartido entre instancias del hook de Query** -- pedido explícito de continuar ("avanza con el cache"). Antes de esta ronda, dos componentes llamando al MISMO `use{Servicio}{Rpc}Query` con los MISMOS parámetros disparaban dos fetches independientes sin relación entre sí -- el problema clásico que react-query/SWR resuelven con un cache global, acá resuelto DENTRO del propio `hooks.ts` generado, sin sumar ninguna librería nueva. Un `Map<string, QueryCacheEntry<T>>` a nivel de módulo (singleton por archivo cargado), clave por rpc+parámetros (`JSON.stringify`), con `useSyncExternalStore` (la API que React 18 documenta para suscribirse a un store externo) reemplazando los `useState` locales -- dos instancias con la misma clave comparten el mismo `entry`, se re-renderizan juntas cuando cualquiera actualiza el estado. Dedupe real vía `entry.promise`: si ya hay un fetch en vuelo para esa clave, un `refetch()` nuevo se une a la MISMA promesa en vez de disparar su propio `client.rpc(...)` -- dos componentes montándose juntos generan UNA sola request HTTP. El `requestIdRef` que el ítem anterior le había puesto al hook de Query queda SUPERADO (el cache resuelve el mismo problema de fondo por construcción); el de `Mutation` sigue igual, sin cambios -- una mutación no comparte cache. Alcance documentado: cache por rpc+parámetros, NO por instancia de `client`; SIN invalidación automática tras una `Mutation` relacionada (necesitaría una forma de declarar esa relación, que no existe hoy -- cada componente sigue llamando a `refetch()` a mano donde corresponda). Verificado con un script de Node standalone probando la lógica central del dedupe (sin React) más `examples/taskboard/frontend` regenerado y tipando limpio contra React 18 real. Ver GRAMMAR.md §3.124.

**Hecho** (25/08/2026): **Invalidación automática de cache tras una Mutation** -- pedido explícito de continuar ("si, sigue con eso") sobre el límite documentado en el ítem anterior. Nueva anotación `@invalidates(rpc1, rpc2, ...)` sobre un rpc de Mutation, nombrando rpcs de Query de la MISMA service cuyas entradas de cache se limpian (reset a `data: null` + notificación a los listeners suscriptos) tras un `mutate()` exitoso -- reusa el auto-fetch que el hook de Query ya tiene (§3.124) en vez de construir un segundo camino de fetch. Checker con 4 reglas de validación: el target tiene que existir en la misma service, no puede ser un `stream`, tiene que tener forma de Query (`RpcDecl::looks_like_a_query()`, heurístico extraído a un único método compartido entre checker y codegen para que nunca puedan divergir), y la anotación no puede repetirse. `examples/taskboard/backend/taskboard.link` demuestra el uso real: sus tres mutations (`create`/`update`/`remove`) invalidan `list`/`listByColumn`/`stats`. Verificado con 10 tests nuevos (parser/checker/codegen) + el camino feliz y los 5 caminos de error probados a mano contra el binario real + `examples/taskboard/frontend` regenerado (con `@invalidates` de verdad en sus mutations) y tipando limpio contra React 18 real. Ver GRAMMAR.md §3.125.

**Hecho** (25/08/2026): **`LinkTransportError` con `status: number` tipado** -- continuación del mismo pedido ("mejora... typescript, nest.js y react", aclarado a seguir profundizando TypeScript/React). Antes de esta ronda, el status HTTP de un fallo de transporte (401/404/500/...) solo viajaba interpolado dentro del mensaje (`` `HTTP ${res.status}` ``) -- un componente no podía distinguir un 401 de un 500 sin parsear ese string con una regex. Ahora `LinkTransportError` lleva `status: number` como propiedad real, poblada con el `res.status` genuino en los dos puntos donde `client.ts` la lanza. Sin cambios del lado de los hooks -- `QueryState.error`/`MutationState.error` siguen `Error | null`, el narrowing (`error instanceof LinkTransportError && error.status === 401`) es responsabilidad de quien consume el hook. Verificado con 2 tests nuevos en `codegen::ts_emit`. Ver GRAMMAR.md §3.126.

**Hecho** (25/08/2026): **`loading` vs `isFetching` en el hook de Query** -- tercera ronda seguida sobre el mismo pedido ("sigue"). Desde §3.124, `loading` era un único flag verdadero durante CUALQUIER fetch, inicial o de fondo -- un componente naive (`if (loading) return <Spinner/>`) ocultaba una lista con datos válidos cada vez que se refrescaba. `isFetching` (renombre del flag interno que `loading` ocupaba en `QueryCacheState<T>`) queda verdadero durante cualquier fetch; `loading` pasa a ser DERIVADO (`data === null && isFetching`, "no hay nada que mostrar todavía"), eliminando la posibilidad de que ambos queden desincronizados. Sin cambios en cuándo fetchea (dedupe, auto-fetch, `@invalidates`) -- puramente qué expone el hook. Mutation queda afuera a propósito: no tiene el concepto de "dato cacheado válido mientras se recarga". Verificado con 1 test nuevo + el test de cache compartido actualizado a la nueva forma del `return`, y `examples/taskboard/frontend` regenerado tipando limpio contra React 18 real. Ver GRAMMAR.md §3.127.

**Hecho** (25/08/2026): **`mutate` vs `mutateAsync`** -- cuarta ronda seguida sobre "sigue", esta vez encontrando el gap directamente en `examples/taskboard/frontend/src/App.tsx`: `handleCreate` hacía `await createTask(input)` (el `mutate` del hook) SIN try/catch -- el uso más natural. `mutate` SIEMPRE relanzaba, así que un fallo real producía una promesa rechazada sin manejar ("Uncaught (in promise)"), pese a que `error` ya quedaba seteado en el estado del hook. `mutateAsync` (nombre que react-query usa para lo mismo) es ahora la función que relanza; `mutate` es un wrapper que llama a `mutateAsync` y devuelve `null` en el fallo en vez de relanzar -- mismo patrón que `refetch()` de Query ya usaba. `MutationState<T>` no cambia; el cambio vive en la intersección de tipos que cada hook devuelve. `App.tsx` actualizado al patrón nuevo (chequea el `null` antes de limpiar el formulario). De paso, el mismo test encontró un gap real en `taskboard.link` (`getById(id) -> Task?`): `Promise<Task | null | null>` -- un `| null` agregado a mano sobre un retorno YA opcional, en cuatro lugares (Mutation `data`/`mutate`/`mutateAsync`, Query `refetch`, stream `latest`), unificado en una sola variable `nullable_ret_str`. Verificado con 2 tests nuevos + los 2 tests existentes de Mutation sin cambios (viven dentro de `mutateAsync`, que no cambió de comportamiento), y `examples/taskboard/frontend` regenerado tipando limpio contra React 18 real. Ver GRAMMAR.md §3.128.

**Hecho** (25/08/2026): **`options?: { signal?: AbortSignal }` en `client.ts`** -- quinta ronda seguida, esta vez fuera de `hooks.ts`. Hasta ahora ninguna request generada (`rpc` ni `stream`) tenía forma de cancelarse -- un componente que se desmonta o un buscador que abandona la letra anterior solo podía IGNORAR la respuesta, nunca cancelar el `fetch()` real. Nuevo último parámetro, siempre opcional, en cada método de la interfaz Y la implementación; `push_fetch_call` pasa `signal: options?.signal` al `fetch()` real. Alcance deliberado: solo `client.ts` -- integrar cancelación DENTRO de los hooks (ej. abortar automático al desmontar `useQuery`) queda afuera porque la entrada de cache de Query es COMPARTIDA entre instancias (§3.124) y abortar por una instancia no debería cancelar la request que otra sigue esperando; es un diseño aparte, no una extensión mecánica de este ítem. Verificado con 1 test nuevo + los tests existentes de firmas exactas actualizados, y a mano contra un `linkc serve` real (bundle de `client.ts` vía esbuild): abortar antes y con timeout rechaza con `AbortError` real, sin `options` sigue funcionando igual. Ver GRAMMAR.md §3.129.

**Hecho** (25/08/2026): **`reconnect()` manual en el hook de `stream`** -- sexta ronda seguida, auditando el único de los tres hooks sin ninguna forma de recuperarse de un fallo (`use...Query` tiene `refetch()`, `use...Mutation` tiene `reset()`, `use...{Stream}` no tenía nada). Una conexión SSE cortada (blip de red, servidor reiniciando) dejaba `isConnected: false`/`error` PARA SIEMPRE, sin más recurso que desmontar y remontar el componente entero (perdiendo `data`/`latest` acumulados). Un contador (`reconnectAttempt`) como dependencia del `useEffect` -- incrementarlo re-suscribe desde cero; `reconnect()` lo incrementa. `data`/`latest` NO se limpian al reconectar (seguir la conexión viva, no empezar de cero); `error` sí, como cualquier reintento. Manual, no automático con backoff -- mismo criterio que el resto de los hooks: quien consume decide cuándo reintentar. Demostrado en `examples/taskboard/frontend/src/App.tsx`: botón "Reconectar" visible cuando `!isConnected`. Verificado con 1 test nuevo + el test existente de generación de hooks sin cambios, y `examples/taskboard/frontend` regenerado tipando limpio contra React 18 real. Ver GRAMMAR.md §3.130.

**Hecho** (25/08/2026): **`isOk`/`isErr` y el schema Zod de `Result<T,E>` -- bug real, no una mejora**. Séptima ronda seguida, esta vez encontrando algo roto de verdad: `isOk`/`isErr` (client.ts) tipaban/chequeaban contra `{ ok: true|false, ... }`, un campo que NINGÚN `Result<T,E>` real tiene -- el wire usa `type: "Ok"|"Err"` desde siempre. Pasarle un `Result<T,E>` real (literalmente `await client.create(...)`) a `isOk`/`isErr` ni siquiera tipaba contra `tsc` real -- las dos funciones eran inutilizables tal cual estaban. Mismo bug en `zod_emit.rs`: el schema de `Result<T,E>` discriminaba por `"ok"`, rechazando cualquier `Result` real. **Alcance real, no sobre-vendido**: `validators.ts` -- la validación REAL de cada respuesta -- ya usaba `.type` correctamente desde siempre, nunca tuvo el bug; el impacto está acotado a dos exports auxiliares. `client.ts` ahora importa `Result` SIEMPRE (antes solo si algún rpc lo usaba, lo que podía dejar `isOk`/`isErr` referenciando un nombre nunca importado). Verificado con 3 tests nuevos + a mano con `tsc` real (antes/después del fix, confirmando el error de tipo real y su desaparición) + Zod real en runtime (el schema arreglado acepta un payload real y RECHAZA explícitamente la forma vieja). Ver GRAMMAR.md §3.131.

**Hecho** (25/08/2026): **Schema Zod de un enum ADT -- misma familia de bug, octava ronda seguida**. Auditando `zod_emit.rs` de punta a punta tras el fix anterior: `Item::Enum` generaba `z.enum([...])` (unión de strings) para CUALQUIER enum, incluidos los ADT con datos por variante -- `ValidationError` de `examples/users.link` es exactamente ese caso. El wire real es un objeto con tag `type`, nunca un string pelado; `z.enum([...])` aceptaba el string y rechazaba el objeto real, exactamente al revés de lo correcto. Mismo criterio `all_unit` que `emit_enum_decl` (ts_emit.rs) ya usa: sin datos en ninguna variante, sigue `z.enum([...])` sin cambios; con datos, ahora `z.discriminatedUnion("type", [z.object({ type: z.literal(...), ...campos }), ...])`, reusando `render_zod_type_for_field` para los campos. Regresión real atrapada de paso por `docs_examples.rs`: un ADT GENÉRICO (`enum Result<T, E> { Ok { value: T }, ... }`, ejemplo educativo de la documentación) rompía `linkc build` ENTERO porque el primer intento resolvía campos con `resolve_type` a secas, que rechaza `T` -- arreglado con `resolve_type_abstract` (mismo criterio que `resolve_field_ty` en ts_emit.rs), que deja `T` como `Type::TypeParam` y cae al `z.unknown()` catch-all ya existente en vez de fallar. El mismo bug de resolución (no de forma) apareció también en el branch hermano `Item::Type` (un `type Box<T> = { value: T }` genérico), mismo fix. Verificado con 4 tests nuevos + Zod real en runtime + `docs_examples.rs` (todos los bloques de la documentación, incluido el ADT genérico) vuelve a pasar. Ver GRAMMAR.md §3.132.

**Hecho** (25/08/2026): **`openapi.json` -- mismos tres bugs que `isOk`/`isErr` y el schema Zod, esta vez en la especificación pública de la API**. Continuación directa del mismo audit: `openapi_emit.rs` tenía EXACTAMENTE los mismos tres bugs. (1) `Type::ResultOf` describía el wire como `{ ok: boolean, ... }` -- arreglado a `oneOf`+`const` (equivalente JSON Schema del `discriminatedUnion` de Zod). (2) Un enum ADT se describía como `{"type":"string","enum":[...]}` -- arreglado con el mismo criterio `all_unit`, ahora `oneOf` con un objeto por variante. (3) Mismo bug de generics que el ítem anterior, en `Item::Type` de este archivo -- `Box<T>` rompía `openapi.json` específicamente, después de arreglar el mismo caso en `schemas.ts` -- mismo fix (`resolve_type_abstract`). Impacto real: `openapi.json` es lo que consume Swagger UI o un generador de SDK externo -- describir el shape equivocado ahí no es cosmético. Verificado con 4 tests nuevos (parseando el JSON generado con `serde_json`, no comparando texto) + a mano contra `examples/users.link` regenerado, inspeccionando el `openapi.json` real byte a byte. Ver GRAMMAR.md §3.133.

**Hecho** (25/08/2026): **`@infinite(cursor, limit)` -- scroll infinito real**. Vuelta a mejoras (no bugs) tras cerrar el audit de bugs de Result/ADT/generics: ninguno de los tres tipos de hook sabía manejar paginación -- un componente con scroll infinito gestionaba el cursor a mano. Nueva anotación que nombra los dos parámetros con rol de cursor/limit en un rpc, exigiendo las MISMAS firmas que `db.<c>.pageAfter(cursor: Int?, limit: Int)` (§3.61, el único mecanismo de paginación por cursor del lenguaje) y que el retorno sea `T[]` con `T` teniendo `id: Int`. Genera `use{Servicio}{Rpc}Infinite` en vez del hook de Query normal para ese rpc -- `data` aplanada, `hasNextPage` por heurístico de largo de página (sin conteo total), próximo cursor = `id` del último elemento (mismo criterio que `pageAfter` usa puertas adentro). Sin cache compartido entre instancias (alcance v0, a diferencia de Query). Demostrado en `examples/taskboard`: `listPaged(cursor, limit)` + una sección "Historial paginado" real en `App.tsx`. Verificado con 11 tests nuevos (parser/checker/codegen) + a mano contra un `linkc serve` real: 7 tareas, `limit=3`, exactamente 3 páginas (3+3+1), sin duplicados, orden correcto -- y `examples/taskboard/frontend` regenerado tipando limpio contra React 18 real. Ver GRAMMAR.md §3.134.

**Hecho** (25/08/2026): **Versión bundle -- los cuatro límites de alcance documentados a lo largo de la sesión (§3.124, §3.129, §3.134), cerrados juntos en una sola ronda**, a pedido explícito del usuario ("recopilá todo lo que haríamos en otras versiones relacionadas con TypeScript y la compatibilidad, y terminá todo eso en una sola versión"):
1. **Cache de Query aislado por instancia de `client`** -- de `Map<string, ...>` a nivel de módulo a `WeakMap<client, Map<string, ...>>`, para que dos instancias de `client` distintas (multi-tenant, múltiples sesiones) nunca compartan cache entre sí, mientras múltiples componentes con el MISMO client lo siguen compartiendo igual que antes.
2. **`AbortSignal` real dentro de los hooks** -- Query gana `AbortController` reference-counted (cancela el fetch compartido SOLO cuando el último componente que lo mira se desmonta, vía `entry.listeners.size === 0`); Mutation e Infinite ganan `AbortSignal`/`AbortController` sin reference counting (su estado no es compartido entre "líneas de trabajo" concurrentes, cancelar siempre es seguro). De paso, `tsc` (no un test) atrapó una regresión real: el primer intento del `catch` de Query mezclaba `return`/`throw`, rompiendo el tipo de `entry.promise` -- arreglado relanzando en los dos caminos.
3. **Mutaciones optimistas** -- `mutate`/`mutateAsync` ganan `options?.optimisticData`, mostrado en `data` antes de la red, reemplazado por el valor real en éxito o revertido a `null` en fallo (rollback ligado al mismo `requestIdRef` de siempre). Alcance deliberado: el optimismo es sobre el `data` PROPIO de la Mutation, no sobre el cache de una Query relacionada -- los targets de `@invalidates` pueden tener formas heterogéneas, un updater tipado contra eso es una pieza de diseño más grande que esta ronda no amerita.
4. **Cache de Infinite compartido entre instancias** -- mismo `WeakMap`/`useSyncExternalStore`/dedupe/abort reference-counted que Query, cerrando el último "alcance v0" que quedaba documentado.

Demostrado en `examples/taskboard/frontend/src/App.tsx`: `createTask` ahora pasa `optimisticData`, con un indicador visible mientras se confirma. Verificado con 8 tests nuevos en `codegen::ts_emit` + a mano contra un `linkc serve` real (Node, sin React): cache aislado por client confirmado con dos instancias reales; `AbortController` reference-counted confirmado con dos "listeners" simulados; rollback optimista confirmado con una mutación real exitosa Y una fallida; dedupe de Infinite confirmado con dos "instancias" simultáneas generando un solo fetch. `examples/taskboard/frontend` regenerado tipando limpio contra React 18 real (atrapando de paso la regresión de tipos del punto 2). Ver GRAMMAR.md §3.135-§3.138.

### 9.14 Núcleo del lenguaje y JWT -- reporte MyFinance, ronda de hardening/completado (30/08/2026)

**Origen**: reporte cruzado de otra sesión de Claude trabajando sobre MyFinance en producción (`backend-link/main.link`), tras una pasada real de seguridad + cálculos fiscales + deuda técnica ya desplegada. 5 ítems reportados, verificados uno por uno contra el código real (`checker.rs`, no contra la documentación) antes de sumarlos acá -- 4 confirmados como límite real, 1 descartado (`.toLowerCase()`/`.toUpperCase()`: YA EXISTEN como `.toLower()`/`.toUpper()`, `checker.rs:4144-4150` y GRAMMAR.md línea 2564 -- el reporte buscó el nombre estilo Java/JS, no el nombre real, más corto, que ya tiene el lenguaje. Sin acción de este lado, se lo aviso a la sesión de MyFinance directamente).

1. **`String` sin slicing/replace/split/pad**: superficie real hoy (`checker.rs:4115-4155`) es `length`/`contains`/`startsWith`/`endsWith`/`trim`/`toUpper`/`toLower`/`escapeHtml` -- sin `.substring()`, `.replace()`, `.split()`, ni un equivalente a `padStart`/`padEnd`. Bloqueó de verdad portar dos exports contables reales ya auditados y en producción en el Express viejo: formato A3 Contable (fixed-width, necesita padding) y ContaPlus/XDIARIO (necesita sanear `;`/saltos de línea de un campo de texto libre antes de unirlo con `;`, si no un `;` dentro de un concepto corrompe las columnas del software real de la gestoría). Quedó como stub documentado en MyFinance en vez de un export que "parece" correcto pero está sutilmente mal.
2. **Sin listas mutables ni concatenación**: `+` (`checker.rs:3869-3882`) solo acepta `Int+Int`/`Int64+Int64`/`Decimal+Decimal`/`Float+Float`/`String+String` -- ningún operando de lista. `List<T>` (`checker.rs:4510-4577`) tiene `join`/`reverse`/`take`/`length`/`sum`/`filter`/`map`, sin `push`/`append`/mutación, y no existe ningún `Set<T>`. Bloqueó marcar facturas "ya conciliadas" durante conciliación bancaria (para no cruzar la misma factura contra dos movimientos del mismo importe exacto) -- quedó como límite documentado, funciona bien en el caso común, puede duplicar en el edge case de importes idénticos. El mismo hueco forzó un `COUNT(DISTINCT proveedor)` para un modelo fiscal a un doble loop O(n²) comparando contra filas anteriores -- aceptable al volumen real de un trimestre, pero es un workaround, no una solución.
3. ~~**JWT sin accessor genérico de claims**~~ -- **RESUELTO (31/08/2026)**: `auth.claim(name: String) -> String?`, mismo mecanismo que `currentRole`/`currentUserId` pero con el nombre del claim como argumento, sin flag de CLI nuevo. Cierra el riesgo de seguridad real citado -- `auth.claim("tokenVersion") == real.toString()` revoca un JWT sin esperar su expiración natural. Ver GRAMMAR.md §3.197.
4. ~~**`Timestamp` sin aritmética**~~ -- **RESUELTO (31/08/2026)**: `.addMillis`/`.addSeconds`/`.addMinutes`/`.addHours`/`.addDays`, aritmética entera pura (`Value::Timestamp` ya es milisegundos planos) con `checked_mul`/`checked_add`. `now().addMinutes(5)` resuelve el caso real citado (expiración de OTP). Ver GRAMMAR.md §3.196.

Ninguno atacado todavía esta ronda -- quedan documentados con evidencia real para priorizar, siguiendo el mismo criterio del resto de esta sección (resolver el caso evidenciado cuando se ataque, no antes).

**Ataque planeado (31/08/2026)**: los 4 ítems de arriba, más un bug crítico no relacionado (`Decimal == Decimal` siempre `false` en runtime, encontrado durante la validación de diseño) van en un plan de 5 piezas -- ver commits siguientes para el detalle de cada una.

**Segundo reporte de MyFinance (31/08/2026), tras una auditoría explícita pedida por el usuario -- "¿qué le falta a Link para reemplazar Node/Express del todo, no solo un subconjunto?"**. 2 hallazgos genuinamente nuevos, de tamaño mucho mayor a los 4 de arriba -- quedan documentados para una ronda propia futura, deliberadamente NO sumados al ataque en curso de los 4 ítems de arriba:

5. **Sin generación de PDF/Excel real**: MYF genera facturas/presupuestos en PDF real (`pdfkit`) y hace parsing/generación de Excel (`exceljs`) para exports e importación de extractos bancarios. c-script no tiene ningún primitivo para generar bytes de PDF/XLSX -- solo puede adjuntar un blob YA generado (base64) a un email. Sin esto, facturación con PDF real no puede migrar nunca. Alcance grande: necesitaría sumar una dependencia de generación de documentos (excepción nueva a "cero dependencias"), diseño propio de API (¿qué subconjunto de PDF/XLSX cubrir?), decisión de alcance previa antes de atacar -- no es una ronda autocontenida como las 4 de arriba.
6. **Sin protocolo MCP real (sesión + streaming bidireccional)**: MYF expone un servidor MCP real para claude.ai (`@modelcontextprotocol/sdk`, `StreamableHTTPServerTransport`, `mcp-session-id`, framing JSON-RPC `tools/list`/`tools/call`, OAuth2). El mecanismo de `stream` de c-script hoy es SSE unidireccional (`subscribe()`) -- sin concepto de sesión ni de streaming bidireccional. Portar esto de verdad necesita más que RPCs request/response. Alcance grande, cambio real al modelo de ejecución (mismo tipo de ítem que PLAN.md §9.4 ítem 1, "hooks de middleware", que se dejó explícitamente abierto por el mismo motivo) -- necesita su propio discovery antes de diseñar nada.

**Confirmado como YA cubierto por el plan en curso, no un ítem nuevo**: el reporte pide "sesión de admin revocable al instante, sin cookies" -- exactamente lo que la Pieza 2 del plan en curso (`auth.claim(name) -> String?`) resuelve: comparar `auth.claim("tokenVersion")` contra el valor real en DB en cada rpc da revocación tan rápida como la siguiente request de esa sesión, sin necesitar cookies. Nada nuevo que sumar acá.

**Confirmado como ya trackeado, no nuevo**: SMTP asíncrono (PLAN.md §8.3.3) -- este reporte lo refuerza con evidencia real (8+ triggers de email en producción), no agrega alcance nuevo.

**Explícitamente fuera de alcance de este proyecto, por decisión propia del reporte** (no se suma acá): SSR de React (decisión de arquitectura de MyFinance, no algo que un compilador de backend deba resolver), OCR de recibos (MyFinance ya tiene un camino 100% portable hoy, vía `http.postWithHeaders` a una API de visión -- adoptarlo es decisión suya), servir el bundle estático del frontend (ya vive detrás de Nginx, sin requisito nuevo).

---

### Sobre el nombre

Decidido: **c-script**, en minúsculas. La extensión de archivo (`.link`) y el nombre del binario del compilador (`linkc`) se mantienen como está — no hace falta que deletreen la marca, igual que `.rs` no dice "rust". Ver [GRAMMAR.md](GRAMMAR.md) para el resto de las convenciones de nomenclatura.

